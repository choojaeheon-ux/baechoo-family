# 예방접종 차수(몇차) 재설계 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 예방접종을 백신별 차수(1차~N차) 기록 구조로 재설계하고, 배추 실제 접종 내역을 DB에 적재한다.

**Architecture:** `BaechooVaccine`의 `history: string[]`·`lastDone`을 제거하고 `doses: {n, date}[]`로 교체. 최근 접종일은 doses에서 파생. UI는 카드 요약 + 상세시트(차수 목록 관리). 마이그레이션 0014로 컬럼 교체, 1회성 임포트로 배추 7종 적재. 모델 변경은 types/repo/UI에 동시 적용해 빌드 무결성 유지.

**Tech Stack:** Next.js 16 · React 19 · TypeScript · Tailwind v4 · Supabase

## Global Constraints

- Next.js 16/React 19. **테스트 하네스 없음** — 검증 = `npx tsc --noEmit` + `npx eslint` + `npm run build`.
- `react-hooks/set-state-in-effect`: effect 안 setState 금지(해당 컴포넌트엔 effect 없음).
- 저장소 이중화(localStorage/Supabase) + 소프트삭제 유지. ID는 `newId()`, 폼 저장 시 빈 문자열 → `save*`가 채움.
- 날짜 절대값 `YYYY-MM-DD`. 한글 INSERT는 node https + UTF-8(`Buffer.from`).
- 커밋은 변경 파일만 스테이징, 트레일러 없이. 푸시 금지.
- 개인 Supabase ref `jyfjnxvsblfkkbscddnw`, 토큰은 `~/.claude.json`의 `SUPABASE_ACCESS_TOKEN`(출력 금지). 회사 MCP(`Supabase_2`) 사용 금지.

---

## File Structure

| 파일 | 역할 | 변경 |
| --- | --- | --- |
| `lib/types.ts` | `VaccineDose` 추가, `BaechooVaccine`에서 lastDone/history 제거 + doses 추가, `STANDARD_VACCINES` 7종 교체 | 수정 |
| `lib/vaccine.ts` | `latestDose`·`nextDoseNumber` 파생 헬퍼 | 생성 |
| `lib/repo.ts` | `toVaccine`/`fromVaccine`를 doses 기반으로 교체 | 수정 |
| `components/baechoo/VaccineList.tsx` | 카드(최근 N차 표시)·complete(차수 추가)·addStandard(doses) | 수정 |
| `components/baechoo/forms.tsx` | `VaccineForm` → `VaccineDetailSheet`(차수 목록 관리) | 수정 |
| `supabase/migrations/0014_vaccine_doses.sql` | doses 컬럼 추가, history/last_done 제거 | 생성 |

`lib/data-context.tsx`는 `BaechooVaccine`을 제네릭하게 다뤄 구조 변경 불필요(타입만 재컴파일).

---

## Task 1: 차수 모델 코드 변경 (types · 헬퍼 · repo · UI)

모델·UI를 원자적으로 교체해 빌드 무결성을 유지한다.

**Files:**
- Modify: `lib/types.ts`
- Create: `lib/vaccine.ts`
- Modify: `lib/repo.ts`
- Modify: `components/baechoo/VaccineList.tsx`
- Modify: `components/baechoo/forms.tsx`

**Interfaces:**
- Produces:
  - `interface VaccineDose { n: number; date: string }`
  - `interface BaechooVaccine { id; name; nextDue: string|null; intervalMonths: number; doses: VaccineDose[]; memo: string|null; createdAt: string }`
  - `latestDose(doses): VaccineDose | null`, `nextDoseNumber(doses): number` (`lib/vaccine.ts`)
  - `VaccineDetailSheet`(named export, forms.tsx)

- [ ] **Step 1: 타입 교체 (`lib/types.ts`)**

기존 배추 예방접종 블록(`export interface BaechooVaccine {...}` + `STANDARD_VACCINES`)을 통째로 아래로 교체:

```ts
/* ───────────── 배추 예방접종 ───────────── */

// 차수별 접종 기록
export interface VaccineDose {
  n: number;      // 차수 (1, 2, ... 비연속 가능)
  date: string;   // 접종일 (YYYY-MM-DD)
}

// 예방접종 — 백신별 차수 기록. 최근 접종일은 doses에서 파생.
export interface BaechooVaccine {
  id: string;
  name: string;
  nextDue: string | null;     // 다음 예정일 (YYYY-MM-DD, 없으면 null)
  intervalMonths: number;     // 추가접종 주기(개월, 기본 12)
  doses: VaccineDose[];       // 차수별 접종 기록
  memo: string | null;
  createdAt: string;          // YYYY-MM-DD
}

// 표준 백신 프리셋 ("표준 백신 추가" 버튼) — 배추 정기 7종
export const STANDARD_VACCINES: string[] = [
  "켄넬코프백신",
  "코로나장염백신",
  "종합백신",
  "광견병백신",
  "심장사상충",
  "외부기생충",
  "신종인플루엔자백신",
];
```

- [ ] **Step 2: 파생 헬퍼 생성 (`lib/vaccine.ts`)**

```ts
import type { VaccineDose } from "./types";

// 접종일이 가장 늦은 차수 (없으면 null) — 최근 접종 표시·정렬용
export function latestDose(doses: VaccineDose[]): VaccineDose | null {
  if (doses.length === 0) return null;
  return doses.reduce((a, b) => (b.date > a.date ? b : a));
}

// 다음 차수 번호 = max(n) + 1 (없으면 1)
export function nextDoseNumber(doses: VaccineDose[]): number {
  return doses.reduce((m, d) => Math.max(m, d.n), 0) + 1;
}
```

- [ ] **Step 3: repo 매퍼 교체 (`lib/repo.ts`)**

(a) import 타입에 `VaccineDose` 추가 — `BaechooVaccine,` 다음 줄:
```ts
  VaccineDose,
```
(b) `toVaccine`/`fromVaccine` 정의를 통째로 교체:
```ts
// 배추 — 예방접종 (doses는 jsonb 배열 [{n,date}])
const toVaccine = (r: Record<string, unknown>): BaechooVaccine => ({
  id: r.id as string,
  name: (r.name as string) ?? "",
  nextDue: (r.next_due as string) ?? null,
  intervalMonths: Number(r.interval_months ?? 12),
  doses: Array.isArray(r.doses) ? (r.doses as VaccineDose[]) : [],
  memo: (r.memo as string) ?? null,
  createdAt: (r.created_at as string) ?? "",
});
const fromVaccine = (x: BaechooVaccine) => ({
  id: x.id,
  name: x.name,
  next_due: x.nextDue,
  interval_months: x.intervalMonths,
  doses: x.doses,
  memo: x.memo,
  created_at: x.createdAt || null,
});
```
(repo의 나머지 — lsRead/emptySnapshot/loadAll/save·delete/휴지통 — 는 `baechooVaccines` 키만 쓰므로 변경 불필요.)

- [ ] **Step 4: VaccineList 교체 (`components/baechoo/VaccineList.tsx`)**

import 2줄 교체/추가:
```ts
import { VaccineDetailSheet } from "./forms";
import { latestDose, nextDoseNumber } from "@/lib/vaccine";
```
(기존 `import { VaccineForm } from "./forms";` 줄을 위 두 줄로 대체. `STANDARD_VACCINES, type BaechooVaccine` import는 유지.)

`complete` 함수 교체:
```ts
  // 빠른 기록: 다음 차수를 오늘 날짜로 추가 + 다음 예정일 = 오늘+주기
  async function complete(v: BaechooVaccine) {
    const today = todayISO();
    await saveBaechooVaccine({
      ...v,
      doses: [...v.doses, { n: nextDoseNumber(v.doses), date: today }],
      nextDue: addMonths(today, v.intervalMonths),
    });
  }
```

`addStandard`의 save 객체 교체(history/lastDone 제거):
```ts
        await saveBaechooVaccine({
          id: "",
          name,
          nextDue: null,
          intervalMonths: 12,
          doses: [],
          memo: null,
          createdAt: todayISO(),
        });
```

빈 상태 버튼 라벨 교체:
```tsx
            표준 백신 추가 (배추 정기 7종)
```

카드의 최근 접종 표시 교체 — `sorted.map((v) => {` 블록에서 `const left = ...` 다음에 `const last = latestDose(v.doses);` 추가하고, 최근 접종 `<span>`을 교체:
```tsx
              const left = v.nextDue ? dday(v.nextDue) : null;
              const last = latestDose(v.doses);
```
```tsx
                    <span className="block text-[11px] text-stone">
                      {last ? `최근 ${last.n}차 · ${last.date}` : "접종 기록 없음"}
                    </span>
```

폼 렌더 교체(`<VaccineForm` → `<VaccineDetailSheet`):
```tsx
      {form.open && (
        <VaccineDetailSheet
          key={form.initial?.id ?? "new"}
          open={form.open}
          onClose={() => setForm({ open: false })}
          initial={form.initial}
        />
      )}
```

- [ ] **Step 5: VaccineForm → VaccineDetailSheet 교체 (`components/baechoo/forms.tsx`)**

(a) 타입 import에 `VaccineDose` 추가 — `type BaechooVaccine,` 다음 줄:
```ts
  type VaccineDose,
```
(b) 파일 상단 import에 헬퍼 추가(다른 `./` import들 근처, 예: `import CategorySelect from "./CategorySelect";` 다음 줄):
```ts
import { nextDoseNumber } from "@/lib/vaccine";
```
(c) 기존 `export function VaccineForm({...}) {...}` 전체(`/* ───── 예방접종 폼 ───── */` 주석 포함, 함수 닫는 `}`까지)를 아래로 교체:
```tsx
/* ───────────── 예방접종 상세시트 (차수 관리) ───────────── */
export function VaccineDetailSheet({
  open,
  onClose,
  initial,
}: {
  open: boolean;
  onClose: () => void;
  initial?: BaechooVaccine;
}) {
  const { saveBaechooVaccine, removeBaechooVaccine } = useData();
  const [name, setName] = useState(initial?.name ?? "");
  const [doses, setDoses] = useState<VaccineDose[]>(initial?.doses ?? []);
  const [nextDue, setNextDue] = useState(initial?.nextDue ?? "");
  const [interval, setInterval] = useState(String(initial?.intervalMonths ?? 12));
  const [memo, setMemo] = useState(initial?.memo ?? "");

  const valid = name.trim().length > 0;

  function updateDose(i: number, patch: Partial<VaccineDose>) {
    setDoses((prev) => prev.map((d, idx) => (idx === i ? { ...d, ...patch } : d)));
  }
  function addDose() {
    setDoses((prev) => [...prev, { n: nextDoseNumber(prev), date: todayISO() }]);
  }
  function removeDose(i: number) {
    setDoses((prev) => prev.filter((_, idx) => idx !== i));
  }

  async function submit() {
    if (!valid) return;
    const months = Number(interval) || 12;
    const cleaned = doses
      .filter((d) => d.date)
      .map((d) => ({ n: Number(d.n) || 1, date: d.date }))
      .sort((a, b) => a.n - b.n);
    await saveBaechooVaccine({
      id: initial?.id ?? "",
      name: name.trim(),
      nextDue: nextDue || null,
      intervalMonths: months,
      doses: cleaned,
      memo: memo.trim() || null,
      createdAt: initial?.createdAt ?? todayISO(),
    });
    onClose();
  }

  return (
    <Sheet open={open} onClose={onClose} title={initial ? "백신 관리" : "백신 추가"}>
      <Field label="백신 이름">
        <input
          className={inputCls}
          value={name}
          onChange={(e) => setName(e.target.value)}
          placeholder="예: 종합백신, 광견병백신"
        />
      </Field>

      <Field label="차수별 접종일">
        <div className="space-y-2">
          {doses.length === 0 && (
            <p className="text-xs text-stone">아직 접종 차수가 없어요. 아래에서 추가하세요.</p>
          )}
          {doses.map((d, i) => (
            <div key={i} className="flex items-center gap-2">
              <input
                type="text"
                inputMode="numeric"
                className={`${inputCls} w-12 text-center`}
                value={String(d.n)}
                onChange={(e) =>
                  updateDose(i, { n: Number(e.target.value.replace(/[^0-9]/g, "")) || 0 })
                }
              />
              <span className="shrink-0 text-xs text-stone">차</span>
              <input
                type="date"
                className={`${inputCls} min-w-0 flex-1`}
                value={d.date}
                onChange={(e) => updateDose(i, { date: e.target.value })}
              />
              <button
                type="button"
                onClick={() => removeDose(i)}
                className="shrink-0 px-1 text-sm text-coral"
              >
                삭제
              </button>
            </div>
          ))}
          <button
            type="button"
            onClick={addDose}
            className="w-full rounded-xl border border-dashed border-leaf bg-leaf-light/40 py-2 text-sm font-bold text-leaf-dark active:scale-[0.99]"
          >
            + 차수 추가
          </button>
        </div>
      </Field>

      <div className="flex gap-2">
        <div className="min-w-0 flex-1">
          <Field label="다음 예정일 (선택)">
            <input
              type="date"
              className={inputCls}
              value={nextDue}
              onChange={(e) => setNextDue(e.target.value)}
            />
          </Field>
        </div>
        <div className="min-w-0 flex-1">
          <Field label="주기 (개월)">
            <input
              type="text"
              className={inputCls}
              inputMode="numeric"
              value={interval}
              onChange={(e) => setInterval(e.target.value.replace(/[^0-9]/g, ""))}
              placeholder="12"
            />
          </Field>
        </div>
      </div>

      <Field label="메모 (선택)">
        <input
          className={inputCls}
          value={memo}
          onChange={(e) => setMemo(e.target.value)}
        />
      </Field>

      <div className="mt-2">
        <PrimaryButton onClick={submit} disabled={!valid}>
          저장
        </PrimaryButton>
      </div>
      {initial && (
        <DeleteButton
          onDelete={async () => {
            await removeBaechooVaccine(initial.id);
            onClose();
          }}
        />
      )}
    </Sheet>
  );
}
```

- [ ] **Step 6: 검증**

Run: `cd "C:/Users/MGRV/.claude/baechoo-family" && npx eslint lib/types.ts lib/vaccine.ts lib/repo.ts components/baechoo/VaccineList.tsx components/baechoo/forms.tsx`
Expected: 에러 없음

Run: `cd "C:/Users/MGRV/.claude/baechoo-family" && npm run build`
Expected: 빌드 성공(타입·린트 통과). `VaccineForm` 잔존 참조가 있으면 빌드 실패 → grep으로 확인 후 제거.

- [ ] **Step 7: 커밋**

```bash
cd "C:/Users/MGRV/.claude/baechoo-family"
git add lib/types.ts lib/vaccine.ts lib/repo.ts components/baechoo/VaccineList.tsx components/baechoo/forms.tsx
git commit -m "feat: 예방접종 차수(doses) 모델 재설계 + 상세시트 차수 관리"
```

---

## Task 2: 마이그레이션 0014 (doses 컬럼 교체)

**Files:**
- Create: `supabase/migrations/0014_vaccine_doses.sql`

**Interfaces:**
- Consumes: Task 1의 컬럼 계약(`doses` jsonb)
- Produces: `baechoo_vaccines`에 `doses` 컬럼, `history`·`last_done` 제거

- [ ] **Step 1: 마이그레이션 파일 작성**

Create `supabase/migrations/0014_vaccine_doses.sql`:
```sql
-- 배추가족 — 예방접종 차수(doses) 모델로 전환
-- 0013 baechoo_vaccines에 차수 배열 컬럼 추가, 단순 history/last_done 제거.

alter table baechoo_vaccines add column if not exists doses jsonb not null default '[]'::jsonb;
alter table baechoo_vaccines drop column if exists history;
alter table baechoo_vaccines drop column if exists last_done;
```

- [ ] **Step 2: 개인 프로젝트에 적용 + 검증**

`~/.claude.json`의 `SUPABASE_ACCESS_TOKEN`으로 Management API node https 적용(토큰 출력 금지):
`POST https://api.supabase.com/v1/projects/jyfjnxvsblfkkbscddnw/database/query`, body `{"query": "<위 SQL>"}`.
검증 쿼리:
```sql
select column_name from information_schema.columns where table_name='baechoo_vaccines' order by ordinal_position;
```
Expected: `doses` 존재, `history`·`last_done` 부재.

- [ ] **Step 3: 커밋**

```bash
cd "C:/Users/MGRV/.claude/baechoo-family"
git add supabase/migrations/0014_vaccine_doses.sql
git commit -m "feat: 0014 baechoo_vaccines doses 컬럼(차수 모델)"
```

---

## Task 3: 배추 실제 접종 내역 임포트 (1회성)

**Files:** 없음(코드 변경 아님). 1회성 DB 작업.

**Interfaces:**
- Consumes: Task 2 적용 후의 `baechoo_vaccines`(doses 컬럼)

- [ ] **Step 1: 기존 테스트 행 삭제 + 7종 임포트**

Management API node https로 아래를 한 트랜잭션 SQL로 실행(한글 UTF-8). doses는 jsonb 리터럴:

```sql
delete from baechoo_vaccines;
insert into baechoo_vaccines (id, name, next_due, interval_months, doses, memo, created_at) values
(gen_random_uuid()::text, '켄넬코프백신', '2027-06-27', 12, '[{"n":1,"date":"2023-06-15"},{"n":2,"date":"2023-06-29"},{"n":3,"date":"2024-06-20"},{"n":4,"date":"2025-06-20"},{"n":5,"date":"2026-06-27"}]'::jsonb, null, now()),
(gen_random_uuid()::text, '코로나장염백신', '2027-06-27', 12, '[{"n":1,"date":"2023-05-30"},{"n":2,"date":"2023-06-15"},{"n":3,"date":"2024-06-20"},{"n":4,"date":"2025-06-20"},{"n":5,"date":"2026-06-27"}]'::jsonb, null, now()),
(gen_random_uuid()::text, '종합백신', '2027-06-27', 12, '[{"n":5,"date":"2023-05-30"},{"n":6,"date":"2024-06-20"},{"n":7,"date":"2025-06-20"},{"n":8,"date":"2026-06-27"}]'::jsonb, null, now()),
(gen_random_uuid()::text, '광견병백신', '2026-05-02', 12, '[{"n":1,"date":"2023-05-15"},{"n":2,"date":"2024-05-17"},{"n":3,"date":"2025-05-02"}]'::jsonb, null, now()),
(gen_random_uuid()::text, '심장사상충', null, 12, '[{"n":1,"date":"2023-05-24"},{"n":2,"date":"2023-08-31"}]'::jsonb, null, now()),
(gen_random_uuid()::text, '외부기생충', '2023-11-11', 12, '[{"n":1,"date":"2023-08-11"}]'::jsonb, null, now()),
(gen_random_uuid()::text, '신종인플루엔자백신', '2024-07-23', 12, '[{"n":1,"date":"2023-06-29"},{"n":2,"date":"2023-07-24"}]'::jsonb, null, now());
```

node 스크립트에서 위 SQL을 JS 문자열로 만들되, doses는 `JSON.stringify([...])`로 생성해 오타를 방지한다.

- [ ] **Step 2: 검증**

```sql
select name, next_due, jsonb_array_length(doses) as dose_count, doses from baechoo_vaccines order by name;
```
Expected: 7행. dose_count = 켄넬코프5·코로나5·종합4·광견병3·심장사상충2·외부기생충1·신종인플루엔자2. 한글 백신명 정상.

- [ ] **Step 3: 라이브 반영 확인**

마이그레이션·임포트는 클라우드 직접 반영이므로 앱 코드 배포와 무관하게 데이터는 즉시 존재. 단 차수 UI는 Task 1 코드가 배포돼야 보임 → main 병합·push 후 라이브에서 확인.

---

## Self-Review

**1. Spec coverage**

| 스펙 | 태스크 |
| --- | --- |
| doses 모델(차수+날짜), lastDone/history 제거 | Task 1 Step 1, 3 |
| 최근 접종 파생(latestDose), nextDoseNumber | Task 1 Step 2 |
| 프리셋 7종 교체 | Task 1 Step 1 |
| 카드 요약(최근 N차)+D-day | Task 1 Step 4 |
| 카드 ✓ = 다음 차수 추가 + nextDue+주기 | Task 1 Step 4 |
| 상세시트 차수 목록 관리(추가/수정/삭제)+메타 편집+2단계 삭제 | Task 1 Step 5 |
| 마이그레이션 0014(doses 추가, history/last_done 제거) | Task 2 |
| 테스트행 삭제 + 7종 임포트(켄넬코프5·코로나5·종합8·…, 26.06.27 반영) | Task 3 |
| 한글 INSERT node https UTF-8 | Task 3 Step 1 |

누락 없음.

**2. Placeholder scan**: TBD/모호 표현 없음. 모든 코드 스텝에 완전 코드.

**3. Type consistency**: `VaccineDose{n,date}`·`BaechooVaccine{...doses}` 정의(Task1 Step1)와 사용(repo Step3, vaccine.ts Step2, VaccineList Step4, forms Step5) 일치. `latestDose`/`nextDoseNumber` 정의처(vaccine.ts)·사용처(VaccineList·forms) 일치. `VaccineDetailSheet` export(forms)·import(VaccineList) 일치. DB `doses` 컬럼(Task2)·매퍼(Task1)·임포트(Task3) 일치. 제거된 `VaccineForm`·`lastDone`·`history` 잔존 참조는 Task1 Step6 빌드/grep에서 차단.
