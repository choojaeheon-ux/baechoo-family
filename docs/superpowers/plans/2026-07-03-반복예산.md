# 매달 반복 예산 (기본 예산 + 월 오버라이드) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 예산을 한 번 설정하면 매달 자동 적용되고, 특정 달·특정 카테고리만 오버라이드할 수 있게 한다.

**Architecture:** 기존 `budgets` 테이블을 재사용하되 `yearMonth`를 nullable로 확장(`null`=기본, `"YYYY-MM"`=그 달 오버라이드). 순수 조회 헬퍼(`lib/compute.ts`)가 "오버라이드 우선, 없으면 기본" 규칙을 담당(vitest 테스트). 예산·목표 탭 UI는 `[기본 예산]`/`[이번 달 조정]` 2섹션으로 나뉘고, 편집 시트에서 적용 범위(기본/이번 달만)를 고른다.

**Tech Stack:** Next.js 16, React 19, TypeScript, Supabase, vitest.

## Global Constraints

- 언어: 모든 UI 카피 한국어. `"use client"` 컴포넌트
- 저장: `hasSupabase`면 Supabase, 아니면 localStorage — `saveBudget`/`removeBudget`(기존 mutator) 그대로 사용, 신규 저장소 코드 없음
- `yearMonth`: `null` = 기본 예산(매달), `"YYYY-MM"` = 그 달 오버라이드. `(categoryId, yearMonth)` 조합이 예산 레코드를 유일 식별
- `budgetForCategory` 반환 타입은 `number | null` 유지(둘 다 없으면 null) — `components/budget/Analysis.tsx`의 `ReducibleItem.budget: number | null` 소비처 호환. spec 4장은 `?? 0`로 적었으나, 기존 소비처 호환을 위해 null 유지가 정확(이 플랜이 우선)
- 회귀 없음: 기존 예산은 전부 `"YYYY-MM"`이라 "그 달 오버라이드"로 해석돼, 기본 예산을 세팅하기 전까지 현행과 동일 동작
- 마이그레이션은 재헌님이 개인 Supabase SQL Editor에서 실행(또는 publishable key로 REST 불가하므로 SQL 직접) — 코드는 파일만 생성

---

## File Structure

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `lib/types.ts` | `Budget.yearMonth`를 `string \| null`로 | 수정 |
| `lib/repo.ts` | `toBudget`의 `year_month` null 허용 | 수정 |
| `supabase/migrations/0016_budget_nullable_yearmonth.sql` | `year_month` drop not null | 신규 |
| `lib/compute.ts` | `budgetForCategory`·`totalBudget` 오버라이드 로직 | 수정 |
| `lib/compute.test.ts` | 조회 헬퍼 단위 테스트 | 신규 |
| `components/budget/forms.tsx` | `BudgetForm`에 적용 범위(기본/이번 달) 선택 + `initial` prop | 수정 |
| `components/budget/Plans.tsx` | 예산 영역을 `[기본 예산]`/`[이번 달 조정]` 2섹션으로 | 수정 |

---

## Task 1: 데이터 모델 nullable 확장

**Files:**
- Modify: `lib/types.ts` (Budget.yearMonth)
- Modify: `lib/repo.ts` (toBudget)
- Create: `supabase/migrations/0016_budget_nullable_yearmonth.sql`

**Interfaces:**
- Produces: `Budget { id: string; yearMonth: string | null; categoryId: string | null; amount: number }`

- [ ] **Step 1: types.ts에서 yearMonth nullable로**

`lib/types.ts`의 `Budget` 인터페이스를 수정:
```ts
export interface Budget {
  id: string;
  yearMonth: string | null; // null = 기본 예산(매달 적용), "YYYY-MM" = 그 달 오버라이드
  categoryId: string | null; // null = 전체 예산
  amount: number;
}
```

- [ ] **Step 2: repo.ts toBudget에서 null 허용**

`lib/repo.ts`의 `toBudget`를 수정(`year_month`가 null일 수 있음):
```ts
const toBudget = (r: Record<string, unknown>): Budget => ({
  id: r.id as string,
  yearMonth: (r.year_month as string) ?? null,
  categoryId: (r.category_id as string) ?? null,
  amount: Number(r.amount),
});
```
(`fromBudget`은 `year_month: x.yearMonth`가 null을 그대로 보내므로 수정 불필요.)

- [ ] **Step 3: 마이그레이션 파일 작성**

`supabase/migrations/0016_budget_nullable_yearmonth.sql`:
```sql
-- 배추가족 — 매달 반복 예산: year_month를 nullable로(null = 기본 예산, 'YYYY-MM' = 그 달 오버라이드)
-- 개인 Supabase SQL Editor에서 실행하세요.
alter table budgets alter column year_month drop not null;
```

- [ ] **Step 4: 빌드 확인**

Run: `npm run build`
Expected: 타입 에러 없이 성공

- [ ] **Step 5: 커밋**

```bash
git add lib/types.ts lib/repo.ts supabase/migrations/0016_budget_nullable_yearmonth.sql
git commit -m "feat(budget): Budget.yearMonth nullable 확장 + 마이그레이션(반복 예산 기반)"
```

---

## Task 2: 조회 헬퍼 오버라이드 로직 (TDD)

**Files:**
- Modify: `lib/compute.ts` (budgetForCategory, totalBudget)
- Create: `lib/compute.test.ts`

**Interfaces:**
- Consumes: `Budget` (Task 1)
- Produces:
  - `budgetForCategory(budgets: Budget[], ym: string, categoryId: string | null): number | null` — 오버라이드 우선, 없으면 기본, 둘 다 없으면 null
  - `totalBudget(budgets: Budget[], ym: string): number` — 전체 예산(categoryId=null) 적용값 있으면 그것, 없으면 카테고리별 적용값 합

- [ ] **Step 1: 실패하는 테스트 작성**

`lib/compute.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { budgetForCategory, totalBudget } from "./compute";
import type { Budget } from "./types";

const b = (yearMonth: string | null, categoryId: string | null, amount: number): Budget =>
  ({ id: `${yearMonth}-${categoryId}`, yearMonth, categoryId, amount });

describe("budgetForCategory", () => {
  it("기본 예산만 있으면 그 값(모든 달)", () => {
    const budgets = [b(null, "cat-food", 300000)];
    expect(budgetForCategory(budgets, "2026-06", "cat-food")).toBe(300000);
    expect(budgetForCategory(budgets, "2026-09", "cat-food")).toBe(300000);
  });
  it("그 달 오버라이드가 있으면 오버라이드 우선", () => {
    const budgets = [b(null, "cat-food", 300000), b("2026-06", "cat-food", 400000)];
    expect(budgetForCategory(budgets, "2026-06", "cat-food")).toBe(400000);
    expect(budgetForCategory(budgets, "2026-07", "cat-food")).toBe(300000);
  });
  it("기본도 오버라이드도 없으면 null", () => {
    expect(budgetForCategory([], "2026-06", "cat-food")).toBe(null);
  });
});

describe("totalBudget", () => {
  it("전체 예산(categoryId=null) 기본값 우선", () => {
    const budgets = [b(null, null, 1000000), b(null, "cat-food", 300000)];
    expect(totalBudget(budgets, "2026-06")).toBe(1000000);
  });
  it("전체 예산 없으면 카테고리별 적용값 합(오버라이드 반영)", () => {
    const budgets = [
      b(null, "cat-food", 300000),
      b(null, "cat-living", 200000),
      b("2026-06", "cat-food", 400000),
    ];
    expect(totalBudget(budgets, "2026-06")).toBe(600000); // 400000 + 200000
    expect(totalBudget(budgets, "2026-07")).toBe(500000); // 300000 + 200000
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL — 현재 `budgetForCategory`는 `yearMonth === ym`만 매칭하므로 기본 예산(null) 케이스가 틀림

- [ ] **Step 3: budgetForCategory / totalBudget 구현**

`lib/compute.ts`에서 두 함수를 교체:
```ts
export function budgetForCategory(
  budgets: Budget[],
  ym: string,
  categoryId: string | null
): number | null {
  const override = budgets.find(
    (x) => x.yearMonth === ym && x.categoryId === categoryId
  );
  if (override) return override.amount;
  const base = budgets.find(
    (x) => x.yearMonth === null && x.categoryId === categoryId
  );
  return base ? base.amount : null;
}

export function totalBudget(budgets: Budget[], ym: string): number {
  const overall = budgetForCategory(budgets, ym, null); // 전체 예산(카테고리 null)
  if (overall !== null) return overall;
  const catIds = new Set(
    budgets.filter((x) => x.categoryId !== null).map((x) => x.categoryId!)
  );
  let sum = 0;
  for (const id of catIds) {
    const v = budgetForCategory(budgets, ym, id);
    if (v !== null) sum += v;
  }
  return sum;
}
```
(`budgetForCategory`의 세 번째 인자 타입이 `string` → `string | null`로 넓어진다. 기존 호출부 `budgetForCategory(budgets, ym, c.id)`는 `c.id: string`이라 그대로 호환.)

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`
Expected: PASS (기존 pnl 테스트 포함 전체 통과)

- [ ] **Step 5: 빌드 확인**

Run: `npm run build`
Expected: 성공 (Analysis.tsx·Dashboard.tsx의 기존 호출이 새 시그니처와 호환)

- [ ] **Step 6: 커밋**

```bash
git add lib/compute.ts lib/compute.test.ts
git commit -m "feat(budget): 예산 조회 오버라이드 로직(기본→월 오버라이드) + 테스트"
```

---

## Task 3: 예산 편집 폼 — 적용 범위 선택

**Files:**
- Modify: `components/budget/forms.tsx` (BudgetForm)

**Interfaces:**
- Consumes: `useData().saveBudget`, `budgets` (Task 1 타입)
- Produces: `BudgetForm({ open, onClose, ym, initial? })` — `initial?: Budget`로 수정 대상 전달. 적용 범위 `"base" | "month"` 토글

- [ ] **Step 1: BudgetForm에 initial prop + 범위 토글 추가**

`components/budget/forms.tsx`의 `BudgetForm` 전체를 교체:
```tsx
export function BudgetForm({
  open,
  onClose,
  ym,
  initial,
}: {
  open: boolean;
  onClose: () => void;
  ym: string;
  initial?: Budget;
}) {
  const { categories, budgets, saveBudget } = useData();
  const cats = categories.filter((c) => c.type === "expense");
  const [scope, setScope] = useState<string>(
    initial ? (initial.categoryId ?? "__all__") : "__all__"
  );
  const [range, setRange] = useState<"base" | "month">(
    initial ? (initial.yearMonth === null ? "base" : "month") : "base"
  );
  const [amount, setAmount] = useState(initial ? String(initial.amount) : "");

  const amt = Number(amount.replace(/[^0-9]/g, ""));

  async function submit() {
    if (amt <= 0) return;
    const categoryId = scope === "__all__" ? null : scope;
    const targetYm = range === "base" ? null : ym;
    const existing = budgets.find(
      (b) => b.yearMonth === targetYm && b.categoryId === categoryId
    );
    await saveBudget({
      id: existing?.id ?? "",
      yearMonth: targetYm,
      categoryId,
      amount: amt,
    } as Budget);
    setAmount("");
    onClose();
  }

  return (
    <Sheet open={open} onClose={onClose} title="예산 설정">
      <Field label="대상">
        <select
          className={inputCls}
          value={scope}
          onChange={(e) => setScope(e.target.value)}
        >
          <option value="__all__">전체 월예산</option>
          {cats.map((c) => (
            <option key={c.id} value={c.id}>
              {c.icon} {c.name}
            </option>
          ))}
        </select>
      </Field>
      <Field label="적용 범위">
        <div className="flex gap-1">
          {(["base", "month"] as const).map((r) => (
            <button
              key={r}
              type="button"
              onClick={() => setRange(r)}
              className={`flex-1 rounded-lg py-2 text-sm font-semibold transition ${
                range === r ? "bg-leaf text-white" : "bg-card border border-line text-stone"
              }`}
            >
              {r === "base" ? "기본 예산 (매달)" : `이번 달만 (${ymLabel(ym)})`}
            </button>
          ))}
        </div>
      </Field>
      <Field label="예산 금액">
        <input
          className={inputCls + " text-right tabular"}
          inputMode="numeric"
          value={amount ? Number(amt).toLocaleString("ko-KR") : ""}
          onChange={(e) => setAmount(e.target.value)}
          placeholder="0"
        />
      </Field>
      <div className="mt-2">
        <PrimaryButton onClick={submit} disabled={amt <= 0}>
          저장
        </PrimaryButton>
      </div>
    </Sheet>
  );
}
```

- [ ] **Step 2: import 확인**

`components/budget/forms.tsx` 상단 import에 `ymLabel`이 없으면 추가:
```ts
import { ymLabel } from "@/lib/format";
```
(`Budget` 타입도 이 파일에서 이미 쓰이는지 확인 — 안 쓰이면 `import { ... , type Budget } from "@/lib/types";`에 추가.)

- [ ] **Step 3: 빌드 확인**

Run: `npm run build`
Expected: 성공. (Plans.tsx가 아직 `initial`을 안 넘겨도 optional이라 무방)

- [ ] **Step 4: 커밋**

```bash
git add components/budget/forms.tsx
git commit -m "feat(budget): 예산 폼에 적용 범위(기본/이번 달) 선택 + initial 수정 지원"
```

---

## Task 4: 예산·목표 탭 — 기본/조정 2섹션 UI

**Files:**
- Modify: `components/budget/Plans.tsx`

**Interfaces:**
- Consumes: `budgetForCategory`·`totalBudget` (Task 2), `BudgetForm` with `initial` (Task 3)
- Produces: 없음(최종 UI)

- [ ] **Step 1: Plans.tsx 예산 영역을 2섹션으로 교체**

`components/budget/Plans.tsx`에서 예산 관련 상태·계산·렌더를 아래로 교체한다. 기존 `monthBudgets/overall/catBudgets` 계산과 "월 예산" `SectionTitle`+`Card` 블록을 다음으로 대체(목표·카테고리 관리 등 나머지 섹션은 그대로 유지):

상태·계산 (`spend` 계산 근처):
```tsx
  const [budgetInitial, setBudgetInitial] = useState<Budget | undefined>(undefined);

  const baseBudgets = budgets.filter((b) => b.yearMonth === null);
  const baseOverall = baseBudgets.find((b) => b.categoryId === null);
  const baseCats = baseBudgets.filter((b) => b.categoryId !== null);
  const overrides = budgets.filter((b) => b.yearMonth === ym);
  const spend = spendByCategory(monthTransactions(transactions, ym));

  const openNew = () => { setBudgetInitial(undefined); setBudgetOpen(true); };
  const openEdit = (bud: Budget) => { setBudgetInitial(bud); setBudgetOpen(true); };
```

기본 예산 섹션 렌더:
```tsx
      <SectionTitle
        right={
          <button onClick={openNew} className="text-xs font-semibold text-leaf">
            + 설정
          </button>
        }
      >
        기본 예산 · 매달 적용
      </SectionTitle>
      <Card className="space-y-3">
        {baseOverall && (
          <div className="flex items-center justify-between">
            <span className="text-sm font-semibold text-ink">전체 월예산</span>
            <div className="flex items-center gap-2">
              <span className="text-sm tabular text-ink">{won(baseOverall.amount)}</span>
              <button onClick={() => openEdit(baseOverall)} className="text-xs text-leaf">수정</button>
              <button onClick={() => removeBudget(baseOverall.id)} className="text-xs text-coral">삭제</button>
            </div>
          </div>
        )}
        {baseCats.length === 0 && !baseOverall ? (
          <Empty>
            카테고리별 기본 예산을 설정해 보세요
            <br />
            (한 번 설정하면 매달 적용됩니다)
          </Empty>
        ) : (
          baseCats.map((bud) => {
            const cat = categoryById(bud.categoryId!);
            const applied = budgetForCategory(budgets, ym, bud.categoryId!) ?? bud.amount;
            const used = spend.get(bud.categoryId!) ?? 0;
            return (
              <div key={bud.id}>
                <div className="mb-1 flex items-center justify-between">
                  <span className="text-sm font-semibold text-ink">
                    {cat?.icon} {cat?.name}
                  </span>
                  <div className="flex items-center gap-2">
                    <span className="text-xs text-stone">
                      {won(used)} / {won(applied)}
                    </span>
                    <button onClick={() => openEdit(bud)} className="text-xs text-leaf">수정</button>
                    <button onClick={() => removeBudget(bud.id)} className="text-xs text-coral">삭제</button>
                  </div>
                </div>
                <ProgressBar value={used} max={applied} />
              </div>
            );
          })
        )}
        <div className="border-t border-line pt-2 text-right text-xs text-stone">
          {ymLabel(ym)} 적용 합계 {won(totalBudget(budgets, ym))}
        </div>
      </Card>
```

이번 달 조정 섹션 렌더 (기본 예산 섹션 바로 아래):
```tsx
      {overrides.length > 0 && (
        <>
          <SectionTitle>{ymLabel(ym)} 이번 달 조정</SectionTitle>
          <Card className="space-y-2">
            {overrides.map((ov) => {
              const label =
                ov.categoryId === null ? "전체 월예산" : categoryById(ov.categoryId)?.name ?? "카테고리";
              const icon = ov.categoryId === null ? "" : categoryById(ov.categoryId)?.icon ?? "";
              const base = baseBudgets.find((b) => b.categoryId === ov.categoryId);
              return (
                <div key={ov.id} className="flex items-center justify-between">
                  <span className="text-sm font-semibold text-ink">{icon} {label}</span>
                  <div className="flex items-center gap-2">
                    <span className="text-sm tabular text-ink">{won(ov.amount)}</span>
                    {base && <span className="text-xs text-stone">(기본 {won(base.amount)})</span>}
                    <button onClick={() => openEdit(ov)} className="text-xs text-leaf">수정</button>
                    <button onClick={() => removeBudget(ov.id)} className="text-xs text-coral">기본으로</button>
                  </div>
                </div>
              );
            })}
          </Card>
        </>
      )}
```

- [ ] **Step 2: BudgetForm 호출에 initial 전달**

`Plans.tsx` 하단의 `BudgetForm` 렌더를 수정:
```tsx
      <BudgetForm open={budgetOpen} onClose={() => setBudgetOpen(false)} ym={ym} initial={budgetInitial} />
```
(기존에 `<BudgetForm open={budgetOpen} onClose={...} ym={ym} />` 형태를 찾아 `initial={budgetInitial}` 추가. `setBudgetOpen(false)` 시 `budgetInitial`은 다음 openNew/openEdit에서 재설정되므로 별도 초기화 불필요.)

- [ ] **Step 3: import·타입 확인**

`Plans.tsx` 상단 import에 다음이 있는지 확인하고 없으면 추가:
```ts
import { budgetForCategory, totalBudget, spendByCategory, monthTransactions } from "@/lib/compute";
import { type Budget } from "@/lib/types";
```
(`ymLabel`, `won`, `ProgressBar`, `Empty`, `SectionTitle`, `Card`, `categoryById`, `removeBudget`, `budgets`, `transactions`는 기존에 이미 사용 중.)

- [ ] **Step 4: 빌드 확인**

Run: `npm run build`
Expected: 성공, `/budget` 라우트 생성

- [ ] **Step 5: 앱에서 확인**

Run: `npm run dev` → 가계부 → 예산·목표 탭
- "+ 설정" → 카테고리 선택, 적용 범위 "기본 예산" → 저장 → 기본 예산 섹션에 표시
- 다른 달로 이동해도 기본 예산이 그대로 적용(진행률 max가 기본값)
- 한 카테고리 "수정" → 적용 범위 "이번 달만" → 다른 금액 저장 → "이번 달 조정" 섹션에 표시(기본 N 병기)
- 조정 행 "기본으로" → 오버라이드 삭제 → 기본값 복귀
Expected: 위 흐름 정상 동작

- [ ] **Step 6: 커밋**

```bash
git add components/budget/Plans.tsx
git commit -m "feat(budget): 예산·목표 탭 기본/이번 달 조정 2섹션 UI"
```

---

## Self-Review (완료)

**1. Spec coverage**
- 데이터 모델 nullable → Task 1 ✓
- 조회 로직(오버라이드 우선) → Task 2 ✓
- UI 2섹션 + 편집 범위 선택 + "기본으로" → Task 3·4 ✓
- 전체 월예산(categoryId=null)도 동일 적용 → Task 2 `totalBudget`·Task 3 scope `__all__` + range ✓
- 마이그레이션 → Task 1 ✓
- 삭제 규칙(기본 삭제=제거, 오버라이드 삭제=복귀) → Task 4 removeBudget 두 경로 ✓
- 회귀 없음(기존 "YYYY-MM"=오버라이드 해석) → Task 2 로직상 기존 데이터는 그 달 오버라이드로 잡히고 기본(null) 없으면 그대로 ✓

**2. Placeholder scan:** TBD/TODO 없음. 모든 코드 스텝에 실제 코드.

**3. Type consistency:** `Budget.yearMonth: string | null`(Task 1) → `budgetForCategory(…, categoryId: string | null)`(Task 2) → `BudgetForm initial?: Budget`(Task 3) → `openEdit(bud: Budget)`(Task 4) 일관. `budgetForCategory` 반환 `number | null` 전 태스크 일치.

## 미결(구현 시 확정)
- 기본 예산 삭제 시 그 카테고리의 잔존 오버라이드 처리: 이 플랜은 **기본 레코드만 삭제하고 오버라이드는 남긴다**(spec 8장 기본안). 남은 오버라이드는 "이번 달 조정"에 계속 뜨므로 사용자가 개별 "기본으로" 가능. 별도 정리 UI는 YAGNI로 제외.
- 마이그레이션 0016은 재헌님이 개인 Supabase에서 실행해야 기본 예산(null) 저장 가능(그 전엔 null 저장 시 not-null 위반). 배포 전 실행 안내 필요.
