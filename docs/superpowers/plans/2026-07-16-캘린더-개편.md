# 캘린더 개편 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 배추가족 가족 캘린더를 풀블리드 그리드 + 셀 텍스트 일정 + 자유 카테고리(색 선택)로 개편한다.

**Architecture:** `FamilyEvent`의 고정 `assignee`(추추/배찌/함께) 축을 사용자 관리 엔티티 `EventCategory`(이름·색 hex)로 대체한다. 추추/배찌/함께는 고정 ID(`cat-chuchu/baejji/together`) 기본 카테고리로 시드하고, 색은 앱 톤 팔레트 스와치에서 고른다. 월 그리드는 Card·좌우 여백을 벗겨 화면 전폭으로 키우고 각 셀에 카테고리 색 텍스트 막대를 표시한다.

**Tech Stack:** Next.js 16(App Router)·TS·Tailwind v4·Supabase(supabase-js)·Chart.js(무관)·vitest. 저장소 이중화 `lib/repo.ts`(cloud/localStorage).

## Global Constraints

- ⚠️ **DDL 먼저, 코드 배포 나중.** `sbUpsert`(lib/repo.ts)는 error를 검사하지 않아 스키마-코드 불일치는 크래시가 아니라 **조용한 유실**이 된다. Task 1(마이그레이션)이 모든 코드 태스크보다 먼저 프로덕션에 적용·검증돼야 한다.
- **프로덕션 DDL은 재헌님 명시 승인 후.** auto mode classifier가 차단. 개인 토큰 `~/.claude.json`의 `SUPABASE_ACCESS_TOKEN`(sbp_) → Management API `POST https://api.supabase.com/v1/projects/jyfjnxvsblfkkbscddnw/database/query`, **node https**(PowerShell 아님), `dangerouslyDisableSandbox: true`. "푸시"는 DDL 승인이 아니다. 토큰 끝자리도 로그 금지.
- **한글 시드는 SQL INSERT 금지 → `loadAll` 부트스트랩(JS)로만.** Management API 한글 INSERT는 인코딩 깨짐(plan_items 패턴 준수).
- **동적 색은 Tailwind 클래스가 아니라 `style={{ backgroundColor: hex }}`** 로 적용(purge 대상 아님).
- **`WeekTodo.assignee`(52주 투두 담당)는 손대지 않는다.** FamilyEvent와 타입만 공유했을 뿐 별개 축. 이번 변경은 FamilyEvent 전용.
- **Next16 ESLint 함정**: effect 안 직접 `setState` 금지(`react-hooks/set-state-in-effect`). 조건부 리셋은 파생값으로. `npx eslint <file>`로 사전 확인.
- 개인 프로젝트(회사 무관). 커밋 co-author: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.

---

## File Structure

- `supabase/migrations/0021_event_categories.sql` — **생성**. event_categories 테이블 + family_events.category_id + 백필 + assignee 완화.
- `lib/eventCategoryPalette.ts` — **생성**. 팔레트 hex 배열 + `readableText(hex)` 순수함수.
- `lib/eventCategoryPalette.test.ts` — **생성**. readableText vitest.
- `lib/types.ts` — **수정**. `EventCategory` 추가, `FamilyEvent.assignee`→`categoryId`, `DataSnapshot.eventCategories`.
- `lib/seed.ts` — **수정**. `SEED_EVENT_CATEGORIES` 추가.
- `lib/repo.ts` — **수정**. event_categories 매퍼·loadAll 시드·save/delete, family_event 매퍼 category_id화, `normalizeFamilyEvent`, lsRead/emptySnapshot.
- `lib/data-context.tsx` — **수정**. eventCategories state·refresh·mutator·context 타입.
- `components/calendar/EventForm.tsx` — **수정**. 담당 토글 → 카테고리 선택줄 + 관리 진입.
- `components/calendar/FamilyCalendar.tsx` — **수정**. 색 룩업, 셀 텍스트 막대, 풀블리드, 범례, 관리 진입.
- `components/calendar/CategoryManager.tsx` — **생성**. 카테고리 추가/편집/삭제(재배정) 시트.
- `app/calendar/page.tsx` — **수정**. 가족 탭 그리드 구간 여백 제거(풀블리드).

---

## Task 1: 마이그레이션 0021 (DDL) — 프로덕션 적용·검증

**Files:**
- Create: `supabase/migrations/0021_event_categories.sql`

**Interfaces:**
- Produces: DB에 `event_categories` 테이블(id·name·color·emoji·sort_order·created_at), `family_events.category_id`(백필됨), `family_events.assignee`는 nullable + CHECK 제거.

- [ ] **Step 1: 마이그레이션 SQL 작성** (순수 ASCII)

`supabase/migrations/0021_event_categories.sql`:

```sql
-- 0021: event_categories — user-managed calendar categories (color + name).
-- Replaces the fixed family_events.assignee axis. Korean default names are
-- seeded from JS (loadAll bootstrap), NOT here, to avoid encoding corruption.

create table if not exists event_categories (
  id text primary key,
  name text not null,
  color text not null,
  emoji text,
  sort_order integer not null default 0,
  created_at date
);

alter table event_categories enable row level security;
drop policy if exists family_all on event_categories;
create policy family_all on event_categories for all using (true) with check (true);
create index if not exists idx_event_categories_sort on event_categories (sort_order);

-- family_events: add category_id, backfill from assignee (ASCII enum → safe).
alter table family_events add column if not exists category_id text;
update family_events set category_id = 'cat-' || assignee where category_id is null;

-- New code stops writing assignee; relax NOT NULL + CHECK so inserts succeed.
-- (assignee column kept for rollback; a later migration drops it.)
alter table family_events alter column assignee drop not null;
alter table family_events drop constraint if exists family_events_assignee_check;
```

- [ ] **Step 2: 재헌님께 DDL 적용 승인 요청**

프로덕션 DB에 적용되는 DDL임을 알리고 명시 승인을 받는다. 승인 전 진행 금지.

- [ ] **Step 3: node https로 Management API 적용**

`~/.claude.json`의 `SUPABASE_ACCESS_TOKEN`(sbp_)을 읽어 아래 스크립트를 node로 실행(`dangerouslyDisableSandbox: true`). SQL은 위 파일 내용을 그대로 payload로.

```js
// scratchpad에 apply-0021.js 로 저장 후 node 실행
const https = require("https");
const fs = require("fs");
const cfg = JSON.parse(fs.readFileSync(process.env.USERPROFILE + "/.claude.json", "utf8"));
const token = cfg.SUPABASE_ACCESS_TOKEN; // 로그 출력 금지
const sql = fs.readFileSync("supabase/migrations/0021_event_categories.sql", "utf8");
const body = Buffer.from(JSON.stringify({ query: sql }), "utf8");
const req = https.request({
  hostname: "api.supabase.com",
  path: "/v1/projects/jyfjnxvsblfkkbscddnw/database/query",
  method: "POST",
  headers: { Authorization: "Bearer " + token, "Content-Type": "application/json", "Content-Length": body.length },
}, (res) => { let d = ""; res.on("data", c => d += c); res.on("end", () => console.log(res.statusCode, d)); });
req.write(body); req.end();
```

Expected: `200 []` (또는 빈 결과 배열).

- [ ] **Step 4: 적용 검증 (DB 직접 조회)** — 화면 아님, DB로.

같은 방식으로 검증 쿼리 실행:

```sql
select count(*) filter (where category_id is not null) as backfilled,
       count(*) as total from family_events;
```

그리고 `select column_name, is_nullable from information_schema.columns where table_name='family_events' and column_name in ('assignee','category_id');`

Expected: `backfilled === total` (모든 기존 일정 category_id 채워짐), `assignee` is_nullable=YES, `category_id` 존재. `event_categories` 테이블 존재(select count(*) from event_categories → 0행, 시드는 코드가 넣음).

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/0021_event_categories.sql
git commit -m "feat(db): 0021 event_categories + family_events.category_id 백필"
```

---

## Task 2: 팔레트 + readableText (TDD)

**Files:**
- Create: `lib/eventCategoryPalette.ts`
- Test: `lib/eventCategoryPalette.test.ts`

**Interfaces:**
- Produces: `CATEGORY_PALETTE: string[]`(hex), `readableText(hex: string): string`(대비 글씨색 `#2f2a20` 또는 `#ffffff`).

- [ ] **Step 1: 실패 테스트 작성**

`lib/eventCategoryPalette.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { CATEGORY_PALETTE, readableText } from "./eventCategoryPalette";

describe("readableText", () => {
  it("어두운 배경 → 흰 글씨", () => {
    expect(readableText("#5b8c3e")).toBe("#ffffff"); // leaf
    expect(readableText("#5c93a8")).toBe("#ffffff"); // sky
    expect(readableText("#e07a5f")).toBe("#ffffff"); // coral
  });
  it("밝은 배경 → 짙은 글씨", () => {
    expect(readableText("#d9a441")).toBe("#2f2a20"); // gold
    expect(readableText("#8ab560")).toBe("#2f2a20"); // sprout
    expect(readableText("#ffffff")).toBe("#2f2a20");
  });
  it("팔레트는 최소 10색, 전부 6자리 hex", () => {
    expect(CATEGORY_PALETTE.length).toBeGreaterThanOrEqual(10);
    for (const c of CATEGORY_PALETTE) expect(c).toMatch(/^#[0-9a-f]{6}$/);
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run lib/eventCategoryPalette.test.ts`
Expected: FAIL (모듈 없음).

- [ ] **Step 3: 구현**

`lib/eventCategoryPalette.ts`:

```ts
// 캘린더 카테고리 색 팔레트 — 앱 크림/어스톤 톤에 맞춘 스와치.
export const CATEGORY_PALETTE = [
  "#5b8c3e", // leaf
  "#8ab560", // sprout
  "#4a9d9a", // teal
  "#5c93a8", // sky
  "#8a6fb0", // lavender
  "#b5586f", // plum
  "#e07a5f", // coral
  "#c9784f", // terracotta
  "#d9a441", // gold
  "#a3785a", // caramel
  "#6d6875", // slate
  "#7c766a", // stone
];

// 배경 hex 위에 얹을 글씨색 — 밝기(YIQ)로 흰색/짙은색 결정.
export function readableText(hex: string): string {
  const h = hex.replace("#", "");
  const r = parseInt(h.slice(0, 2), 16);
  const g = parseInt(h.slice(2, 4), 16);
  const b = parseInt(h.slice(4, 6), 16);
  const yiq = (r * 299 + g * 587 + b * 114) / 1000;
  return yiq >= 150 ? "#2f2a20" : "#ffffff";
}
```

- [ ] **Step 4: 통과 확인**

Run: `npx vitest run lib/eventCategoryPalette.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add lib/eventCategoryPalette.ts lib/eventCategoryPalette.test.ts
git commit -m "feat: 캘린더 카테고리 색 팔레트 + readableText"
```

---

## Task 3: 데이터 축 전환 (types·seed·repo·context) + 소비처 최소 수정

이 태스크는 `assignee`→`categoryId` 원자적 전환이라 types·repo·context·EventForm·FamilyCalendar를 한 번에 바꿔 빌드를 초록으로 유지한다. 셀 텍스트/풀블리드/관리 UI는 Task 4·5.

**Files:**
- Modify: `lib/types.ts`, `lib/seed.ts`, `lib/repo.ts`, `lib/data-context.tsx`, `components/calendar/EventForm.tsx`, `components/calendar/FamilyCalendar.tsx`

**Interfaces:**
- Consumes: Task 2의 `CATEGORY_PALETTE`(EventForm 기본색엔 불필요, Task 5에서 사용).
- Produces:
  - `EventCategory { id: string; name: string; color: string; emoji: string | null; sortOrder: number; createdAt: string }`
  - `FamilyEvent.categoryId: string`(assignee 제거)
  - `DataSnapshot.eventCategories: EventCategory[]`
  - context: `eventCategories: EventCategory[]`, `saveEventCategory(c): Promise<void>`, `removeEventCategory(id): Promise<void>`
  - repo: `saveEventCategory(c): Promise<EventCategory>`, `deleteEventCategory(id): Promise<void>`, `SEED_EVENT_CATEGORIES`

- [ ] **Step 1: types.ts — EventCategory + FamilyEvent + DataSnapshot**

`lib/types.ts` — `FamilyEvent` 앞(가족 캘린더 섹션)에 `EventCategory` 추가:

```ts
// 캘린더 카테고리 — 사용자 관리(이름 + 색). assignee 축을 대체.
export interface EventCategory {
  id: string;
  name: string;
  color: string; // hex
  emoji: string | null;
  sortOrder: number;
  createdAt: string; // YYYY-MM-DD
}
```

`FamilyEvent`의 `assignee: TodoAssignee;` 줄을 교체:

```ts
  categoryId: string; // event_categories.id
```

`DataSnapshot`(약 line 404 근처, `familyEvents: FamilyEvent[];` 옆)에 추가:

```ts
  eventCategories: EventCategory[];
```

- [ ] **Step 2: seed.ts — SEED_EVENT_CATEGORIES**

`lib/seed.ts` 상단 import에 `EventCategory` 추가(`import type { Category, PaymentMethod, BaechooCategory, PlanItem, EventCategory } from "./types";`), 파일 하단에 추가:

```ts
// 캘린더 기본 카테고리 (고정 ID → 기존 일정 백필 category_id와 정합)
export const SEED_EVENT_CATEGORIES: EventCategory[] = [
  { id: "cat-chuchu", name: "추추", color: "#5c93a8", emoji: "🧑", sortOrder: 10, createdAt: "2026-07-16" },
  { id: "cat-baejji", name: "배찌", color: "#e07a5f", emoji: "👩", sortOrder: 20, createdAt: "2026-07-16" },
  { id: "cat-together", name: "함께", color: "#5b8c3e", emoji: "👫", sortOrder: 30, createdAt: "2026-07-16" },
];
```

- [ ] **Step 3: repo.ts — 매퍼·loadAll·save/delete·normalize·ls**

`lib/repo.ts` 상단 seed import에 `SEED_EVENT_CATEGORIES` 추가, 타입 import에 `EventCategory` 추가.

(a) family_event 매퍼 교체 — `toFamilyEvent`의 `assignee:` 줄을, `fromFamilyEvent`의 `assignee:` 줄을 category_id로:

```ts
// toFamilyEvent 안: assignee 줄 대체
  categoryId: (r.category_id as string) ?? "cat-together",
// fromFamilyEvent 안: assignee 줄 대체
  category_id: x.categoryId,
```

(b) event_categories 매퍼 추가(family_event 매퍼 근처):

```ts
const toEventCategory = (r: Record<string, unknown>): EventCategory => ({
  id: r.id as string,
  name: (r.name as string) ?? "",
  color: (r.color as string) ?? "#7c766a",
  emoji: (r.emoji as string) ?? null,
  sortOrder: Number(r.sort_order ?? 0),
  createdAt: (r.created_at as string) ?? "",
});
const fromEventCategory = (x: EventCategory) => ({
  id: x.id,
  name: x.name,
  color: x.color,
  emoji: x.emoji,
  sort_order: x.sortOrder,
  created_at: x.createdAt || null,
});
```

(c) `normalizeFamilyEvent` 추가(normalizeExam 근처) — 구버전 localStorage assignee 승격:

```ts
// 구버전 localStorage 일정(assignee만 있음)을 categoryId로 승격
function normalizeFamilyEvent(x: FamilyEvent): FamilyEvent {
  const legacy = x as FamilyEvent & { assignee?: string };
  if (!x.categoryId && legacy.assignee) {
    return { ...x, categoryId: "cat-" + legacy.assignee };
  }
  return x;
}
```

(d) `loadAll`: Promise.all 배열에 `sb.from("event_categories").select("*")` 추가(구조분해 이름 `ecats`), 부트스트랩 + 반환:

```ts
// Promise.all 구조분해에 ecats 추가 (예: fevents 앞)
    sb.from("event_categories").select("*"),
// planItems 시드 블록 아래에 추가
  let eventCategories = (ecats.data ?? []).map(toEventCategory);
  if (eventCategories.length === 0) {
    await sb.from("event_categories").insert(SEED_EVENT_CATEGORIES.map(fromEventCategory));
    eventCategories = SEED_EVENT_CATEGORIES;
  }
// return 객체에 추가
    eventCategories,
// familyEvents 매핑을 normalize 통과
    familyEvents: (fevents.data ?? []).map(toFamilyEvent).map(normalizeFamilyEvent),
```

(e) `lsRead`·`emptySnapshot`에 `eventCategories` 추가:

```ts
// lsRead의 파싱 반환(seeded 블록 포함)과 emptySnapshot에:
    eventCategories: parsed.eventCategories ?? SEED_EVENT_CATEGORIES, // lsRead parsed
// seeded 블록:
        eventCategories: SEED_EVENT_CATEGORIES,
// emptySnapshot:
    eventCategories: [],
// lsRead familyEvents도 normalize:
      familyEvents: (parsed.familyEvents ?? []).map(normalizeFamilyEvent),
```

(f) save/delete 공개 API 추가(saveFamilyEvent 근처):

```ts
export async function saveEventCategory(c: EventCategory): Promise<EventCategory> {
  const row = { ...c, id: c.id || newId() };
  if (hasSupabase) await sbUpsert("event_categories", fromEventCategory(row));
  else lsUpsert("eventCategories", row);
  return row;
}
export async function deleteEventCategory(id: string) {
  if (hasSupabase) await sbDelete("event_categories", id);
  else lsDelete("eventCategories", id);
}
```

- [ ] **Step 4: data-context.tsx — state·refresh·mutator·타입**

`lib/data-context.tsx`:

```ts
// import 타입에 EventCategory 추가
// context 인터페이스(familyEvents 근처)에:
  eventCategories: EventCategory[];
  saveEventCategory: (c: EventCategory) => Promise<void>;
  removeEventCategory: (id: string) => Promise<void>;
// state:
  const [eventCategories, setEventCategories] = useState<EventCategory[]>([]);
// refresh 안(setFamilyEvents 근처):
    setEventCategories(snap.eventCategories);
// provider value 객체(familyEvents 근처, 두 곳 모두면 실제 반환 객체 1곳):
      eventCategories,
// mutator(saveFamilyEvent 블록 근처):
      saveEventCategory: async (c) => {
        const saved = await repo.saveEventCategory(c);
        upsertLocal(setEventCategories, saved);
      },
      removeEventCategory: async (id) => {
        await repo.deleteEventCategory(id);
        setEventCategories((p) => p.filter((x) => x.id !== id));
      },
// useMemo 의존성 배열에 eventCategories 추가
```

- [ ] **Step 5: EventForm.tsx — 담당 토글 → 카테고리 선택줄**

`components/calendar/EventForm.tsx`:
- import에서 `TODO_ASSIGNEES` 제거, `useData()`에서 `eventCategories` 추가로 가져오기.
- state `assignee` → `categoryId`:

```ts
  const { saveFamilyEvent, removeFamilyEvent, eventCategories } = useData();
  // ...
  const [categoryId, setCategoryId] = useState(
    initial?.categoryId ?? eventCategories[0]?.id ?? "cat-together"
  );
```

- `submit()`의 `assignee,` → `categoryId,`.
- `<Field label="담당">` 블록 전체를 카테고리 선택줄로 교체(색 스와치 원 + 이름, 가로 스크롤):

```tsx
      <Field label="카테고리">
        <div className="flex gap-1.5 overflow-x-auto pb-1">
          {eventCategories.map((c) => (
            <button
              key={c.id}
              type="button"
              onClick={() => setCategoryId(c.id)}
              className={`flex shrink-0 items-center gap-1.5 rounded-full border px-3 py-1.5 text-sm font-semibold transition ${
                categoryId === c.id ? "border-leaf bg-leaf-light text-ink" : "border-line text-stone"
              }`}
            >
              <span className="h-3 w-3 rounded-full" style={{ backgroundColor: c.color }} />
              {c.emoji ? `${c.emoji} ` : ""}{c.name}
            </button>
          ))}
        </div>
      </Field>
```

- [ ] **Step 6: FamilyCalendar.tsx — 색 룩업만 교체(마커는 아직 점 유지)**

`components/calendar/FamilyCalendar.tsx`:
- `ASSIGNEE_DOT`·`ASSIGNEE_EMOJI`·`TODO_ASSIGNEES` import·`TodoAssignee` 사용 제거.
- `useData()`에 `eventCategories` 추가, 룩업 맵:

```ts
  const { loading, familyEvents, weekTodos, eventCategories } = useData();
  const catById = useMemo(
    () => new Map(eventCategories.map((c) => [c.id, c])),
    [eventCategories]
  );
  const catColor = (id: string) => catById.get(id)?.color ?? "#7c766a";
```

- 셀 마커 점: `ASSIGNEE_DOT[o.event.assignee]` className → 인라인 색:

```tsx
                  {occs.slice(0, 3).map((o, i) => (
                    <span
                      key={i}
                      className="h-1.5 w-1.5 rounded-full"
                      style={{ backgroundColor: catColor(o.event.categoryId) }}
                    />
                  ))}
```

- 상세 패널의 색 바·이모지: `ASSIGNEE_DOT` → `style={{ backgroundColor: catColor(o.event.categoryId) }}`, `ASSIGNEE_EMOJI[o.event.assignee]` → `catById.get(o.event.categoryId)?.emoji ?? ""`.
- 범례: `TODO_ASSIGNEES.map` → `eventCategories.map`:

```tsx
          {eventCategories.map((c) => (
            <span key={c.id} className="flex items-center gap-1">
              <span className="h-1.5 w-1.5 rounded-full" style={{ backgroundColor: c.color }} />
              {c.name}
            </span>
          ))}
```

- [ ] **Step 7: 린트·빌드·타입 확인**

Run: `npx eslint components/calendar/FamilyCalendar.tsx components/calendar/EventForm.tsx lib/data-context.tsx lib/repo.ts` → clean.
Run: `npx tsc --noEmit` → 에러 0.
Expected: assignee 잔재 없음, 빌드 통과.

- [ ] **Step 8: 로컬 동작 확인** (cloud 모드, Task 1 DDL 적용 필수)

`npm run dev` → `/calendar` 가족 탭. 기존 일정이 카테고리 색으로 뜨는지(추추 sky·배찌 coral·함께 leaf), 새 일정 추가 시 카테고리 선택줄이 뜨고 저장·새로고침 후 유지되는지 확인.

- [ ] **Step 9: Commit**

```bash
git add lib/types.ts lib/seed.ts lib/repo.ts lib/data-context.tsx components/calendar/EventForm.tsx components/calendar/FamilyCalendar.tsx
git commit -m "feat: 캘린더 담당 축을 자유 카테고리(EventCategory)로 전환"
```

---

## Task 4: 풀블리드 그리드 + 셀 텍스트 막대

**Files:**
- Modify: `components/calendar/FamilyCalendar.tsx`, `app/calendar/page.tsx`

**Interfaces:**
- Consumes: Task 2 `readableText`, Task 3 `catById`/`catColor`.

- [ ] **Step 1: 가족 탭 그리드 여백 제거(풀블리드)**

`app/calendar/page.tsx`의 가족 탭 래퍼에서 좌우 여백을 그리드가 벗어나게 한다. `+일정` 버튼·패널은 여백 유지하되 그리드만 전폭이 되도록, FamilyCalendar가 자체적으로 처리하므로 페이지 래퍼는 그대로 두고(수정 불필요면 skip), FamilyCalendar 내부에서 그리드를 `-mx-4`로 좌우 여백을 상쇄한다. (page 래퍼 `px-4` 유지, 그리드만 breakout)

- [ ] **Step 2: FamilyCalendar 그리드를 Card 밖 전폭으로 + 셀 높이 확대 + 텍스트 막대**

`components/calendar/FamilyCalendar.tsx`:
- import에 `readableText` 추가: `import { CATEGORY_PALETTE, readableText } from "@/lib/eventCategoryPalette";`(팔레트는 Task 5에서 쓰지만 같이 import해도 무방, 미사용 경고 나면 readableText만).
- 월 그리드를 감싼 `<Card>` 중 **월 스위처+그리드 부분을 Card 밖으로** 빼거나, 그리드 컨테이너에 `-mx-4`를 줘 좌우로 전폭 확장. 요일 헤더도 동일 `-mx-4`.
- 셀 버튼: `flex-col items-center` 고정 높이 → **`min-h-[76px]` + 상단 날짜, 하단 막대 영역**. 셀 사이 옅은 구분선 `border-t border-line`.
- 마커 점 블록(`<span className="mt-0.5 flex h-1.5 gap-0.5">…`)을 **텍스트 막대 리스트**로 교체:

```tsx
                <div className="mt-0.5 w-full space-y-0.5 px-0.5">
                  {(() => {
                    const items: { color: string; label: string }[] = [
                      ...occs.map((o) => ({
                        color: catColor(o.event.categoryId),
                        label: o.event.title,
                      })),
                      ...(todoDue.get(iso) ?? []).map((t) => ({
                        color: "#d9a441",
                        label: t.title,
                      })),
                    ];
                    const shown = items.slice(0, 3);
                    const extra = items.length - shown.length;
                    return (
                      <>
                        {shown.map((it, i) => (
                          <span
                            key={i}
                            className="block truncate rounded px-1 text-left text-[9px] leading-tight"
                            style={{ backgroundColor: it.color, color: readableText(it.color) }}
                          >
                            {it.label}
                          </span>
                        ))}
                        {extra > 0 && (
                          <span className="block px-1 text-left text-[9px] leading-tight text-stone">
                            +{extra}
                          </span>
                        )}
                      </>
                    );
                  })()}
                </div>
```

- 날짜 숫자 `<span className="flex h-8 w-8 …">`는 유지하되 셀 상단 정렬(`items-start`), 오늘/선택 강조는 숫자 원형에만.
- 기존 `hasTodo` 점 로직·범례 gold 항목은 유지(범례는 그대로).

- [ ] **Step 3: 린트·타입**

Run: `npx eslint components/calendar/FamilyCalendar.tsx app/calendar/page.tsx` → clean.
Run: `npx tsc --noEmit` → 0.

- [ ] **Step 4: 시각 확인(Playwright 390px 또는 dev)**

`/calendar` 가족 탭에서 그리드가 좌우 끝까지 차고, 셀에 일정 제목이 색 막대로 최대 3개 + `+N`이 보이는지 확인. 탭·드래그(롱프레스)·`+일정`이 여전히 작동하는지.

- [ ] **Step 5: Commit**

```bash
git add components/calendar/FamilyCalendar.tsx app/calendar/page.tsx
git commit -m "feat: 캘린더 풀블리드 그리드 + 셀 텍스트 일정 막대"
```

---

## Task 5: 카테고리 관리 시트 (추가·편집·삭제 재배정)

**Files:**
- Create: `components/calendar/CategoryManager.tsx`
- Modify: `components/calendar/EventForm.tsx`(관리 진입), `components/calendar/FamilyCalendar.tsx`(관리 진입 버튼)

**Interfaces:**
- Consumes: context `eventCategories`·`saveEventCategory`·`removeEventCategory`·`familyEvents`·`saveFamilyEvent`; `CATEGORY_PALETTE`; `newId`(from `@/lib/repo`).

- [ ] **Step 1: CategoryManager 시트 작성**

`components/calendar/CategoryManager.tsx`:

```tsx
"use client";

import { useState } from "react";
import { useData } from "@/lib/data-context";
import { newId } from "@/lib/repo";
import { todayISO } from "@/lib/format";
import { CATEGORY_PALETTE } from "@/lib/eventCategoryPalette";
import type { EventCategory } from "@/lib/types";
import { Sheet, Field, inputCls, PrimaryButton } from "@/components/budget/ui";

// 개별 카테고리 편집 행(이름 + 색 스와치 + 삭제)
function Row({ cat }: { cat: EventCategory }) {
  const { eventCategories, familyEvents, saveEventCategory, removeEventCategory, saveFamilyEvent } = useData();
  const [name, setName] = useState(cat.name);
  const [color, setColor] = useState(cat.color);
  const [confirming, setConfirming] = useState(false);

  const usedCount = familyEvents.filter((e) => e.categoryId === cat.id).length;
  const others = eventCategories.filter((c) => c.id !== cat.id);
  const canDelete = others.length >= 1;

  async function save() {
    if (!name.trim()) return;
    await saveEventCategory({ ...cat, name: name.trim(), color });
  }

  async function del() {
    // 이 카테고리 일정을 첫 다른 카테고리로 재배정 후 삭제(고아 방지)
    const target = others[0];
    if (!target) return;
    for (const e of familyEvents.filter((x) => x.categoryId === cat.id)) {
      await saveFamilyEvent({ ...e, categoryId: target.id });
    }
    await removeEventCategory(cat.id);
  }

  return (
    <div className="rounded-xl border border-line p-2.5">
      <div className="flex items-center gap-2">
        <span className="h-5 w-5 shrink-0 rounded-full" style={{ backgroundColor: color }} />
        <input
          className={inputCls}
          value={name}
          onChange={(e) => setName(e.target.value)}
          onBlur={save}
        />
      </div>
      <div className="mt-2 flex flex-wrap gap-1.5">
        {CATEGORY_PALETTE.map((c) => (
          <button
            key={c}
            type="button"
            onClick={async () => { setColor(c); await saveEventCategory({ ...cat, name: name.trim() || cat.name, color: c }); }}
            className={`h-6 w-6 rounded-full ${color === c ? "ring-2 ring-leaf ring-offset-1" : ""}`}
            style={{ backgroundColor: c }}
            aria-label={c}
          />
        ))}
      </div>
      {canDelete && !confirming && (
        <button type="button" onClick={() => setConfirming(true)} className="mt-2 text-xs text-coral">
          삭제
        </button>
      )}
      {canDelete && confirming && (
        <div className="mt-2 space-y-1.5">
          <p className="text-xs text-stone">
            {usedCount > 0
              ? `이 카테고리 일정 ${usedCount}개는 '${others[0].name}'로 옮겨져요`
              : "삭제할까요?"}
          </p>
          <div className="flex gap-2">
            <button type="button" onClick={() => setConfirming(false)} className="flex-1 rounded-xl border border-line py-2 text-sm font-semibold text-stone">
              취소
            </button>
            <button type="button" onClick={del} className="flex-1 rounded-xl bg-coral py-2 text-sm font-bold text-white">
              삭제 확정
            </button>
          </div>
        </div>
      )}
    </div>
  );
}

export default function CategoryManager({ open, onClose }: { open: boolean; onClose: () => void }) {
  const { eventCategories, saveEventCategory } = useData();

  async function add() {
    const used = eventCategories.map((c) => c.color);
    const color = CATEGORY_PALETTE.find((c) => !used.includes(c)) ?? CATEGORY_PALETTE[0];
    const maxSort = eventCategories.reduce((m, c) => Math.max(m, c.sortOrder), 0);
    await saveEventCategory({
      id: newId(),
      name: "새 카테고리",
      color,
      emoji: null,
      sortOrder: maxSort + 10,
      createdAt: todayISO(),
    });
  }

  return (
    <Sheet open={open} onClose={onClose} title="카테고리 관리">
      <div className="space-y-2">
        {[...eventCategories].sort((a, b) => a.sortOrder - b.sortOrder).map((c) => (
          <Row key={c.id} cat={c} />
        ))}
      </div>
      <div className="mt-3">
        <PrimaryButton onClick={add}>+ 새 카테고리</PrimaryButton>
      </div>
      <button type="button" onClick={onClose} className="mt-2 w-full py-2 text-sm text-stone">
        닫기
      </button>
    </Sheet>
  );
}
```

- [ ] **Step 2: EventForm에서 관리 진입**

`components/calendar/EventForm.tsx`:
- `import CategoryManager from "./CategoryManager";` 추가, `const [manage, setManage] = useState(false);`.
- 카테고리 선택줄 끝에 관리 버튼 추가(스크롤 컨테이너 안):

```tsx
          <button
            type="button"
            onClick={() => setManage(true)}
            className="shrink-0 rounded-full border border-dashed border-line px-3 py-1.5 text-sm font-semibold text-stone"
          >
            + 관리
          </button>
```

- Sheet 밖(컴포넌트 return 최상위 fragment)에 `{manage && <CategoryManager open={manage} onClose={() => setManage(false)} />}` 추가. (EventForm이 Sheet 하나만 반환하면 `<>…</>`로 감싸기)

- [ ] **Step 3: FamilyCalendar 범례 옆 관리 버튼**

`components/calendar/FamilyCalendar.tsx`:
- `import CategoryManager from "./CategoryManager";`, `const [manageCat, setManageCat] = useState(false);`.
- 범례 블록 끝에:

```tsx
          <button
            type="button"
            onClick={() => setManageCat(true)}
            className="ml-auto text-[11px] font-semibold text-leaf-dark"
          >
            카테고리 관리
          </button>
```

- 컴포넌트 하단 시트들 근처에 `{manageCat && <CategoryManager open={manageCat} onClose={() => setManageCat(false)} />}`.

- [ ] **Step 4: 린트·타입**

Run: `npx eslint components/calendar/CategoryManager.tsx components/calendar/EventForm.tsx components/calendar/FamilyCalendar.tsx` → clean.
Run: `npx tsc --noEmit` → 0.

- [ ] **Step 5: 동작 확인**

`/calendar` → 카테고리 관리 열기 → 새 카테고리 추가(색 선택), 이름 변경, 일정에 새 카테고리 지정 후 저장. 카테고리 삭제 시 그 일정이 다른 카테고리로 옮겨지고 새로고침 후 유지되는지. 마지막 1개는 삭제 버튼 안 뜨는지.

- [ ] **Step 6: Commit**

```bash
git add components/calendar/CategoryManager.tsx components/calendar/EventForm.tsx components/calendar/FamilyCalendar.tsx
git commit -m "feat: 캘린더 카테고리 관리(추가·편집·삭제 재배정)"
```

---

## Task 6: 배포 + 프로덕션 검증

**Files:** 없음(배포·검증 전용).

- [ ] **Step 1: 전체 빌드**

Run: `npm run build`
Expected: 성공(타입·린트 통과).

- [ ] **Step 2: 회귀 테스트**

Run: `npx vitest run`
Expected: 전부 통과(특히 `lib/calendar.test.ts` 12개, `lib/eventCategoryPalette.test.ts` 3개).

- [ ] **Step 3: push(자동 배포)** — Task 1 DDL이 이미 프로덕션에 적용돼 있어야 함.

```bash
git push
```

Vercel 자동 배포. 완료 대기.

- [ ] **Step 4: 프로덕션 검증(라이브)**

https://baechoo-family.vercel.app `/calendar`(PIN 1106): 기존 일정 카테고리 색·셀 텍스트·전폭 그리드 확인. 새 카테고리 추가→일정 지정→**새로고침 후 유지**(조용한 유실 없음). DB 직접 조회로 `event_categories` 3+N행, 새 일정 `category_id` 저장 확인.

- [ ] **Step 5: 메모리 업데이트**

`project_baechoo_family.md`의 캘린더 탭 섹션에 이번 개편 반영(assignee→categoryId 전환, event_categories 0021, 팔레트·readableText, 풀블리드·셀 텍스트, CategoryManager, 삭제 재배정 규칙). 함정: DDL-먼저·assignee nullable화·동적 색 인라인 스타일.

**후속(이번 계획 범위 밖):** 프로덕션 안정 확인 후 별도 마이그레이션으로 `family_events.assignee` 컬럼 완전 삭제. `sbUpsert` error 무검사 근본 수정.

---

## Self-Review 결과

- **스펙 커버리지**: 풀블리드(T4)·셀 텍스트(T4)·자유 카테고리+색 팔레트(T2·T3·T5)·기본 시드(T3)·삭제 재배정(T5)·마이그레이션·DDL 순서(T1·T6)·투두기한 유지(T4 gold)·localStorage 승격(T3 normalize) 전부 태스크 매핑됨.
- **플레이스홀더**: 없음(모든 코드 스텝에 실제 코드).
- **타입 일관성**: `EventCategory`(id·name·color·emoji·sortOrder·createdAt)·`FamilyEvent.categoryId`·`catColor`/`catById`·`saveEventCategory`/`removeEventCategory` 태스크 간 시그니처 일치.
