# 가족 P&L 「계획」 탭 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 배추가족 PWA `📊손익` 탭에 「계획」 서브탭을 추가해, 손글씨 월 예산 계획(27개 항목)을 앱에서 편집하고 실적과 대조한다.

**Architecture:** `plan_items` 테이블을 신설하고 항목마다 `pnlClass`(revenue/fixed/saving/variable)를 붙여, 실적 P&L과 **동일한 4버킷**으로 계획 P&L을 산출한다. 그래서 기존 `computePnl`의 반환 타입 `PnlSummary`와 `WaterfallChart`를 그대로 재사용할 수 있고, 계획 vs 실적 대조가 성립한다. 시드 27개 항목은 마이그레이션 INSERT가 아니라 `repo.loadAll()` 부트스트랩으로 넣는다(한글 인코딩 함정 회피 + localStorage 모드 대칭).

**Tech Stack:** Next.js 16 (App Router) · React 19 · TypeScript · Tailwind v4 · Supabase (supabase-js) · Chart.js + react-chartjs-2 · vitest

## Global Constraints

- **언어**: 모든 UI 문자열·주석은 한국어.
- **작업 디렉터리**: `C:\Users\MGRV\.claude\baechoo-family` (모든 경로는 이 레포 기준 상대경로).
- **테스트 실행**: `npm test` (vitest run). 단일 파일은 `npx vitest run lib/plan.test.ts`.
- **린트**: `npx eslint <파일>`. ⚠️ Next16/React19의 `react-hooks/set-state-in-effect` 룰이 effect 안의 `setState` 직접 호출을 **에러**로 막는다. 조건부 상태 리셋은 useEffect가 아니라 **파생값 계산**으로 해결한다.
- **UI 프리미티브**: 새로 만들지 않는다. `components/budget/ui.tsx`의 `Card`·`SectionTitle`·`Sheet`·`Field`·`inputCls`·`PrimaryButton`·`Pill`·`Empty`·`ProgressBar`를 재사용한다.
- **숫자 입력**: `type="number"` 금지(iOS에서 글씨가 안 보이는 기존 버그). `type="text"` + `inputMode="numeric"` + 숫자만 남기는 필터를 쓴다.
- **금액 포맷**: `lib/format.ts`의 `won(n)`.
- **DB 컬럼 네이밍**: snake_case. 앱 타입은 camelCase. `lib/repo.ts`의 `toX`/`fromX` 매퍼가 변환한다.
- **커밋**: 각 Task 끝에서 1커밋. 메시지는 한국어 본문 + 아래 트레일러.
  ```
  Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
  ```
- **골든 값(절대 바뀌면 안 됨)**: 시드 27개 항목의 계획 P&L은 `revenue 4,400,000 / fixed 2,299,206 / saving 775,000 / variable 1,300,000 / operatingProfit 25,794`. 마지막 값 **25,794**가 손글씨 계획의 잔액이다.

---

## File Structure

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `lib/types.ts` | `PlanItem`·`PlanGroup`·`PLAN_GROUP_LABEL`·`PNL_CLASS_LABEL`·`BUDGET_RULES` 추가, `DataSnapshot`에 `planItems` 추가 | 수정 |
| `lib/seed.ts` | `SEED_PLAN_ITEMS` (27개 항목) | 수정 |
| `lib/plan.ts` | 순수 계산 — `activeItems`·`computePlanPnl`·`groupTotal`·`remainingMonths` | 신규 |
| `lib/plan.test.ts` | 위 4함수 테스트 (골든 25,794 포함) | 신규 |
| `supabase/migrations/0018_plan_items.sql` | 테이블 DDL (순수 ASCII) | 신규 |
| `lib/repo.ts` | `toPlanItem`/`fromPlanItem` 매퍼, `loadAll` select + 빈 테이블 시드, `savePlanItem`/`deletePlanItem` | 수정 |
| `lib/data-context.tsx` | `planItems` state + `savePlanItem`/`removePlanItem` mutator | 수정 |
| `app/pnl/page.tsx` | 서브탭에 `plan` 추가 | 수정 |
| `components/pnl/Plan.tsx` | 계획 탭 루트 — 5블록 배치 | 신규 |
| `components/pnl/PlanSummary.tsx` | 히어로 (수입/지출/여유 시 집행/잔액) | 신규 |
| `components/pnl/PlanVsActual.tsx` | 계획 vs 실적 대조표 | 신규 |
| `components/pnl/PlanItemList.tsx` | 그룹별 항목 리스트 (4섹션) | 신규 |
| `components/pnl/PlanItemForm.tsx` | 항목 편집·추가 바텀시트 | 신규 |
| `components/pnl/BudgetRules.tsx` | 예산 규칙 접이식 메모 | 신규 |

---

## Task 1: 타입 · 시드 · 계산 로직 (순수함수, DB 없음)

계획 탭의 심장. DB도 화면도 없이, **27개 항목에서 25,794가 나오는지**만 검증한다. 이게 맞으면 나머지는 배선이다.

**Files:**
- Modify: `lib/types.ts` (파일 끝, `WaterfallSegment` 아래)
- Modify: `lib/seed.ts` (파일 끝)
- Create: `lib/plan.ts`
- Test: `lib/plan.test.ts`

**Interfaces:**
- Consumes: `PnlClass`·`PnlSummary` (`lib/types.ts` 기존 정의)
- Produces:
  - `PlanGroup = "income" | "spending" | "saving"`
  - `PlanItem { id, group, name, amount, pnlClass, conditional, endYearMonth, targetTotal, note, sortOrder }`
  - `PLAN_GROUP_LABEL: Record<PlanGroup, string>`
  - `PNL_CLASS_LABEL: Record<Exclude<PnlClass,"excluded">, string>`
  - `BUDGET_RULES: string[]`
  - `SEED_PLAN_ITEMS: PlanItem[]` (27개)
  - `activeItems(items: PlanItem[], yearMonth: string): PlanItem[]`
  - `computePlanPnl(items: PlanItem[], yearMonth: string): PnlSummary`
  - `groupTotal(items: PlanItem[], group: PlanGroup, conditional?: boolean): number`
  - `remainingMonths(endYearMonth: string | null, yearMonth: string): number | null`

---

- [ ] **Step 1: `lib/types.ts`에 타입 추가**

`lib/types.ts` 맨 끝(`WaterfallSegment` 인터페이스 다음)에 붙인다.

```ts
/* ───────────── 가족 P&L — 예산 계획 ───────────── */

// 계획 항목의 구획 (손글씨 노트의 좌/우 블록)
export type PlanGroup = "income" | "spending" | "saving";

export const PLAN_GROUP_LABEL: Record<PlanGroup, string> = {
  income: "수입",
  spending: "월 지출",
  saving: "저축·상환",
};

// 손익분류 라벨 (계획 항목 편집 시트에서 선택지로 노출)
export const PNL_CLASS_LABEL: Record<Exclude<PnlClass, "excluded">, string> = {
  revenue: "매출",
  fixed: "고정비",
  saving: "선저축",
  variable: "변동비",
};

// 월 예산 계획 1항목.
// group은 화면 구획, pnlClass는 손익 집계 버킷 — 둘은 독립이다.
// (예: 학자금 상환은 group=saving이지만 pnlClass=fixed)
export interface PlanItem {
  id: string;
  group: PlanGroup;
  name: string;
  amount: number; // 월 금액
  pnlClass: Exclude<PnlClass, "excluded">;
  conditional: boolean; // ⊖ — 여유 있을 때 집행
  endYearMonth: string | null; // "YYYY-MM", null = 무기한. 이 달까지 집행.
  targetTotal: number | null; // 부채 잔액·적립 목표액 (표시용)
  note: string | null;
  sortOrder: number;
}

// 예산 규칙 (손글씨 포스트잇)
export const BUDGET_RULES: string[] = [
  "금액은 넉넉하게 올려 잡는다",
  "현금·배찌 주식은 계획에서 잊는다",
  "첫만남 이용권·안양시 200만원 등 일회성 금액은 잊는다",
  "엄마 1,000,000은 첫만남 이용권으로 충당 — 예산에서 제외",
];
```

같은 파일의 `DataSnapshot` 인터페이스에 한 줄 추가한다 (`assetSnapshots: AssetSnapshot[];` 다음).

```ts
  planItems: PlanItem[];
```

- [ ] **Step 2: `lib/seed.ts`에 27개 항목 시드 추가**

`lib/seed.ts` 맨 끝에 붙인다. import 문에 `PlanItem`을 추가해야 한다 (기존 `import type { ... } from "./types";` 목록에 넣는다).

⚠️ 금액은 손글씨 계획의 골든 값이다. **한 자리도 바꾸지 말 것.**

```ts
// 월 예산 계획 시드 — 재헌님 손글씨 노트(2026-07-13). 합계는 골든 값이다.
//   수입 4,400,000 / 월 지출 2,760,000 / 저축·상환 1,254,206 / 여유 시 집행 360,000
//   → 계획 운영이익 25,794 (노트의 "잔액"과 원 단위 일치)
export const SEED_PLAN_ITEMS: PlanItem[] = [
  // ── 수입 4,400,000
  { id: "plan-inc-salary", group: "income", name: "추 급여", amount: 3300000, pnlClass: "revenue", conditional: false, endYearMonth: null, targetTotal: null, note: "매월 25일", sortOrder: 10 },
  { id: "plan-inc-parent", group: "income", name: "부모급여", amount: 1000000, pnlClass: "revenue", conditional: false, endYearMonth: null, targetTotal: null, note: null, sortOrder: 20 },
  { id: "plan-inc-child", group: "income", name: "아동수당", amount: 100000, pnlClass: "revenue", conditional: false, endYearMonth: null, targetTotal: null, note: null, sortOrder: 30 },

  // ── 월 지출 2,760,000 (고정비 1,460,000 + 변동비 1,300,000)
  { id: "plan-exp-rent", group: "spending", name: "월세", amount: 600000, pnlClass: "fixed", conditional: false, endYearMonth: null, targetTotal: null, note: null, sortOrder: 110 },
  { id: "plan-exp-maint", group: "spending", name: "관리비", amount: 150000, pnlClass: "fixed", conditional: false, endYearMonth: null, targetTotal: null, note: null, sortOrder: 120 },
  { id: "plan-exp-gas", group: "spending", name: "가스", amount: 50000, pnlClass: "fixed", conditional: false, endYearMonth: null, targetTotal: null, note: null, sortOrder: 130 },
  { id: "plan-exp-elec", group: "spending", name: "전기", amount: 50000, pnlClass: "fixed", conditional: false, endYearMonth: null, targetTotal: null, note: null, sortOrder: 140 },
  { id: "plan-exp-loan", group: "spending", name: "대출", amount: 80000, pnlClass: "fixed", conditional: false, endYearMonth: null, targetTotal: null, note: null, sortOrder: 150 },
  { id: "plan-exp-phone", group: "spending", name: "휴대폰", amount: 150000, pnlClass: "fixed", conditional: false, endYearMonth: null, targetTotal: null, note: null, sortOrder: 160 },
  { id: "plan-exp-nhis", group: "spending", name: "국민건강보험", amount: 20000, pnlClass: "fixed", conditional: false, endYearMonth: null, targetTotal: null, note: null, sortOrder: 170 },
  { id: "plan-exp-uju-ins", group: "spending", name: "우주보험", amount: 200000, pnlClass: "fixed", conditional: false, endYearMonth: null, targetTotal: null, note: null, sortOrder: 180 },
  { id: "plan-exp-chu-allowance", group: "spending", name: "추 용돈", amount: 300000, pnlClass: "variable", conditional: false, endYearMonth: null, targetTotal: null, note: null, sortOrder: 190 },
  { id: "plan-exp-chu-ins", group: "spending", name: "추 보험", amount: 150000, pnlClass: "fixed", conditional: false, endYearMonth: null, targetTotal: null, note: null, sortOrder: 200 },
  { id: "plan-exp-apple", group: "spending", name: "애플", amount: 10000, pnlClass: "fixed", conditional: false, endYearMonth: null, targetTotal: null, note: null, sortOrder: 210 },
  { id: "plan-exp-food", group: "spending", name: "식비", amount: 700000, pnlClass: "variable", conditional: false, endYearMonth: null, targetTotal: null, note: null, sortOrder: 220 },
  { id: "plan-exp-fuel", group: "spending", name: "연료비", amount: 300000, pnlClass: "variable", conditional: false, endYearMonth: null, targetTotal: null, note: null, sortOrder: 230 },

  // ── 저축·상환 1,254,206
  { id: "plan-sav-uju-emergency", group: "saving", name: "우주 비상금", amount: 300000, pnlClass: "saving", conditional: false, endYearMonth: null, targetTotal: null, note: null, sortOrder: 310 },
  { id: "plan-sav-cheongyak", group: "saving", name: "청약", amount: 20000, pnlClass: "fixed", conditional: false, endYearMonth: null, targetTotal: null, note: null, sortOrder: 320 },
  { id: "plan-sav-uju-deposit", group: "saving", name: "우주 적금", amount: 200000, pnlClass: "saving", conditional: false, endYearMonth: null, targetTotal: null, note: null, sortOrder: 330 },
  { id: "plan-sav-baechoo-deposit", group: "saving", name: "배추 적금", amount: 100000, pnlClass: "saving", conditional: false, endYearMonth: null, targetTotal: null, note: "아동수당으로 충당", sortOrder: 340 },
  { id: "plan-sav-student-loan", group: "saving", name: "학자금", amount: 266000, pnlClass: "fixed", conditional: false, endYearMonth: "2028-12", targetTotal: 6264000, note: "월 216,000 + 추가상환 50,000", sortOrder: 350 },
  { id: "plan-sav-car-loan", group: "saving", name: "자동차 할부", amount: 353206, pnlClass: "fixed", conditional: false, endYearMonth: "2027-03", targetTotal: 2425648, note: "월 303,206 + 추가상환 50,000", sortOrder: 360 },
  { id: "plan-sav-car-tax", group: "saving", name: "자동차세", amount: 15000, pnlClass: "saving", conditional: false, endYearMonth: "2027-06", targetTotal: 150000, note: "연 150,000 적립 (6월 납부)", sortOrder: 370 },

  // ── 여유 시 집행 ⊖ 360,000 — 부모급여·아동수당이 들어오면 집행
  { id: "plan-cond-mom-loan", group: "saving", name: "엄니 상환", amount: 200000, pnlClass: "fixed", conditional: true, endYearMonth: null, targetTotal: 5000000, note: null, sortOrder: 410 },
  { id: "plan-cond-car-ins", group: "saving", name: "자동차보험", amount: 100000, pnlClass: "saving", conditional: true, endYearMonth: "2027-02", targetTotal: 700000, note: "연 700,000 적립 (2월 납부)", sortOrder: 420 },
  { id: "plan-cond-baejji-wedding", group: "saving", name: "배찌 결혼 축의금", amount: 40000, pnlClass: "saving", conditional: true, endYearMonth: "2026-12", targetTotal: 200000, note: null, sortOrder: 430 },
  { id: "plan-cond-gyeongjosa", group: "saving", name: "경조사 저축", amount: 20000, pnlClass: "saving", conditional: true, endYearMonth: null, targetTotal: null, note: null, sortOrder: 440 },
];
```

- [ ] **Step 3: 실패하는 테스트 작성 — `lib/plan.test.ts`**

```ts
import { describe, it, expect } from "vitest";
import { activeItems, computePlanPnl, groupTotal, remainingMonths } from "./plan";
import { SEED_PLAN_ITEMS } from "./seed";
import type { PlanItem } from "./types";

// 기준월 — 시드의 어떤 항목도 아직 끝나지 않은 달
const YM = "2026-08";

describe("computePlanPnl — 손글씨 계획 골든 케이스", () => {
  it("시드 27항목의 운영이익이 노트의 잔액 25,794와 일치한다", () => {
    const s = computePlanPnl(SEED_PLAN_ITEMS, YM);
    expect(s.operatingProfit).toBe(25794);
  });

  it("4버킷 합계가 골든 값과 일치한다", () => {
    const s = computePlanPnl(SEED_PLAN_ITEMS, YM);
    expect(s.revenue).toBe(4400000);
    expect(s.fixed).toBe(2299206);
    expect(s.saving).toBe(775000);
    expect(s.variable).toBe(1300000);
    expect(s.grossProfit).toBe(4400000 - 2299206 - 775000);
    expect(s.bepAchieved).toBe(true);
  });

  it("빈 계획이면 전 필드 0이고 operatingMargin은 0이다 (0으로 나누기 가드)", () => {
    const s = computePlanPnl([], YM);
    expect(s.revenue).toBe(0);
    expect(s.operatingProfit).toBe(0);
    expect(s.operatingMargin).toBe(0);
  });
});

describe("groupTotal — 노트의 구획별 소계", () => {
  it("월 지출 = 2,760,000", () => {
    expect(groupTotal(SEED_PLAN_ITEMS, "spending")).toBe(2760000);
  });

  it("저축·상환(⊖ 제외) = 1,254,206", () => {
    expect(groupTotal(SEED_PLAN_ITEMS, "saving", false)).toBe(1254206);
  });

  it("여유 시 집행(⊖만) = 360,000", () => {
    expect(groupTotal(SEED_PLAN_ITEMS, "saving", true)).toBe(360000);
  });

  it("수입 = 4,400,000", () => {
    expect(groupTotal(SEED_PLAN_ITEMS, "income")).toBe(4400000);
  });
});

describe("activeItems — 종료된 항목 제외", () => {
  it("endYearMonth가 기준월보다 이전이면 제외한다", () => {
    // 자동차 할부는 2027-03까지 → 2027-04에는 빠진다
    const ids = activeItems(SEED_PLAN_ITEMS, "2027-04").map((i) => i.id);
    expect(ids).not.toContain("plan-sav-car-loan");
  });

  it("endYearMonth가 기준월과 같으면 포함한다 (그 달까지 납부)", () => {
    const ids = activeItems(SEED_PLAN_ITEMS, "2027-03").map((i) => i.id);
    expect(ids).toContain("plan-sav-car-loan");
  });

  it("endYearMonth가 null이면 항상 포함한다", () => {
    const ids = activeItems(SEED_PLAN_ITEMS, "2099-12").map((i) => i.id);
    expect(ids).toContain("plan-exp-rent");
  });

  it("computePlanPnl은 종료된 항목을 빼고 집계한다", () => {
    const s = computePlanPnl(SEED_PLAN_ITEMS, "2027-04");
    // 자동차 할부 353,206이 고정비에서 빠진다
    expect(s.fixed).toBe(2299206 - 353206);
  });
});

describe("remainingMonths", () => {
  it("2026-08 기준 2027-03까지는 8회 남는다", () => {
    expect(remainingMonths("2027-03", "2026-08")).toBe(8);
  });

  it("2026-08 기준 2028-12까지는 29회 남는다", () => {
    expect(remainingMonths("2028-12", "2026-08")).toBe(29);
  });

  it("당월이 마지막이면 1회 남는다", () => {
    expect(remainingMonths("2026-08", "2026-08")).toBe(1);
  });

  it("이미 지났으면 0이다", () => {
    expect(remainingMonths("2026-07", "2026-08")).toBe(0);
  });

  it("무기한(null)이면 null이다", () => {
    expect(remainingMonths(null, "2026-08")).toBe(null);
  });
});

describe("conditional 항목도 손익 집계에는 포함된다", () => {
  it("⊖ 항목을 빼면 운영이익이 360,000 늘어난다", () => {
    const withoutCond: PlanItem[] = SEED_PLAN_ITEMS.filter((i) => !i.conditional);
    const s = computePlanPnl(withoutCond, YM);
    expect(s.operatingProfit).toBe(25794 + 360000);
  });
});
```

> **테스트 설계 노트**: 골든 케이스(25,794)가 이 파일의 존재 이유다. 손글씨 노트를 손으로 검산해 얻은 값이고, 이게 깨지면 계획이 잘못 들어간 것이다. 나머지 테스트는 경계(같은 달 포함/제외, null, 빈 배열)를 지킨다.

- [ ] **Step 4: 테스트가 실패하는지 확인**

```bash
npx vitest run lib/plan.test.ts
```
Expected: FAIL — `Failed to resolve import "./plan"` (아직 `lib/plan.ts`가 없다)

- [ ] **Step 5: `lib/plan.ts` 구현**

```ts
// 월 예산 계획 — 순수 계산.
// 계획 항목을 실적 P&L과 같은 4버킷(revenue/fixed/saving/variable)으로 집계해
// 같은 PnlSummary 타입을 반환한다. 그래야 폭포 차트와 대조표를 공유할 수 있다.
import type { PlanItem, PlanGroup, PnlSummary } from "./types";

// 기준월에 살아 있는 항목만. endYearMonth가 그 달이면 아직 포함한다(그 달까지 납부).
// "YYYY-MM" 문자열은 사전순 비교 = 시간순 비교라 그대로 비교한다.
export function activeItems(items: PlanItem[], yearMonth: string): PlanItem[] {
  return items.filter(
    (i) => i.endYearMonth == null || i.endYearMonth >= yearMonth
  );
}

export function computePlanPnl(items: PlanItem[], yearMonth: string): PnlSummary {
  let revenue = 0, fixed = 0, saving = 0, variable = 0;
  for (const i of activeItems(items, yearMonth)) {
    if (i.pnlClass === "revenue") revenue += i.amount;
    else if (i.pnlClass === "fixed") fixed += i.amount;
    else if (i.pnlClass === "saving") saving += i.amount;
    else variable += i.amount;
  }
  const grossProfit = revenue - fixed - saving;
  const operatingProfit = grossProfit - variable;
  return {
    revenue, fixed, saving, grossProfit, variable,
    operatingProfit,
    operatingMargin: revenue > 0 ? operatingProfit / revenue : 0,
    bepAchieved: operatingProfit >= 0,
    variableByHabit: {}, // 계획엔 습관 태그가 없다
  };
}

// 구획별 소계. conditional을 주면 ⊖ 여부로 한 번 더 거른다.
export function groupTotal(
  items: PlanItem[],
  group: PlanGroup,
  conditional?: boolean
): number {
  return items
    .filter((i) => i.group === group)
    .filter((i) => conditional === undefined || i.conditional === conditional)
    .reduce((sum, i) => sum + i.amount, 0);
}

// 기준월 포함 남은 개월 수. 무기한이면 null, 이미 지났으면 0.
export function remainingMonths(
  endYearMonth: string | null,
  yearMonth: string
): number | null {
  if (endYearMonth == null) return null;
  const [ey, em] = endYearMonth.split("-").map(Number);
  const [cy, cm] = yearMonth.split("-").map(Number);
  const n = (ey - cy) * 12 + (em - cm) + 1;
  return Math.max(n, 0);
}
```

- [ ] **Step 6: 테스트 통과 확인**

```bash
npx vitest run lib/plan.test.ts
```
Expected: PASS — 15 tests.

만약 골든 테스트(25,794)가 실패하면 **`lib/plan.ts`가 아니라 `SEED_PLAN_ITEMS`를 의심하라.** 금액이나 `pnlClass`가 잘못 들어간 것이다.

- [ ] **Step 7: 전체 테스트 + 린트**

```bash
npm test
npx eslint lib/plan.ts lib/plan.test.ts lib/seed.ts lib/types.ts
```
Expected: 전체 PASS, 린트 에러 0.

- [ ] **Step 8: 커밋**

```bash
git add lib/types.ts lib/seed.ts lib/plan.ts lib/plan.test.ts
git commit -m "$(cat <<'EOF'
feat(pnl): 예산 계획 타입·시드·계산 로직

손글씨 월 예산 계획 27개 항목을 PlanItem으로 모델링.
항목마다 pnlClass를 붙여 실적과 같은 4버킷으로 집계.
골든 테스트: 시드 27항목 → 운영이익 25,794 (노트의 잔액과 일치).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: 저장 계층 (마이그레이션 · repo · data-context)

계획 항목을 Supabase/localStorage에 저장한다. **화면은 아직 없다.**

**Files:**
- Create: `supabase/migrations/0018_plan_items.sql`
- Modify: `lib/repo.ts`
- Modify: `lib/data-context.tsx`

**Interfaces:**
- Consumes: Task 1의 `PlanItem`·`SEED_PLAN_ITEMS`·`DataSnapshot.planItems`
- Produces:
  - `repo.savePlanItem(x: PlanItem): Promise<PlanItem>`
  - `repo.deletePlanItem(id: string): Promise<void>`
  - `useData()`에 `planItems: PlanItem[]`, `savePlanItem: (p: PlanItem) => Promise<void>`, `removePlanItem: (id: string) => Promise<void>`

---

- [ ] **Step 1: 마이그레이션 SQL 작성**

`supabase/migrations/0018_plan_items.sql` — **순수 ASCII만.** 한글 시드 INSERT를 여기 넣지 않는다(인코딩 함정). 시드는 Step 3의 `loadAll` 부트스트랩이 넣는다.

```sql
-- 0018: plan_items — monthly budget plan (family P&L "plan" tab)
-- group is the UI section (income/spending/saving);
-- pnl_class is the accounting bucket. They are independent:
-- e.g. student loan repayment is group=saving but pnl_class=fixed.

create table if not exists plan_items (
  id text primary key,
  "group" text not null check ("group" in ('income','spending','saving')),
  name text not null,
  amount integer not null,
  pnl_class text not null check (pnl_class in ('revenue','fixed','saving','variable')),
  conditional boolean not null default false,
  end_year_month text,
  target_total integer,
  note text,
  sort_order integer not null default 0,
  created_at timestamptz not null default now()
);

alter table plan_items enable row level security;

drop policy if exists family_all on plan_items;
create policy family_all on plan_items for all using (true) with check (true);

create index if not exists idx_plan_items_sort on plan_items (sort_order);
```

> ⚠️ `group`은 Postgres 예약어다. DDL과 쿼리에서 **반드시 큰따옴표**로 감싼다. supabase-js의 `.upsert({ group: ... })`는 내부적으로 안전하게 처리하므로 JS 쪽은 따옴표 불필요.

- [ ] **Step 2: `lib/repo.ts` — 매퍼 추가**

import 목록(`from "./types"`)에 `PlanItem`, `PlanGroup`을 추가하고, `SEED_...` import에 `SEED_PLAN_ITEMS`를 추가한다.

```ts
import {
  SEED_CATEGORIES,
  SEED_PAYMENT_METHODS,
  SEED_BAECHOO_CATEGORIES,
  SEED_PLAN_ITEMS,
} from "./seed";
```

매퍼는 `toVaccine`/`fromVaccine` 정의 다음(공개 API 섹션 바로 위)에 붙인다.

```ts
// 예산 계획 항목
const toPlanItem = (r: Record<string, unknown>): PlanItem => ({
  id: r.id as string,
  group: (r.group as PlanGroup) ?? "spending",
  name: (r.name as string) ?? "",
  amount: Number(r.amount ?? 0),
  pnlClass: (r.pnl_class as PlanItem["pnlClass"]) ?? "variable",
  conditional: Boolean(r.conditional),
  endYearMonth: (r.end_year_month as string) ?? null,
  targetTotal: r.target_total == null ? null : Number(r.target_total),
  note: (r.note as string) ?? null,
  sortOrder: Number(r.sort_order ?? 0),
});
const fromPlanItem = (x: PlanItem) => ({
  id: x.id,
  group: x.group,
  name: x.name,
  amount: x.amount,
  pnl_class: x.pnlClass,
  conditional: x.conditional,
  end_year_month: x.endYearMonth,
  target_total: x.targetTotal,
  note: x.note,
  sort_order: x.sortOrder,
});
```

- [ ] **Step 3: `lib/repo.ts` — `emptySnapshot`·`lsRead`·`loadAll` 배선**

`emptySnapshot()`의 반환 객체에 추가:
```ts
    planItems: [],
```

`lsRead()`의 반환 객체에 추가 (⚠️ **`?? SEED_PLAN_ITEMS`** — 카테고리와 같은 방식으로 localStorage 모드도 시드된다):
```ts
      planItems: parsed.planItems ?? SEED_PLAN_ITEMS,
```

`lsRead()`의 최초 시드 블록(`if (!raw)`)의 `seeded` 객체에도 추가:
```ts
        planItems: SEED_PLAN_ITEMS,
```

`loadAll()`의 `Promise.all` 배열 끝에 select를 추가하고 구조분해 이름(`assetSnaps` 다음)에 `plans`를 추가한다.
```ts
    sb.from("plan_items").select("*"),
```

`loadAll()`에서 `baechooCategories` 시드 블록 다음에 부트스트랩을 추가한다. **테이블이 비어 있으면 27개 항목을 넣는다.**
```ts
  let planItems = (plans.data ?? []).map(toPlanItem);
  if (planItems.length === 0) {
    await sb.from("plan_items").insert(SEED_PLAN_ITEMS.map(fromPlanItem));
    planItems = SEED_PLAN_ITEMS;
  }
```

`loadAll()`의 반환 객체에 추가:
```ts
    planItems,
```

- [ ] **Step 4: `lib/repo.ts` — save/delete 공개 API**

`saveAssetSnapshot`/`deleteAssetSnapshot` 다음에 붙인다. 계획 항목은 **하드 삭제**다 — 배추 기록물(휴지통 대상)과 달리 되살릴 이력 가치가 없고, 실수로 지워도 항목 재입력이 쉽다.

```ts
export async function savePlanItem(x: PlanItem): Promise<PlanItem> {
  const row = { ...x, id: x.id || newId() };
  if (hasSupabase) await sbUpsert("plan_items", fromPlanItem(row));
  else lsUpsert("planItems", row);
  return row;
}
export async function deletePlanItem(id: string) {
  if (hasSupabase) await sbDelete("plan_items", id);
  else lsDelete("planItems", id);
}
```

- [ ] **Step 5: `lib/data-context.tsx` 배선**

import type 목록에 `PlanItem` 추가. `DataContextValue`에 3줄 추가:
```ts
  planItems: PlanItem[];
```
(mutator 선언부, `removeAssetSnapshot` 다음)
```ts
  savePlanItem: (p: PlanItem) => Promise<void>;
  removePlanItem: (id: string) => Promise<void>;
```

state 추가 (`assetSnapshots` state 다음):
```ts
  const [planItems, setPlanItems] = useState<PlanItem[]>([]);
```

`refresh()`에 추가:
```ts
    setPlanItems(snap.planItems);
```

`value` 객체에 `assetSnapshots` 다음으로 `planItems,` 추가, mutator는 `removeAssetSnapshot` 다음에:
```ts
      savePlanItem: async (p) => {
        const saved = await repo.savePlanItem(p);
        upsertLocal(setPlanItems, saved);
      },
      removePlanItem: async (id) => {
        await repo.deletePlanItem(id);
        setPlanItems((p) => p.filter((x) => x.id !== id));
      },
```

`useMemo` 의존성 배열에 `planItems` 추가 (`assetSnapshots` 다음).

- [ ] **Step 6: 타입체크 + 린트 + 테스트**

```bash
npx tsc --noEmit
npx eslint lib/repo.ts lib/data-context.tsx
npm test
```
Expected: 에러 0, 테스트 전체 PASS.

`DataSnapshot`에 `planItems`를 넣었으므로 `emptySnapshot`·`lsRead`·`loadAll` 셋 다 채우지 않으면 tsc가 잡아준다. 잡히면 빠뜨린 곳을 채운다.

- [ ] **Step 7: 커밋**

```bash
git add supabase/migrations/0018_plan_items.sql lib/repo.ts lib/data-context.tsx
git commit -m "$(cat <<'EOF'
feat(pnl): plan_items 저장 계층 — 마이그레이션·repo·data-context

시드는 마이그레이션 INSERT가 아니라 loadAll 부트스트랩으로 넣는다
(한글 인코딩 함정 회피 + localStorage 모드 대칭).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: 계획 탭 뼈대 — 서브탭 · 히어로 · 폭포 차트

처음으로 화면에 보이는 단계. 이 Task가 끝나면 `/pnl` → 「계획」 탭에서 **잔액 25,794와 폭포 차트**가 보여야 한다.

**Files:**
- Modify: `app/pnl/page.tsx`
- Create: `components/pnl/Plan.tsx`
- Create: `components/pnl/PlanSummary.tsx`

**Interfaces:**
- Consumes: Task 1의 `computePlanPnl`·`groupTotal`, Task 2의 `useData().planItems`, 기존 `buildWaterfall`(`lib/pnl.ts`)·`WaterfallChart`(`components/pnl/WaterfallChart.tsx`)
- Produces: `<Plan />` (default export), `<PlanSummary items={PlanItem[]} />` (default export)

---

- [ ] **Step 1: `components/pnl/PlanSummary.tsx` 작성**

손글씨 노트 맨 아래 계산을 그대로 화면 최상단에 올린다.

```tsx
"use client";

import { won } from "@/lib/format";
import { groupTotal } from "@/lib/plan";
import { Card } from "@/components/budget/ui";
import type { PlanItem } from "@/lib/types";

export default function PlanSummary({ items }: { items: PlanItem[] }) {
  const income = groupTotal(items, "income");
  const spending = groupTotal(items, "spending");
  const saving = groupTotal(items, "saving", false);
  const conditional = groupTotal(items, "saving", true);
  const balance = income - spending - saving - conditional;

  const rows: { label: string; value: number }[] = [
    { label: "수입", value: income },
    { label: "월 지출", value: spending },
    { label: "저축·상환", value: saving },
    { label: "여유 시 집행", value: conditional },
  ];

  return (
    <Card>
      <table className="w-full text-sm">
        <tbody>
          {rows.map((r) => (
            <tr key={r.label}>
              <td className="py-0.5 text-stone">{r.label}</td>
              <td className="py-0.5 text-right tabular-nums text-ink">
                {won(r.value)}
              </td>
            </tr>
          ))}
          <tr className="border-t border-line">
            <td className="pt-2 font-bold text-ink">잔액</td>
            <td
              className={`pt-2 text-right text-lg font-extrabold tabular-nums ${
                balance >= 0 ? "text-leaf-dark" : "text-coral"
              }`}
            >
              {won(balance)}
            </td>
          </tr>
        </tbody>
      </table>
    </Card>
  );
}
```

- [ ] **Step 2: `components/pnl/Plan.tsx` 작성 (이번 Task 범위만)**

Task 4·5에서 리스트·대조표·규칙을 여기에 붙인다. 지금은 히어로 + 폭포까지.

```tsx
"use client";

import { useMemo, useState } from "react";
import { useData } from "@/lib/data-context";
import { computePlanPnl } from "@/lib/plan";
import { buildWaterfall } from "@/lib/pnl";
import { currentYearMonth } from "@/lib/format";
import { Card, SectionTitle } from "@/components/budget/ui";
import WaterfallChart from "./WaterfallChart";
import PlanSummary from "./PlanSummary";

export default function Plan() {
  const { planItems } = useData();
  const [month] = useState(currentYearMonth());

  const summary = useMemo(
    () => computePlanPnl(planItems, month),
    [planItems, month]
  );
  const segments = useMemo(() => buildWaterfall(summary), [summary]);

  return (
    <div className="space-y-4">
      <PlanSummary items={planItems} />

      <SectionTitle>계획 손익</SectionTitle>
      <Card>
        <WaterfallChart segments={segments} />
      </Card>
    </div>
  );
}
```

> `month`를 `useState`로 잡아 두는 이유: Task 5의 계획 vs 실적 대조가 **어느 달의 실적과 비교할지** 알아야 하고, 종료된 계획 항목(`endYearMonth`)을 걸러내는 기준도 이 값이다. Task 5에서 `MonthSwitcher`를 붙여 setter를 쓴다. 지금은 setter 미사용이라 구조분해에서 뺐다.

- [ ] **Step 3: `app/pnl/page.tsx` 서브탭 확장**

`"dashboard" | "manual"` 2개 → 3개로. 라벨 매핑을 `Record`로 바꾼다(삼항 중첩보다 읽기 쉽다).

```tsx
"use client";

import { useState } from "react";
import { useData } from "@/lib/data-context";
import Dashboard from "@/components/pnl/Dashboard";
import Plan from "@/components/pnl/Plan";
import Manual from "@/components/pnl/Manual";

type Sub = "dashboard" | "plan" | "manual";
const SUB_LABEL: Record<Sub, string> = {
  dashboard: "대시보드",
  plan: "계획",
  manual: "설명서",
};

export default function PnlPage() {
  const { mode } = useData();
  const [sub, setSub] = useState<Sub>("dashboard");

  return (
    <div>
      {/* 헤더 */}
      <header className="sticky top-0 z-30 bg-cream/90 px-4 pt-4 pb-3 backdrop-blur">
        <div className="mb-3 flex items-center gap-2">
          <span className="text-2xl">📊</span>
          <div>
            <h1 className="text-lg font-extrabold leading-none text-ink">
              가족 손익
            </h1>
            <p className="mt-0.5 text-[11px] text-stone">
              {mode === "cloud" ? "클라우드 동기화 중" : "이 기기에 저장 중"}
            </p>
          </div>
        </div>
        <div className="flex gap-1 rounded-xl bg-card p-1">
          {(Object.keys(SUB_LABEL) as Sub[]).map((k) => (
            <button
              key={k}
              onClick={() => setSub(k)}
              className={`flex-1 rounded-lg py-2 text-sm font-medium transition ${
                sub === k ? "bg-leaf text-white" : "text-stone"
              }`}
            >
              {SUB_LABEL[k]}
            </button>
          ))}
        </div>
      </header>

      <div className="px-4 pt-4">
        {sub === "dashboard" && <Dashboard />}
        {sub === "plan" && <Plan />}
        {sub === "manual" && <Manual />}
      </div>
    </div>
  );
}
```

- [ ] **Step 4: 타입체크 · 린트 · 빌드**

```bash
npx tsc --noEmit
npx eslint app/pnl/page.tsx components/pnl/Plan.tsx components/pnl/PlanSummary.tsx
npm run build
```
Expected: 에러 0. 빌드 성공.

- [ ] **Step 5: 브라우저에서 눈으로 확인**

```bash
npm run dev
```
`http://localhost:3000/pnl` → 「계획」 탭 클릭.

확인할 것:
1. 히어로에 **잔액 +25,794** (초록)
2. 수입 4,400,000 / 월 지출 2,760,000 / 저축·상환 1,254,206 / 여유 시 집행 360,000
3. 폭포 차트 5개 세그먼트(매출·고정비·선저축·변동비·운영이익)가 그려짐
4. 브라우저 콘솔에 에러 없음

> ⚠️ **클라우드 모드에서 DDL 적용 전이면 화면이 "정상처럼" 보인다 — 이게 함정이다.** 테이블이 없어도 select는 throw하지 않고 빈 배열을 주며, 그러면 시드 부트스트랩이 발동해 `planItems`가 메모리의 `SEED_PLAN_ITEMS`로 채워진다. **잔액 25,794가 멀쩡히 뜬다.** 하지만 `sbUpsert`는 에러를 검사하지 않으므로, 항목을 수정하면 UI만 갱신되고 **새로고침하면 사라진다**(조용한 유실). 따라서 **화면에 25,794가 보인다는 것은 테이블이 있다는 증거가 아니다.** 로컬 육안 검증은 `.env.local`을 임시 rename해 **localStorage 모드**로 하고, 클라우드 검증은 Task 6의 DDL 적용 이후에만 하라.

- [ ] **Step 6: 커밋**

```bash
git add app/pnl/page.tsx components/pnl/Plan.tsx components/pnl/PlanSummary.tsx
git commit -m "$(cat <<'EOF'
feat(pnl): 계획 서브탭 — 히어로 요약 + 계획 폭포 차트

손익 서브탭을 [대시보드][계획][설명서] 3개로 확장.
계획 P&L은 실적과 같은 buildWaterfall/WaterfallChart를 재사용.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: 항목 리스트 · 편집 시트 (CRUD)

계획을 앱에서 고칠 수 있게 한다.

**Files:**
- Create: `components/pnl/PlanItemList.tsx`
- Create: `components/pnl/PlanItemForm.tsx`
- Modify: `components/pnl/Plan.tsx`

**Interfaces:**
- Consumes: Task 1의 `PlanItem`·`PLAN_GROUP_LABEL`·`PNL_CLASS_LABEL`·`remainingMonths`·`groupTotal`, Task 2의 `useData().savePlanItem`/`removePlanItem`
- Produces:
  - `<PlanItemList items={PlanItem[]} month={string} onEdit={(item: PlanItem) => void} onAdd={(group: PlanGroup, conditional: boolean) => void} />`
  - `<PlanItemForm open={boolean} initial={PlanItem | null} draftGroup={PlanGroup} draftConditional={boolean} onClose={() => void} />`

---

- [ ] **Step 1: `components/pnl/PlanItemForm.tsx` 작성**

`initial`이 있으면 편집, 없으면 신규(그 경우 `draftGroup`·`draftConditional`이 초기값).

```tsx
"use client";

import { useState } from "react";
import { useData } from "@/lib/data-context";
import { newId } from "@/lib/repo";
import {
  Sheet,
  Field,
  inputCls,
  PrimaryButton,
} from "@/components/budget/ui";
import {
  PLAN_GROUP_LABEL,
  PNL_CLASS_LABEL,
  type PlanGroup,
  type PlanItem,
} from "@/lib/types";

// 숫자만 남긴다. type="number"는 iOS에서 글씨가 안 보여 쓰지 않는다.
const digits = (s: string) => s.replace(/[^0-9]/g, "");

export default function PlanItemForm({
  open,
  initial,
  draftGroup,
  draftConditional,
  onClose,
}: {
  open: boolean;
  initial: PlanItem | null;
  draftGroup: PlanGroup;
  draftConditional: boolean;
  onClose: () => void;
}) {
  const { savePlanItem, removePlanItem, planItems } = useData();

  const [name, setName] = useState(initial?.name ?? "");
  const [amount, setAmount] = useState(String(initial?.amount ?? ""));
  const [group, setGroup] = useState<PlanGroup>(initial?.group ?? draftGroup);
  const [pnlClass, setPnlClass] = useState<PlanItem["pnlClass"]>(
    initial?.pnlClass ?? "variable"
  );
  const [conditional, setConditional] = useState(
    initial?.conditional ?? draftConditional
  );
  const [endYearMonth, setEndYearMonth] = useState(initial?.endYearMonth ?? "");
  const [targetTotal, setTargetTotal] = useState(
    initial?.targetTotal == null ? "" : String(initial.targetTotal)
  );
  const [note, setNote] = useState(initial?.note ?? "");
  const [confirmDelete, setConfirmDelete] = useState(false);

  const canSave = name.trim().length > 0 && Number(amount) > 0;

  const submit = async () => {
    if (!canSave) return;
    const maxSort = planItems.reduce((m, i) => Math.max(m, i.sortOrder), 0);
    await savePlanItem({
      id: initial?.id ?? newId(),
      group,
      name: name.trim(),
      amount: Number(amount),
      pnlClass,
      conditional,
      endYearMonth: endYearMonth || null,
      targetTotal: targetTotal ? Number(targetTotal) : null,
      note: note.trim() || null,
      sortOrder: initial?.sortOrder ?? maxSort + 10,
    });
    onClose();
  };

  const remove = async () => {
    if (!initial) return;
    await removePlanItem(initial.id);
    onClose();
  };

  return (
    <Sheet
      open={open}
      onClose={onClose}
      title={initial ? "계획 항목 수정" : "계획 항목 추가"}
    >
      <Field label="항목명">
        <input
          className={inputCls}
          value={name}
          onChange={(e) => setName(e.target.value)}
          placeholder="예: 식비"
        />
      </Field>

      <Field label="월 금액">
        <input
          className={inputCls}
          type="text"
          inputMode="numeric"
          value={amount}
          onChange={(e) => setAmount(digits(e.target.value))}
          placeholder="예: 700000"
        />
      </Field>

      <Field label="구획">
        <select
          className={inputCls}
          value={group}
          onChange={(e) => setGroup(e.target.value as PlanGroup)}
        >
          {(Object.keys(PLAN_GROUP_LABEL) as PlanGroup[]).map((g) => (
            <option key={g} value={g}>
              {PLAN_GROUP_LABEL[g]}
            </option>
          ))}
        </select>
      </Field>

      <Field label="손익분류">
        <select
          className={inputCls}
          value={pnlClass}
          onChange={(e) =>
            setPnlClass(e.target.value as PlanItem["pnlClass"])
          }
        >
          {(
            Object.keys(PNL_CLASS_LABEL) as PlanItem["pnlClass"][]
          ).map((c) => (
            <option key={c} value={c}>
              {PNL_CLASS_LABEL[c]}
            </option>
          ))}
        </select>
      </Field>

      <label className="mb-3 flex items-center gap-2">
        <input
          type="checkbox"
          checked={conditional}
          onChange={(e) => setConditional(e.target.checked)}
          className="h-4 w-4 accent-[var(--color-leaf)]"
        />
        <span className="text-sm text-ink">
          여유 시 집행 — 부모급여·아동수당이 들어오면 집행
        </span>
      </label>

      <Field label="언제까지 (비우면 무기한)">
        <input
          className={inputCls}
          type="month"
          value={endYearMonth}
          onChange={(e) => setEndYearMonth(e.target.value)}
        />
      </Field>

      <Field label="잔액·목표액 (선택)">
        <input
          className={inputCls}
          type="text"
          inputMode="numeric"
          value={targetTotal}
          onChange={(e) => setTargetTotal(digits(e.target.value))}
          placeholder="예: 6264000"
        />
      </Field>

      <Field label="메모 (선택)">
        <input
          className={inputCls}
          value={note}
          onChange={(e) => setNote(e.target.value)}
          placeholder="예: 아동수당으로 충당"
        />
      </Field>

      <div className="mt-4">
        <PrimaryButton onClick={submit} disabled={!canSave}>
          저장
        </PrimaryButton>
      </div>

      {initial &&
        (confirmDelete ? (
          <div className="mt-3 flex gap-2">
            <button
              onClick={() => setConfirmDelete(false)}
              className="flex-1 rounded-xl border border-line py-2.5 text-sm text-stone"
            >
              취소
            </button>
            <button
              onClick={remove}
              className="flex-1 rounded-xl bg-coral py-2.5 text-sm font-bold text-white"
            >
              삭제 확정
            </button>
          </div>
        ) : (
          <button
            onClick={() => setConfirmDelete(true)}
            className="mt-3 w-full py-2.5 text-center text-sm text-coral"
          >
            삭제
          </button>
        ))}
    </Sheet>
  );
}
```

> 삭제는 **2단계 확인**이다. 이 앱은 1탭 즉시 하드삭제로 산책 기록을 한 번 잃은 적이 있어, 이후 삭제 버튼은 전부 2단계로 통일했다.

- [ ] **Step 2: `components/pnl/PlanItemList.tsx` 작성**

4개 섹션: 수입 / 월 지출 / 저축·상환(⊖ 제외) / 여유 시 집행(⊖만).

```tsx
"use client";

import { won } from "@/lib/format";
import { groupTotal, remainingMonths } from "@/lib/plan";
import { Card, SectionTitle, Pill } from "@/components/budget/ui";
import type { PlanGroup, PlanItem } from "@/lib/types";

function ItemRow({
  item,
  month,
  onEdit,
}: {
  item: PlanItem;
  month: string;
  onEdit: (i: PlanItem) => void;
}) {
  const left = remainingMonths(item.endYearMonth, month);
  const sub = [
    item.note,
    item.targetTotal != null ? `잔액 ${won(item.targetTotal)}` : null,
  ]
    .filter(Boolean)
    .join(" · ");

  return (
    <button
      onClick={() => onEdit(item)}
      className="flex w-full items-center gap-2 py-2 text-left"
    >
      <div className="min-w-0 flex-1">
        <div className="truncate text-sm text-ink">{item.name}</div>
        {sub && <div className="truncate text-[11px] text-stone">{sub}</div>}
      </div>
      {left != null && (
        <Pill tone={left <= 12 ? "gold" : "stone"}>{left}회 남음</Pill>
      )}
      <div className="w-24 shrink-0 text-right text-sm tabular-nums text-ink">
        {won(item.amount)}
      </div>
    </button>
  );
}

function Section({
  title,
  desc,
  items,
  total,
  month,
  onEdit,
  onAdd,
}: {
  title: string;
  desc?: string;
  items: PlanItem[];
  total: number;
  month: string;
  onEdit: (i: PlanItem) => void;
  onAdd: () => void;
}) {
  return (
    <>
      <SectionTitle
        right={
          <span className="text-sm font-bold tabular-nums text-ink">
            {won(total)}
          </span>
        }
      >
        {title}
      </SectionTitle>
      <Card>
        {desc && <p className="mb-1 text-[11px] text-stone">{desc}</p>}
        <div className="divide-y divide-line">
          {items.map((i) => (
            <ItemRow key={i.id} item={i} month={month} onEdit={onEdit} />
          ))}
        </div>
        <button
          onClick={onAdd}
          className="mt-2 w-full rounded-xl border border-dashed border-line py-2 text-sm text-stone"
        >
          + 항목 추가
        </button>
      </Card>
    </>
  );
}

export default function PlanItemList({
  items,
  month,
  onEdit,
  onAdd,
}: {
  items: PlanItem[];
  month: string;
  onEdit: (i: PlanItem) => void;
  onAdd: (group: PlanGroup, conditional: boolean) => void;
}) {
  const bySort = (a: PlanItem, b: PlanItem) => a.sortOrder - b.sortOrder;
  const pick = (g: PlanGroup, cond?: boolean) =>
    items
      .filter((i) => i.group === g)
      .filter((i) => cond === undefined || i.conditional === cond)
      .sort(bySort);

  return (
    <div>
      <Section
        title="수입"
        items={pick("income")}
        total={groupTotal(items, "income")}
        month={month}
        onEdit={onEdit}
        onAdd={() => onAdd("income", false)}
      />
      <Section
        title="월 지출"
        items={pick("spending")}
        total={groupTotal(items, "spending")}
        month={month}
        onEdit={onEdit}
        onAdd={() => onAdd("spending", false)}
      />
      <Section
        title="저축·상환"
        items={pick("saving", false)}
        total={groupTotal(items, "saving", false)}
        month={month}
        onEdit={onEdit}
        onAdd={() => onAdd("saving", false)}
      />
      <Section
        title="여유 시 집행"
        desc="최우선은 아니다. 부모급여·아동수당이 들어오면 집행한다."
        items={pick("saving", true)}
        total={groupTotal(items, "saving", true)}
        month={month}
        onEdit={onEdit}
        onAdd={() => onAdd("saving", true)}
      />
    </div>
  );
}
```

- [ ] **Step 3: `components/pnl/Plan.tsx`에 리스트·시트 연결**

Task 3의 `Plan.tsx`를 아래로 교체한다.

```tsx
"use client";

import { useMemo, useState } from "react";
import { useData } from "@/lib/data-context";
import { computePlanPnl } from "@/lib/plan";
import { buildWaterfall } from "@/lib/pnl";
import { currentYearMonth } from "@/lib/format";
import { Card, SectionTitle } from "@/components/budget/ui";
import WaterfallChart from "./WaterfallChart";
import PlanSummary from "./PlanSummary";
import PlanItemList from "./PlanItemList";
import PlanItemForm from "./PlanItemForm";
import type { PlanGroup, PlanItem } from "@/lib/types";

export default function Plan() {
  const { planItems } = useData();
  const [month] = useState(currentYearMonth());

  // 시트 상태 — editing이 있으면 수정, 없으면 draft로 신규
  const [sheetOpen, setSheetOpen] = useState(false);
  const [editing, setEditing] = useState<PlanItem | null>(null);
  const [draftGroup, setDraftGroup] = useState<PlanGroup>("spending");
  const [draftConditional, setDraftConditional] = useState(false);

  const summary = useMemo(
    () => computePlanPnl(planItems, month),
    [planItems, month]
  );
  const segments = useMemo(() => buildWaterfall(summary), [summary]);

  const openEdit = (item: PlanItem) => {
    setEditing(item);
    setSheetOpen(true);
  };
  const openAdd = (group: PlanGroup, conditional: boolean) => {
    setEditing(null);
    setDraftGroup(group);
    setDraftConditional(conditional);
    setSheetOpen(true);
  };

  return (
    <div className="space-y-4">
      <PlanSummary items={planItems} />

      <SectionTitle>계획 손익</SectionTitle>
      <Card>
        <WaterfallChart segments={segments} />
      </Card>

      <PlanItemList
        items={planItems}
        month={month}
        onEdit={openEdit}
        onAdd={openAdd}
      />

      {sheetOpen && (
        <PlanItemForm
          open
          initial={editing}
          draftGroup={draftGroup}
          draftConditional={draftConditional}
          onClose={() => setSheetOpen(false)}
        />
      )}
    </div>
  );
}
```

> ⚠️ `PlanItemForm`을 `{sheetOpen && ...}`로 **조건부 마운트**하는 게 핵심이다. `PlanItemForm`은 `useState` 초기값으로 `initial`을 읽으므로, 항상 마운트해 두면 다른 항목을 눌러도 폼이 옛 값을 유지한다. 언마운트/재마운트가 초기화 역할을 한다. (effect로 상태를 리셋하면 `react-hooks/set-state-in-effect` 린트 에러가 난다.)

- [ ] **Step 4: 타입체크 · 린트 · 빌드**

```bash
npx tsc --noEmit
npx eslint components/pnl/Plan.tsx components/pnl/PlanItemList.tsx components/pnl/PlanItemForm.tsx
npm run build
```
Expected: 에러 0.

- [ ] **Step 5: 브라우저 수동 검증 (localStorage 모드)**

`.env.local`을 `.env.local.bak`으로 임시 rename → `npm run dev` → `/pnl` 「계획」 탭.

1. 4개 섹션이 보이고 소계가 각각 4,400,000 / 2,760,000 / 1,254,206 / 360,000
2. `자동차 할부` 행에 `8회 남음`(gold) Pill + 부제 `월 303,206 + 추가상환 50,000 · 잔액 2,425,648`
   (⚠️ 기준월이 2026-08이 아니면 회차가 다르다. 오늘이 2026-07이면 `9회 남음`이 정상)
3. `식비` 행 탭 → 시트에 식비/700000/월 지출/변동비가 채워져 있음. 금액을 800000으로 고치고 저장 → 리스트·히어로·폭포가 즉시 갱신, 잔액이 74,206 감소
4. 되돌리기(700000으로 재수정)
5. `여유 시 집행` 섹션에서 `+ 항목 추가` → 시트가 **빈 폼**이고 "여유 시 집행" 체크가 **켜져** 있음
6. 새로 만든 항목을 다시 열어 `삭제` → `삭제 확정` 2단계로 사라짐
7. 새로고침 후에도 수정이 유지됨(localStorage 저장 확인)

끝나면 `.env.local.bak` → `.env.local` **반드시 복원**.

- [ ] **Step 6: 커밋**

```bash
git add components/pnl/Plan.tsx components/pnl/PlanItemList.tsx components/pnl/PlanItemForm.tsx
git commit -m "$(cat <<'EOF'
feat(pnl): 계획 항목 리스트 + 편집 시트

4섹션(수입·월 지출·저축·상환·여유 시 집행) 그룹별 리스트,
행 탭으로 편집·2단계 삭제, 섹션별 항목 추가.
종료 예정 항목은 "N회 남음" Pill.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: 계획 vs 실적 대조 · 예산 규칙

"예산 농사"의 본체. 계획을 세운 값과 실제 쓴 값을 나란히 놓는다.

**Files:**
- Create: `components/pnl/PlanVsActual.tsx`
- Create: `components/pnl/BudgetRules.tsx`
- Modify: `components/pnl/Plan.tsx`

**Interfaces:**
- Consumes: Task 1의 `computePlanPnl`·`BUDGET_RULES`, 기존 `computePnl`(`lib/pnl.ts`)·`useData().transactions`/`categoryById`, 기존 `MonthSwitcher`(`components/budget/ui.tsx`)
- Produces: `<PlanVsActual month={string} />`, `<BudgetRules />`

---

- [ ] **Step 1: `components/pnl/PlanVsActual.tsx` 작성**

```tsx
"use client";

import { useMemo } from "react";
import { useData } from "@/lib/data-context";
import { computePnl } from "@/lib/pnl";
import { computePlanPnl } from "@/lib/plan";
import { won } from "@/lib/format";
import { Card } from "@/components/budget/ui";
import type { PnlSummary } from "@/lib/types";

type Key = "revenue" | "fixed" | "saving" | "variable" | "operatingProfit";

const ROWS: { key: Key; label: string }[] = [
  { key: "revenue", label: "매출" },
  { key: "fixed", label: "고정비" },
  { key: "saving", label: "선저축" },
  { key: "variable", label: "변동비" },
  { key: "operatingProfit", label: "운영이익" },
];

// 차이의 색: 매출·운영이익은 많을수록 좋고(+가 초록), 비용은 적을수록 좋다(+가 코랄).
function diffTone(key: Key, diff: number): string {
  if (diff === 0) return "text-stone";
  const goodWhenHigher = key === "revenue" || key === "operatingProfit";
  const good = goodWhenHigher ? diff > 0 : diff < 0;
  return good ? "text-leaf-dark" : "text-coral";
}

export default function PlanVsActual({ month }: { month: string }) {
  const { planItems, transactions, categoryById } = useData();

  const plan: PnlSummary = useMemo(
    () => computePlanPnl(planItems, month),
    [planItems, month]
  );
  const actual: PnlSummary = useMemo(
    () =>
      computePnl(
        transactions.filter((t) => t.date.startsWith(month)),
        categoryById
      ),
    [transactions, categoryById, month]
  );

  return (
    <Card>
      <table className="w-full text-sm">
        <thead>
          <tr className="text-[11px] text-stone">
            <th className="pb-1 text-left font-semibold">구분</th>
            <th className="pb-1 text-right font-semibold">계획</th>
            <th className="pb-1 text-right font-semibold">실적</th>
            <th className="pb-1 text-right font-semibold">차이</th>
          </tr>
        </thead>
        <tbody>
          {ROWS.map(({ key, label }) => {
            const p = plan[key];
            const a = actual[key];
            const diff = a - p;
            const last = key === "operatingProfit";
            return (
              <tr
                key={key}
                className={last ? "border-t border-line font-bold" : ""}
              >
                <td className={`py-1 text-ink ${last ? "pt-2" : ""}`}>
                  {label}
                </td>
                <td
                  className={`py-1 text-right tabular-nums text-stone ${
                    last ? "pt-2" : ""
                  }`}
                >
                  {won(p)}
                </td>
                <td
                  className={`py-1 text-right tabular-nums text-ink ${
                    last ? "pt-2" : ""
                  }`}
                >
                  {won(a)}
                </td>
                <td
                  className={`py-1 text-right tabular-nums ${diffTone(
                    key,
                    diff
                  )} ${last ? "pt-2" : ""}`}
                >
                  {diff > 0 ? "+" : ""}
                  {won(diff)}
                </td>
              </tr>
            );
          })}
        </tbody>
      </table>
      <p className="mt-3 text-[11px] leading-relaxed text-stone">
        실적은 가계부 거래에서 자동 집계된다. 고정비는 캘린더 탭의 고정지출
        체크리스트를 체크해야 거래가 생겨 잡힌다.
      </p>
    </Card>
  );
}
```

> 마지막 안내문이 중요하다. 이 앱의 고정지출은 **등록만으로는 거래가 생기지 않고** 캘린더에서 체크해야 `source:"auto"` 거래가 만들어진다. 이걸 모르면 "고정비 실적이 0인데 왜?"로 헤맨다.

- [ ] **Step 2: `components/pnl/BudgetRules.tsx` 작성**

```tsx
"use client";

import { useState } from "react";
import { Card } from "@/components/budget/ui";
import { BUDGET_RULES } from "@/lib/types";

export default function BudgetRules() {
  const [open, setOpen] = useState(false);

  return (
    <Card>
      <button
        onClick={() => setOpen((v) => !v)}
        className="flex w-full items-center justify-between text-left"
      >
        <span className="text-sm font-bold text-ink">예산 규칙</span>
        <span className="text-stone">{open ? "▴" : "▾"}</span>
      </button>
      {open && (
        <ul className="mt-3 space-y-1.5">
          {BUDGET_RULES.map((r) => (
            <li key={r} className="flex gap-2 text-[13px] leading-relaxed text-stone">
              <span className="text-leaf">·</span>
              <span>{r}</span>
            </li>
          ))}
        </ul>
      )}
    </Card>
  );
}
```

- [ ] **Step 3: `components/pnl/Plan.tsx` 최종본**

`MonthSwitcher`를 붙여 `setMonth`를 쓴다(Task 3에서 예고한 부분).

Task 4의 `Plan.tsx`에서 바뀌는 곳:
1. import 3줄 추가:
```tsx
import { Card, SectionTitle, MonthSwitcher } from "@/components/budget/ui";
import PlanVsActual from "./PlanVsActual";
import BudgetRules from "./BudgetRules";
```
(기존 `import { Card, SectionTitle } from "@/components/budget/ui";`를 위 첫 줄로 교체)

2. state에 setter 사용:
```tsx
  const [month, setMonth] = useState(currentYearMonth());
```

3. JSX — `<PlanSummary>` 위에 `MonthSwitcher`, `<PlanItemList>` 위에 대조표, 맨 아래 규칙:
```tsx
  return (
    <div className="space-y-4">
      <MonthSwitcher ym={month} onChange={setMonth} />

      <PlanSummary items={planItems} />

      <SectionTitle>계획 손익</SectionTitle>
      <Card>
        <WaterfallChart segments={segments} />
      </Card>

      <SectionTitle>계획 vs 실적</SectionTitle>
      <PlanVsActual month={month} />

      <PlanItemList
        items={planItems}
        month={month}
        onEdit={openEdit}
        onAdd={openAdd}
      />

      <div className="pt-2">
        <BudgetRules />
      </div>

      {sheetOpen && (
        <PlanItemForm
          open
          initial={editing}
          draftGroup={draftGroup}
          draftConditional={draftConditional}
          onClose={() => setSheetOpen(false)}
        />
      )}
    </div>
  );
```

- [ ] **Step 4: 타입체크 · 린트 · 빌드 · 전체 테스트**

```bash
npx tsc --noEmit
npx eslint components/pnl/
npm run build
npm test
```
Expected: 에러 0, 테스트 전체 PASS.

- [ ] **Step 5: 브라우저 수동 검증**

localStorage 모드(`.env.local` 임시 rename)로 `/pnl` 「계획」 탭:
1. 상단 월 스위처 `‹ 2026년 7월 ›` — `›`를 눌러 8월로 이동하면 `자동차 할부` Pill이 `9회 남음` → `8회 남음`으로 바뀜
2. 계획 vs 실적 표에서 계획 열이 4,400,000 / … / 25,794
3. 실적 열은 거래가 없으면 전부 0, 운영이익 차이는 `-25,794`(코랄)
4. 2027-04로 이동하면 계획 고정비가 353,206 줄어듦(자동차 할부 종료)
5. `예산 규칙` 접이식이 열리고 4줄이 보임

`.env.local` **복원**.

- [ ] **Step 6: 커밋**

```bash
git add components/pnl/Plan.tsx components/pnl/PlanVsActual.tsx components/pnl/BudgetRules.tsx
git commit -m "$(cat <<'EOF'
feat(pnl): 계획 vs 실적 대조표 + 예산 규칙 메모

계획·실적을 4버킷으로 나란히 놓고 차이를 색으로 표시
(매출·운영이익은 +가 좋고, 비용은 −가 좋다).
월 스위처로 기준월 이동 — 종료된 계획 항목이 자동으로 빠진다.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: DDL 적용 · 배포 · 프로덕션 검증

코드가 다 됐으니 실제 DB에 테이블을 만들고 배포한다.

**Files:** (코드 변경 없음)

---

- [ ] **Step 1: 재헌님께 DDL 적용 승인 요청**

프로덕션 DDL은 자동으로 실행하지 않는다. 아래를 알리고 **명시 승인을 받는다.**

> `plan_items` 테이블을 개인 Supabase 프로젝트(`jyfjnxvsblfkkbscddnw`)에 생성합니다.
> - 신규 테이블 생성만 — 기존 테이블·데이터를 건드리지 않습니다.
> - `drop`·`alter` 없음. 되돌리려면 `drop table plan_items` 하나면 됩니다.
> - 적용 후 앱을 처음 열 때 27개 시드 항목이 자동 삽입됩니다.

- [ ] **Step 2: DDL 적용**

Supabase Management API로 `0018_plan_items.sql`을 실행한다.

- 토큰: `~/.claude.json`의 `SUPABASE_ACCESS_TOKEN` (`sbp_` personal token)
- 엔드포인트: `POST https://api.supabase.com/v1/projects/jyfjnxvsblfkkbscddnw/database/query`
- **node https로 POST**한다. SQL이 순수 ASCII라 인코딩 함정은 없지만, PowerShell `Invoke-RestMethod`는 이 프로젝트에서 신뢰하지 않는 경로다.
- 크레덴셜 파일을 읽어야 하므로 `dangerouslyDisableSandbox: true`가 필요하다 — 이것도 Step 1의 승인 범위에 포함해 알린다.

- [ ] **Step 3: 테이블 생성 검증**

같은 엔드포인트로 확인 쿼리를 날린다.

```sql
select column_name, data_type, is_nullable
from information_schema.columns
where table_name = 'plan_items'
order by ordinal_position;
```
Expected: 11개 컬럼(`id`, `group`, `name`, `amount`, `pnl_class`, `conditional`, `end_year_month`, `target_total`, `note`, `sort_order`, `created_at`).

> **순서 엄수: DDL 적용(Step 2) → 검증(Step 3) → 배포(Step 4).** 반대로 하면 프로덕션에서 계획 탭이 정상처럼 보이면서 편집이 조용히 유실된다. DDL을 먼저 적용하는 것은 안전하다 — 구버전 프로덕션 코드는 `plan_items`를 참조하지 않으므로 빈 테이블이 놀고 있을 뿐이다.

- [ ] **Step 4: 배포**

```bash
git push
```
Vercel이 자동 배포한다. 배포 완료를 기다린다.

- [ ] **Step 5: 프로덕션 검증**

https://baechoo-family.vercel.app/pnl → 「계획」 탭.

1. 27개 항목이 자동으로 들어가 있다(부트스트랩 동작)
2. **잔액 +25,794** — 종이 계획과 일치
3. 항목 하나를 수정 → 새로고침 → 유지됨(클라우드 저장 확인)
4. 브라우저 콘솔 에러 없음

시드가 실제로 DB에 들어갔는지 쿼리로 재확인한다.
```sql
select count(*) from plan_items;
```
Expected: `27`

> ⚠️ 부트스트랩은 `loadAll`에서 **테이블이 비어 있을 때만** 돈다. 만약 시드가 절반만 들어가는 등 이상하면, `delete from plan_items;` 후 앱을 다시 열어 재시드한다.

- [ ] **Step 6: 최종 커밋 (문서 갱신)**

`docs/가족-pnl/회계모델-규칙서.md`에 계획 탭 절을 추가하고 커밋한다 — 재헌님이 나중에 읽을 정본이다.

```markdown
## 예산 계획 (계획 탭)

계획은 실적과 **같은 4버킷**(매출·고정비·선저축·변동비)으로 집계한다.
항목마다 손익분류를 직접 지정하므로, 카테고리 자동규칙에 의존하지 않는다.

- **구획(group)** = 화면상의 자리 — 수입 / 월 지출 / 저축·상환
- **손익분류(pnlClass)** = 손익 집계 버킷
- 둘은 독립이다. 학자금 상환은 구획이 `저축·상환`이지만 손익분류는 `고정비`다.
- **여유 시 집행(⊖)** = 최우선이 아닌 항목. 부모급여·아동수당이 들어오면 집행한다.
  손익 집계에는 정상 포함되고, 화면에서만 별도 섹션으로 묶인다.
- **언제까지(endYearMonth)** = 그 달까지 집행하고 다음 달부터 계획에서 빠진다.

2026-07 기준 계획 운영이익 = **25,794원**.
```

```bash
git add docs/가족-pnl/회계모델-규칙서.md
git commit -m "$(cat <<'EOF'
docs(pnl): 회계모델 규칙서에 예산 계획 절 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
git push
```

---

## 완료 기준

- [ ] `npm test` 전체 PASS (기존 테스트 + `lib/plan.test.ts` 15개)
- [ ] `npm run build` 성공, `npx eslint .` 에러 0
- [ ] 프로덕션 `/pnl` 「계획」 탭에서 **잔액 +25,794** 표시
- [ ] 항목 추가·수정·삭제가 클라우드에 저장되고 새로고침 후 유지
- [ ] 계획 vs 실적 대조표가 이번 달 실적을 4버킷으로 보여줌
- [ ] 기존 대시보드·설명서 서브탭이 그대로 동작(회귀 없음)
