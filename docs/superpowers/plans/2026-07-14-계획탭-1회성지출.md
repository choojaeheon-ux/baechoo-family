# 계획 탭 1회성 지출 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 배추가족 PWA `📊손익` → 「계획」 탭에서 특정 월에만 반영되는 1회성 지출을 만들고, 앞으로 예정된 1회성들을 현재 달 화면에서 미리 볼 수 있게 한다.

**Architecture:** `PlanItem`에 `startYearMonth` 한 컬럼을 추가해 판정 규칙을 **`시작월 ≤ 기준월 ≤ 종료월`** 하나로 통일한다. 1회성은 `start === end`라는 파생 조건(`isOneTime`)으로 알아내므로 `oneTime` 플래그도, `if (oneTime) … else …` 분기도 필요 없다. 기존 27개 항목은 전부 `start = null`이라 데이터 마이그레이션 없이 동작이 그대로 유지된다.

**Tech Stack:** Next.js 16 (App Router) · React 19 · TypeScript · Tailwind v4 · Supabase (supabase-js) · vitest

## Global Constraints

- **언어**: 모든 UI 문자열·주석은 한국어.
- **작업 디렉터리**: `C:\Users\MGRV\.claude\baechoo-family`. 모든 경로는 이 레포 기준 상대경로.
- **브랜치**: `feat/plan-onetime`을 `main`에서 새로 딴다. main에서 직접 작업하지 않는다.
- **테스트**: `npm test` (vitest run). 단일 파일은 `npx vitest run lib/plan.test.ts`.
- **린트**: `npx eslint <파일>`. ⚠️ `react-hooks/set-state-in-effect` 룰이 `useEffect` 안의 `setState` 직접 호출을 **에러**로 막는다. 조건부 상태 리셋은 effect가 아니라 **파생값/조건부 마운트**로 푼다.
  - ⚠️ 기존 파일 6곳(`PinGate.tsx`·`baechoo/BaechooCalendar.tsx`·`TrashSheet.tsx`·`WalkMap.tsx`·`WalkTracker.tsx`·`budget/CalendarView.tsx`)에 이 룰 위반이 **이미 있다.** 이 프로젝트가 만든 게 아니므로 고치지 않는다. `npx eslint .`가 6 errors를 내는 건 정상이다. **당신이 만든 파일만 깨끗하면 된다.**
- **UI 프리미티브 신설 금지**: `components/budget/ui.tsx`의 `Card`·`SectionTitle`·`Sheet`·`Field`·`inputCls`·`PrimaryButton`·`Pill`·`Empty`를 재사용한다.
- **숫자 입력**: `type="number"` 금지(iOS에서 글씨가 안 보이는 기존 버그). `type="text"` + `inputMode="numeric"` + 숫자 필터.
- **삭제는 2단계 확인** (취소 / 삭제 확정). 과거 1탭 하드삭제로 사용자 기록을 잃은 사고가 있었다.
- **`group`(화면 구획)과 `pnlClass`(손익 집계 버킷)는 독립 축**이다. 한쪽에서 다른 쪽을 자동 추론하지 않는다. 유일한 예외는 **신규 항목의 초기값**(`defaultPnlClass`)이며, 이미 구현돼 있다.
- **마이그레이션 SQL은 순수 ASCII.** 한글 INSERT는 Management API 경유 시 깨진 사고가 있었다.
- **커밋**: 각 Task 끝에서 1커밋. 메시지는 한국어 본문 + 아래 트레일러.
  ```
  Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
  ```

### 골든 값 (절대 바뀌면 안 됨)

시드 28개 항목의 계획 P&L:

| 기준월 | 운영이익 | 의미 |
|---|---|---|
| **2026-07** (앱 기본 화면) | **25,794** | 매달 반복분 기준선 — 재헌님 손글씨 노트의 "잔액" |
| **2026-08** | **−974,206** | 어머니 산후조리비 1,000,000이 반영된 달 |
| **2026-09** | **25,794** | 기준선 복귀 |

2026-08의 4버킷: `revenue 4,400,000 / fixed 2,299,206 / saving 775,000 / variable 2,300,000`.
2026-07·09의 4버킷: `revenue 4,400,000 / fixed 2,299,206 / saving 775,000 / variable 1,300,000`.

---

## File Structure

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `lib/types.ts` | `PlanItem.startYearMonth` 추가, `BUDGET_RULES`에서 한 줄 삭제 | 수정 |
| `lib/seed.ts` | 27개 항목에 `startYearMonth: null` 추가 + 28번째(어머니 산후조리비) | 수정 |
| `lib/plan.ts` | `activeItems` 시작 경계 추가, `isOneTime`·`upcomingOneTime` 신규 | 수정 |
| `lib/plan.test.ts` | 골든 기준월 이동 + 1회성 테스트 | 수정 |
| `supabase/migrations/0019_plan_item_start_month.sql` | `start_year_month` 컬럼 추가 (ASCII) | 신규 |
| `lib/repo.ts` | `toPlanItem`/`fromPlanItem`에 `start_year_month` | 수정 |
| `components/pnl/PlanItemForm.tsx` | 「적용」 세그먼트 `[매달]`/`[이 달만]` | 수정 |
| `components/pnl/PlanItemList.tsx` | 1회성 항목에 `1회성` 배지 | 수정 |
| `components/pnl/UpcomingOneTime.tsx` | 「다가오는 1회성 지출」 카드 | 신규 |
| `components/pnl/Plan.tsx` | 카드 배선 + 1회성 추가 진입점 | 수정 |

---

## Task 0: 브랜치 생성

- [ ] **Step 1: main에서 브랜치를 딴다**

```bash
git checkout main
git pull
git checkout -b feat/plan-onetime
git branch --show-current
```
Expected: `feat/plan-onetime`

커밋 없음. 다음 Task로 넘어간다.

---

## Task 1: 모델 · 시드 · 계산 로직 (순수함수)

DB도 화면도 없이, **28개 항목에서 세 달의 값이 정확히 나오는지**만 검증한다. 이게 맞으면 나머지는 배선이다.

**Files:**
- Modify: `lib/types.ts`
- Modify: `lib/seed.ts`
- Modify: `lib/plan.ts`
- Test: `lib/plan.test.ts`

**Interfaces:**
- Consumes: 기존 `PlanItem`·`PlanGroup`·`PnlSummary`·`SEED_PLAN_ITEMS`
- Produces:
  - `PlanItem.startYearMonth: string | null` (새 필드)
  - `activeItems(items: PlanItem[], yearMonth: string): PlanItem[]` (시작 경계 추가)
  - `isOneTime(i: PlanItem): boolean`
  - `upcomingOneTime(items: PlanItem[], yearMonth: string): PlanItem[]`
  - `SEED_PLAN_ITEMS` 28개 (`plan-once-postpartum` 추가)
  - `BUDGET_RULES` 3줄 (엄마 줄 삭제)

---

- [ ] **Step 1: `lib/types.ts` — `PlanItem`에 `startYearMonth` 추가**

`PlanItem` 인터페이스에서 `endYearMonth` **바로 위**에 넣는다(읽는 순서가 시작→종료라야 자연스럽다).

```ts
  startYearMonth: string | null; // "YYYY-MM", null = 시작 제한 없음. 이 달부터 집행.
  endYearMonth: string | null; // "YYYY-MM", null = 무기한. 이 달까지 집행.
```

같은 파일의 `PlanItem` 위 주석에 규칙을 한 줄 덧붙인다.

```ts
// 월 예산 계획 1항목.
// group은 화면 구획, pnlClass는 손익 집계 버킷 — 둘은 독립이다.
// (예: 학자금 상환은 group=saving이지만 pnlClass=fixed)
// 집행 여부: 시작월 <= 기준월 <= 종료월 (null은 무한대). 1회성은 시작월 === 종료월.
```

- [ ] **Step 2: `lib/types.ts` — `BUDGET_RULES`에서 한 줄 삭제**

아래 줄을 **지운다.**
```ts
  "엄마 1,000,000은 첫만남 이용권으로 충당 — 예산에서 제외",
```

삭제 후 `BUDGET_RULES`는 정확히 이 3줄이어야 한다.
```ts
export const BUDGET_RULES: string[] = [
  "금액은 넉넉하게 올려 잡는다",
  "현금·배찌 주식은 계획에서 잊는다",
  "첫만남 이용권·안양시 200만원 등 일회성 금액은 잊는다",
];
```

> 이유: 그 1,000,000이 이제 **어머니 산후조리비라는 실제 1회성 지출**로 예산에 잡힌다(Step 3). "예산에서 제외"는 더 이상 사실이 아니다.

- [ ] **Step 3: `lib/seed.ts` — 27개 항목에 `startYearMonth: null` 추가 + 28번째 항목**

기존 27개 항목 각각에 **`startYearMonth: null,`을 `endYearMonth` 앞에 넣는다.** 금액·id·pnlClass 등 **다른 값은 한 자리도 바꾸지 말 것** — 재헌님 손글씨 노트의 골든 값이다.

그리고 「월 지출」 블록 맨 끝(`plan-exp-fuel` 다음)에 28번째 항목을 추가한다.

```ts
  // ── 1회성 지출 (특정 월에만 반영)
  { id: "plan-once-postpartum", group: "spending", name: "어머니 산후조리비", amount: 1000000, pnlClass: "variable", conditional: false, startYearMonth: "2026-08", endYearMonth: "2026-08", targetTotal: null, note: null, sortOrder: 240 },
```

시드 상단 주석도 갱신한다.

```ts
// 월 예산 계획 시드 — 재헌님 손글씨 노트(2026-07-13). 합계는 골든 값이다.
//   수입 4,400,000 / 월 지출 2,760,000 / 저축·상환 1,254,206 / 여유 시 집행 360,000
//   → 계획 운영이익 25,794 (노트의 "잔액"과 원 단위 일치)
// 여기에 1회성 지출 1건(어머니 산후조리비, 2026-08)이 더해진다.
//   → 2026-08만 운영이익 -974,206, 그 외 달은 25,794
```

- [ ] **Step 4: 실패하는 테스트 작성 — `lib/plan.test.ts`**

기존 파일을 아래로 **교체**한다. 기존 테스트의 기준월 `YM = "2026-08"`은 이제 산후조리비가 있는 달이라 기준선이 아니다 → **`2026-09`로 옮긴다.**

```ts
import { describe, it, expect } from "vitest";
import {
  activeItems,
  computePlanPnl,
  groupTotal,
  remainingMonths,
  isOneTime,
  upcomingOneTime,
} from "./plan";
import { SEED_PLAN_ITEMS } from "./seed";
import type { PlanItem } from "./types";

// 기준선 달 — 1회성(2026-08 산후조리비)이 없고, 아직 아무 항목도 끝나지 않은 달
const YM = "2026-09";

describe("computePlanPnl — 손글씨 계획 골든 케이스", () => {
  it("기준선 달의 운영이익이 노트의 잔액 25,794와 일치한다", () => {
    expect(computePlanPnl(SEED_PLAN_ITEMS, YM).operatingProfit).toBe(25794);
  });

  it("앱 기본 화면(2026-07)도 기준선 25,794다", () => {
    expect(computePlanPnl(SEED_PLAN_ITEMS, "2026-07").operatingProfit).toBe(25794);
  });

  it("4버킷 합계가 골든 값과 일치한다", () => {
    const s = computePlanPnl(SEED_PLAN_ITEMS, YM);
    expect(s.revenue).toBe(4400000);
    expect(s.fixed).toBe(2299206);
    expect(s.saving).toBe(775000);
    expect(s.variable).toBe(1300000);
    expect(s.grossProfit).toBe(4400000 - 2299206 - 775000);
    expect(s.bepAchieved).toBe(true);
  });

  it("빈 계획이면 전 필드 0이고 operatingMargin은 0이다 (0으로 나누기 가드)", () => {
    const s = computePlanPnl([], YM);
    expect(s.revenue).toBe(0);
    expect(s.operatingProfit).toBe(0);
    expect(s.operatingMargin).toBe(0);
  });
});

describe("1회성 지출 — 어머니 산후조리비 (2026-08)", () => {
  it("2026-08의 운영이익은 산후조리비 1,000,000만큼 떨어진다", () => {
    expect(computePlanPnl(SEED_PLAN_ITEMS, "2026-08").operatingProfit).toBe(-974206);
  });

  it("2026-08의 변동비에 산후조리비가 더해진다", () => {
    expect(computePlanPnl(SEED_PLAN_ITEMS, "2026-08").variable).toBe(1300000 + 1000000);
  });

  it("2026-08에는 살아 있다", () => {
    const ids = activeItems(SEED_PLAN_ITEMS, "2026-08").map((i) => i.id);
    expect(ids).toContain("plan-once-postpartum");
  });

  it("그 전달(2026-07)·다음달(2026-09)에는 없다", () => {
    expect(
      activeItems(SEED_PLAN_ITEMS, "2026-07").map((i) => i.id)
    ).not.toContain("plan-once-postpartum");
    expect(
      activeItems(SEED_PLAN_ITEMS, "2026-09").map((i) => i.id)
    ).not.toContain("plan-once-postpartum");
  });
});

describe("isOneTime", () => {
  const base: PlanItem = {
    id: "x", group: "spending", name: "테스트", amount: 1000,
    pnlClass: "variable", conditional: false,
    startYearMonth: null, endYearMonth: null,
    targetTotal: null, note: null, sortOrder: 0,
  };

  it("시작월과 종료월이 같으면 1회성이다", () => {
    expect(isOneTime({ ...base, startYearMonth: "2026-08", endYearMonth: "2026-08" })).toBe(true);
  });

  it("시작월이 없으면 1회성이 아니다 (종료월만 있는 할부 등)", () => {
    expect(isOneTime({ ...base, startYearMonth: null, endYearMonth: "2027-03" })).toBe(false);
  });

  it("시작월과 종료월이 다르면 1회성이 아니다", () => {
    expect(isOneTime({ ...base, startYearMonth: "2026-08", endYearMonth: "2026-10" })).toBe(false);
  });

  it("둘 다 없으면 1회성이 아니다 (매달 항목)", () => {
    expect(isOneTime(base)).toBe(false);
  });
});

describe("upcomingOneTime — 다가오는 1회성 지출", () => {
  it("2026-07 기준으로 8월 산후조리비가 잡힌다", () => {
    const up = upcomingOneTime(SEED_PLAN_ITEMS, "2026-07");
    expect(up.map((i) => i.id)).toEqual(["plan-once-postpartum"]);
  });

  it("당월(2026-08)은 '다가오는'이 아니다 — 이미 이 달 계획에 들어 있다", () => {
    expect(upcomingOneTime(SEED_PLAN_ITEMS, "2026-08")).toEqual([]);
  });

  it("지나간 뒤(2026-09)에는 비어 있다", () => {
    expect(upcomingOneTime(SEED_PLAN_ITEMS, "2026-09")).toEqual([]);
  });

  it("여러 개면 월 오름차순으로 정렬한다", () => {
    const mk = (id: string, ym: string): PlanItem => ({
      id, group: "spending", name: id, amount: 1000,
      pnlClass: "variable", conditional: false,
      startYearMonth: ym, endYearMonth: ym,
      targetTotal: null, note: null, sortOrder: 0,
    });
    const items = [mk("c", "2026-12"), mk("a", "2026-09"), mk("b", "2026-10")];
    expect(upcomingOneTime(items, "2026-08").map((i) => i.id)).toEqual(["a", "b", "c"]);
  });

  it("매달 항목은 잡히지 않는다", () => {
    const up = upcomingOneTime(SEED_PLAN_ITEMS, "2026-01");
    expect(up.map((i) => i.id)).not.toContain("plan-exp-rent");
  });
});

describe("activeItems — 시작·종료 경계", () => {
  it("endYearMonth가 기준월보다 이전이면 제외한다", () => {
    const ids = activeItems(SEED_PLAN_ITEMS, "2027-04").map((i) => i.id);
    expect(ids).not.toContain("plan-sav-car-loan");
  });

  it("endYearMonth가 기준월과 같으면 포함한다 (그 달까지 납부)", () => {
    const ids = activeItems(SEED_PLAN_ITEMS, "2027-03").map((i) => i.id);
    expect(ids).toContain("plan-sav-car-loan");
  });

  it("startYearMonth가 기준월보다 이후면 제외한다", () => {
    const ids = activeItems(SEED_PLAN_ITEMS, "2026-07").map((i) => i.id);
    expect(ids).not.toContain("plan-once-postpartum");
  });

  it("startYearMonth가 null이면 아무리 과거여도 포함한다", () => {
    const ids = activeItems(SEED_PLAN_ITEMS, "2020-01").map((i) => i.id);
    expect(ids).toContain("plan-exp-rent");
  });

  it("computePlanPnl은 종료된 항목을 빼고 집계한다", () => {
    const s = computePlanPnl(SEED_PLAN_ITEMS, "2027-04");
    expect(s.fixed).toBe(2299206 - 353206);
  });
});

describe("groupTotal — 기준월 필터를 타지 않는다 (호출자가 걸러 넘긴다)", () => {
  it("월 지출 = 2,760,000 + 산후조리비 1,000,000 = 3,760,000", () => {
    // groupTotal에는 yearMonth 인자가 없다. Plan.tsx가 activeItems로 거른
    // 목록을 넘기므로 화면은 정상이다. 이 테스트는 그 계약을 못박아 둔다.
    expect(groupTotal(SEED_PLAN_ITEMS, "spending")).toBe(3760000);
  });

  it("기준월로 거른 목록을 넘기면 그 달의 소계가 나온다", () => {
    const jul = activeItems(SEED_PLAN_ITEMS, "2026-07");
    const aug = activeItems(SEED_PLAN_ITEMS, "2026-08");
    expect(groupTotal(jul, "spending")).toBe(2760000);
    expect(groupTotal(aug, "spending")).toBe(3760000);
  });

  it("저축·상환(⊖ 제외) = 1,254,206", () => {
    expect(groupTotal(SEED_PLAN_ITEMS, "saving", false)).toBe(1254206);
  });

  it("여유 시 집행(⊖만) = 360,000", () => {
    expect(groupTotal(SEED_PLAN_ITEMS, "saving", true)).toBe(360000);
  });

  it("수입 = 4,400,000", () => {
    expect(groupTotal(SEED_PLAN_ITEMS, "income")).toBe(4400000);
  });
});

describe("remainingMonths", () => {
  it("2026-08 기준 2027-03까지는 8회 남는다", () => {
    expect(remainingMonths("2027-03", "2026-08")).toBe(8);
  });

  it("2026-08 기준 2028-12까지는 29회 남는다", () => {
    expect(remainingMonths("2028-12", "2026-08")).toBe(29);
  });

  it("당월이 마지막이면 1회 남는다", () => {
    expect(remainingMonths("2026-08", "2026-08")).toBe(1);
  });

  it("이미 지났으면 0이다", () => {
    expect(remainingMonths("2026-07", "2026-08")).toBe(0);
  });

  it("무기한(null)이면 null이다", () => {
    expect(remainingMonths(null, "2026-08")).toBe(null);
  });
});

describe("conditional 항목도 손익 집계에는 포함된다", () => {
  it("⊖ 항목을 빼면 운영이익이 360,000 늘어난다", () => {
    const withoutCond = SEED_PLAN_ITEMS.filter((i) => !i.conditional);
    expect(computePlanPnl(withoutCond, YM).operatingProfit).toBe(25794 + 360000);
  });
});
```

- [ ] **Step 5: 테스트가 실패하는지 확인**

```bash
npx vitest run lib/plan.test.ts
```
Expected: FAIL — `isOneTime`·`upcomingOneTime`이 `./plan`에 없다는 import 에러.

- [ ] **Step 6: `lib/plan.ts` 구현**

`activeItems`를 교체하고 함수 2개를 추가한다. `computePlanPnl`·`groupTotal`·`remainingMonths`는 **그대로 둔다**(`computePlanPnl`은 `activeItems`를 쓰므로 새 규칙을 자동으로 탄다).

```ts
// 기준월에 살아 있는 항목만 — 시작월 <= 기준월 <= 종료월.
// null은 무한대로 취급한다(시작월 null = 언제부터든, 종료월 null = 무기한).
// "YYYY-MM" 문자열은 사전순 비교 = 시간순 비교라 그대로 비교한다.
export function activeItems(items: PlanItem[], yearMonth: string): PlanItem[] {
  return items.filter(
    (i) =>
      (i.startYearMonth == null || i.startYearMonth <= yearMonth) &&
      (i.endYearMonth == null || i.endYearMonth >= yearMonth)
  );
}

// 1회성 = 시작월과 종료월이 같은 달로 못박힌 항목.
// 별도 플래그를 두지 않고 파생한다 — 판정 규칙을 한 줄로 유지하기 위해서다.
export function isOneTime(i: PlanItem): boolean {
  return i.startYearMonth != null && i.startYearMonth === i.endYearMonth;
}

// 기준월 "이후"의 1회성 항목 — 「다가오는 1회성 지출」 카드용. 월 오름차순.
// 당월 1회성은 이미 그 달 계획에 들어 있으므로 "다가오는"이 아니다.
export function upcomingOneTime(
  items: PlanItem[],
  yearMonth: string
): PlanItem[] {
  return items
    .filter((i) => isOneTime(i) && i.startYearMonth! > yearMonth)
    .sort((a, b) => a.startYearMonth!.localeCompare(b.startYearMonth!));
}
```

- [ ] **Step 7: 테스트 통과 확인**

```bash
npx vitest run lib/plan.test.ts
```
Expected: PASS.

골든 테스트(25,794 / −974,206)가 실패하면 **`lib/plan.ts`가 아니라 `SEED_PLAN_ITEMS`를 의심하라.** 27개 항목 중 하나의 금액이나 `pnlClass`가 바뀌었거나, 산후조리비의 `startYearMonth`/`endYearMonth`가 `2026-08`이 아닐 것이다.

- [ ] **Step 8: 타입체크 · 린트 · 전체 테스트**

```bash
npx tsc --noEmit
npx eslint lib/plan.ts lib/plan.test.ts lib/seed.ts lib/types.ts
npm test
```
Expected: 위 4파일 린트 에러 0. `npx tsc --noEmit`은 `lib/repo.ts`에서 `startYearMonth` 누락 에러를 낼 수 있다 — **그건 Task 2에서 고친다.** 그 외 에러가 있으면 고친다.

- [ ] **Step 9: 커밋**

```bash
git add lib/types.ts lib/seed.ts lib/plan.ts lib/plan.test.ts
git commit -m "$(cat <<'EOF'
feat(pnl): 계획 항목에 시작월 추가 — 1회성 지출 모델

판정 규칙을 "시작월 <= 기준월 <= 종료월" 하나로 통일한다.
1회성은 start === end로 파생한다(별도 플래그·분기 없음).
시드에 어머니 산후조리비 1,000,000(2026-08 1회성)을 추가하고,
예산 규칙에서 "엄마 1,000,000 첫만남 이용권 충당" 줄을 지운다.

골든: 2026-07/09 → 25,794 (기준선), 2026-08 → -974,206

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: 저장 계층 (마이그레이션 · repo 매퍼)

**Files:**
- Create: `supabase/migrations/0019_plan_item_start_month.sql`
- Modify: `lib/repo.ts`

**Interfaces:**
- Consumes: Task 1의 `PlanItem.startYearMonth`
- Produces: `plan_items.start_year_month` 컬럼, `toPlanItem`/`fromPlanItem` 왕복

---

- [ ] **Step 1: 마이그레이션 SQL 작성**

`supabase/migrations/0019_plan_item_start_month.sql` — **순수 ASCII.**

```sql
-- 0019: plan_items.start_year_month — one-time / bounded-start plan items
-- An item is active when: start_year_month <= month <= end_year_month
-- (null = unbounded). A one-time item has start == end.
-- Additive only: existing rows get null and keep their current behaviour.

alter table plan_items add column if not exists start_year_month text;
```

이 Task에서 **실행하지 않는다.** 파일만 만든다. 실제 DDL 적용은 Task 5에서 재헌님 승인 후.

- [ ] **Step 2: `lib/repo.ts` — 매퍼에 컬럼 추가**

`toPlanItem`의 `endYearMonth` **바로 위**에 추가:
```ts
  startYearMonth: (r.start_year_month as string) ?? null,
```

`fromPlanItem`의 `end_year_month` **바로 위**에 추가:
```ts
  start_year_month: x.startYearMonth,
```

- [ ] **Step 3: 타입체크 · 린트 · 전체 테스트**

```bash
npx tsc --noEmit
npx eslint lib/repo.ts
npm test
```
Expected: 에러 0, 테스트 전부 PASS. `DataSnapshot`·`PlanItem` 타입이 매퍼를 강제하므로 빠뜨린 곳은 tsc가 잡는다.

- [ ] **Step 4: 커밋**

```bash
git add supabase/migrations/0019_plan_item_start_month.sql lib/repo.ts
git commit -m "$(cat <<'EOF'
feat(pnl): plan_items.start_year_month 컬럼 + 매퍼

순수 추가(additive) 마이그레이션 — 기존 행은 null이라 동작 그대로.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: 편집 시트 「적용」 세그먼트 + 리스트 배지

사용자에게 "시작월/종료월"이라는 날것을 노출하지 않는다. 「적용」 세그먼트로 **의도**를 고르게 한다.

**Files:**
- Modify: `components/pnl/PlanItemForm.tsx`
- Modify: `components/pnl/PlanItemList.tsx`

**Interfaces:**
- Consumes: Task 1의 `isOneTime`, `lib/format.ts`의 `shiftMonth(ym, delta)`
- Produces:
  - `PlanItemForm`에 새 prop **`draftOneTimeMonth: string | null`** — 신규 항목을 [이 달만] 모드로 열고 그 달을 기본값으로 채운다. `null`이면 [매달] 모드.
  - Task 4의 `Plan.tsx`가 이 prop을 넘긴다.

---

- [ ] **Step 1: `components/pnl/PlanItemForm.tsx` — 「적용」 세그먼트 추가**

**(a) import 수정** — `isOneTime`을 가져온다.

```tsx
import { isOneTime } from "@/lib/plan";
```

**(b) props에 `draftOneTimeMonth` 추가**

```tsx
export default function PlanItemForm({
  open,
  initial,
  draftGroup,
  draftConditional,
  draftOneTimeMonth,
  onClose,
}: {
  open: boolean;
  initial: PlanItem | null;
  draftGroup: PlanGroup;
  draftConditional: boolean;
  draftOneTimeMonth: string | null; // 신규를 [이 달만]으로 열 때의 기본 달
  onClose: () => void;
}) {
```

**(c) 적용 모드 state 추가** — `endYearMonth` state 선언 **바로 아래**에 넣는다.

```tsx
  // 적용 방식 — [매달] / [이 달만]. 편집이면 기존 항목에서 판정한다.
  const [applyMode, setApplyMode] = useState<"monthly" | "once">(
    initial
      ? isOneTime(initial)
        ? "once"
        : "monthly"
      : draftOneTimeMonth
        ? "once"
        : "monthly"
  );
  // [이 달만]일 때의 대상 달 (시작월 = 종료월로 저장된다)
  const [onceMonth, setOnceMonth] = useState(
    initial && isOneTime(initial)
      ? (initial.startYearMonth ?? "")
      : (draftOneTimeMonth ?? "")
  );
```

**(d) `canSave` 교체** — [이 달만]인데 달을 안 골랐으면 저장 불가.

```tsx
  const canSave =
    name.trim().length > 0 &&
    Number(amount) > 0 &&
    (applyMode === "monthly" || onceMonth !== "");
```

**(e) `submit`의 저장 페이로드 교체** — `endYearMonth` 줄을 아래 두 줄로 바꾼다.

```tsx
      startYearMonth: applyMode === "once" ? onceMonth : null,
      endYearMonth: applyMode === "once" ? onceMonth : endYearMonth || null,
```

**(f) JSX** — 기존 `<Field label="언제까지 (비우면 무기한)">` 블록을 통째로 아래로 교체한다.

```tsx
      <Field label="적용">
        <div className="flex gap-1 rounded-xl bg-cream p-1">
          {(
            [
              ["monthly", "매달"],
              ["once", "이 달만"],
            ] as const
          ).map(([k, label]) => (
            <button
              key={k}
              type="button"
              onClick={() => setApplyMode(k)}
              className={`flex-1 rounded-lg py-2 text-sm font-medium transition ${
                applyMode === k ? "bg-leaf text-white" : "text-stone"
              }`}
            >
              {label}
            </button>
          ))}
        </div>
      </Field>

      {applyMode === "monthly" ? (
        <Field label="언제까지 (비우면 무기한)">
          <input
            className={inputCls}
            type="month"
            value={endYearMonth}
            onChange={(e) => setEndYearMonth(e.target.value)}
          />
        </Field>
      ) : (
        <Field label="어느 달">
          <input
            className={inputCls}
            type="month"
            value={onceMonth}
            onChange={(e) => setOnceMonth(e.target.value)}
          />
        </Field>
      )}
```

> ⚠️ 세그먼트를 바꿔도 `group`·`pnlClass`를 건드리지 않는다 — 독립 축 원칙. 그리고 `useEffect`로 상태를 리셋하지 않는다(ESLint 룰이 막는다). 모드를 오갈 때 각 입력값이 남아 있는 건 의도된 동작이며, 저장 시 `applyMode`가 무엇을 쓸지 결정한다.

- [ ] **Step 2: `components/pnl/PlanItemList.tsx` — `1회성` 배지**

**(a) import에 `isOneTime` 추가**

```tsx
import { groupTotal, remainingMonths, isOneTime } from "@/lib/plan";
```

**(b) `ItemRow`의 Pill 렌더 교체** — 기존 `{left != null && (<Pill …>{left}회 남음</Pill>)}` 줄을 아래로 바꾼다.

```tsx
      {isOneTime(item) ? (
        <Pill tone="sky">1회성</Pill>
      ) : (
        left != null && (
          <Pill tone={left <= 12 ? "gold" : "stone"}>{left}회 남음</Pill>
        )
      )}
```

> 왜 필요한가: 1회성 항목은 `remainingMonths`가 `1`을 반환해 `1회 남음`이 뜬다. 할부 `8회 남음`과 같은 계열로 읽혀 "이번이 마지막 할부인가?"로 오독된다. 성격이 다르므로 배지를 나눈다.

- [ ] **Step 3: 타입체크 · 린트 · 빌드**

```bash
npx tsc --noEmit
npx eslint components/pnl/PlanItemForm.tsx components/pnl/PlanItemList.tsx
npm run build
```
Expected: 에러 0.

`Plan.tsx`가 아직 `draftOneTimeMonth`를 안 넘겨서 tsc가 **`Plan.tsx`에서** 에러를 낸다 — **Task 4에서 고친다.** 임시로 통과시키려고 prop을 optional로 만들지 말 것. Task 4가 채운다.

> tsc가 `Plan.tsx` 에러만 내는지 확인하고, 그 외 에러가 있으면 고친다.

- [ ] **Step 4: 커밋**

```bash
git add components/pnl/PlanItemForm.tsx components/pnl/PlanItemList.tsx
git commit -m "$(cat <<'EOF'
feat(pnl): 편집 시트 「적용」 세그먼트 + 1회성 배지

[매달]/[이 달만] 세그먼트로 의도를 고르게 하고, 시작월/종료월은
내부에서 채운다. 1회성 항목은 "1회 남음"(할부와 혼동) 대신 "1회성" 배지.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: 「다가오는 1회성 지출」 카드 + Plan.tsx 배선

이 기능이 실제로 쓸모 있어지는 지점. 미래에 꽂아둔 1회성을 **현재 달 화면에서 볼 수 있게** 하고, 여기가 1회성 추가의 진입점이 된다.

**Files:**
- Create: `components/pnl/UpcomingOneTime.tsx`
- Modify: `components/pnl/Plan.tsx`

**Interfaces:**
- Consumes: Task 1의 `upcomingOneTime`, Task 3의 `PlanItemForm.draftOneTimeMonth`, `lib/format.ts`의 `won`·`shiftMonth`
- Produces: `<UpcomingOneTime items={PlanItem[]} month={string} onEdit={(i: PlanItem) => void} onAdd={() => void} />`

---

- [ ] **Step 1: `components/pnl/UpcomingOneTime.tsx` 작성**

```tsx
"use client";

import { won } from "@/lib/format";
import { upcomingOneTime } from "@/lib/plan";
import { Card, SectionTitle } from "@/components/budget/ui";
import type { PlanItem } from "@/lib/types";

// "2026-08" → "8월"
const monthLabel = (ym: string) => `${Number(ym.slice(5, 7))}월`;

export default function UpcomingOneTime({
  items,
  month,
  onEdit,
  onAdd,
}: {
  items: PlanItem[];
  month: string;
  onEdit: (i: PlanItem) => void;
  onAdd: () => void;
}) {
  const upcoming = upcomingOneTime(items, month);
  const total = upcoming.reduce((sum, i) => sum + i.amount, 0);

  return (
    <>
      <SectionTitle
        right={
          upcoming.length > 0 ? (
            <span className="text-sm font-bold tabular-nums text-ink">
              {won(total)}
            </span>
          ) : undefined
        }
      >
        다가오는 1회성 지출
      </SectionTitle>
      <Card>
        {upcoming.length > 0 ? (
          <>
            <p className="mb-1 text-[11px] text-stone">
              이번 달 잔액에는 안 잡힌다. 그 달로 넘어가야 반영된다.
            </p>
            <div className="divide-y divide-line">
              {upcoming.map((i) => (
                <button
                  key={i.id}
                  onClick={() => onEdit(i)}
                  className="flex w-full items-center gap-2 py-2 text-left"
                >
                  <span className="w-10 shrink-0 text-sm font-bold text-leaf-dark">
                    {monthLabel(i.startYearMonth!)}
                  </span>
                  <span className="min-w-0 flex-1 truncate text-sm text-ink">
                    {i.name}
                  </span>
                  <span className="w-24 shrink-0 text-right text-sm tabular-nums text-ink">
                    {won(i.amount)}
                  </span>
                </button>
              ))}
            </div>
          </>
        ) : (
          <p className="py-1 text-[11px] text-stone">
            앞으로 예정된 1회성 지출이 없다. 큰 지출을 미리 꽂아두면 그 달에만
            반영된다.
          </p>
        )}
        <button
          onClick={onAdd}
          className="mt-2 w-full rounded-xl border border-dashed border-line py-2 text-sm text-stone"
        >
          + 1회성 지출 추가
        </button>
      </Card>
    </>
  );
}
```

> **비어 있어도 카드를 숨기지 않는다.** `+ 1회성 지출 추가` 버튼이 이 기능의 유일한 진입점이므로 항상 보여야 한다.
>
> `items`는 **기준월로 거르지 않은 전체 `planItems`**를 받는다. 다가오는 1회성은 정의상 **현재 달에 살아 있지 않으므로**, `activeItems`로 거른 목록을 넘기면 항상 빈 배열이 된다. Task 4 Step 2에서 이 점을 지킨다.

- [ ] **Step 2: `components/pnl/Plan.tsx` 배선**

파일을 아래로 **교체**한다.

```tsx
"use client";

import { useMemo, useState } from "react";
import { useData } from "@/lib/data-context";
import { activeItems, computePlanPnl } from "@/lib/plan";
import { buildWaterfall } from "@/lib/pnl";
import { currentYearMonth, shiftMonth } from "@/lib/format";
import { Card, SectionTitle, MonthSwitcher } from "@/components/budget/ui";
import WaterfallChart from "./WaterfallChart";
import PlanSummary from "./PlanSummary";
import PlanItemList from "./PlanItemList";
import PlanItemForm from "./PlanItemForm";
import PlanVsActual from "./PlanVsActual";
import UpcomingOneTime from "./UpcomingOneTime";
import BudgetRules from "./BudgetRules";
import type { PlanGroup, PlanItem } from "@/lib/types";

export default function Plan() {
  const { planItems } = useData();
  const [month, setMonth] = useState(currentYearMonth());

  // 시트 상태 — editing이 있으면 수정, 없으면 draft로 신규
  const [sheetOpen, setSheetOpen] = useState(false);
  const [editing, setEditing] = useState<PlanItem | null>(null);
  const [draftGroup, setDraftGroup] = useState<PlanGroup>("spending");
  const [draftConditional, setDraftConditional] = useState(false);
  const [draftOneTimeMonth, setDraftOneTimeMonth] = useState<string | null>(null);

  // 계획 탭은 언제나 "선택한 달 기준의 계획"을 보여준다 — 시작 전·종료된 항목은
  // 이 달의 계획에서 제외하고, 히어로·리스트·대조표가 같은 목록을 공유한다.
  const active = useMemo(
    () => activeItems(planItems, month),
    [planItems, month]
  );
  const summary = useMemo(() => computePlanPnl(active, month), [active, month]);
  const segments = useMemo(() => buildWaterfall(summary), [summary]);

  const openEdit = (item: PlanItem) => {
    setEditing(item);
    setSheetOpen(true);
  };
  const openAdd = (group: PlanGroup, conditional: boolean) => {
    setEditing(null);
    setDraftGroup(group);
    setDraftConditional(conditional);
    setDraftOneTimeMonth(null);
    setSheetOpen(true);
  };
  // 「다가오는 1회성 지출」의 진입점 — [이 달만] 모드로 열고 기본 달은 다음 달
  const openAddOneTime = () => {
    setEditing(null);
    setDraftGroup("spending");
    setDraftConditional(false);
    setDraftOneTimeMonth(shiftMonth(month, 1));
    setSheetOpen(true);
  };

  return (
    <div className="space-y-4">
      <MonthSwitcher ym={month} onChange={setMonth} />

      <PlanSummary items={active} />

      <SectionTitle>계획 손익</SectionTitle>
      <Card>
        <WaterfallChart segments={segments} />
      </Card>

      <SectionTitle>계획 vs 실적</SectionTitle>
      <PlanVsActual month={month} />

      {/* 전체 planItems를 넘긴다 — 다가오는 1회성은 이 달에 살아 있지 않다 */}
      <UpcomingOneTime
        items={planItems}
        month={month}
        onEdit={openEdit}
        onAdd={openAddOneTime}
      />

      <PlanItemList
        items={active}
        month={month}
        onEdit={openEdit}
        onAdd={openAdd}
      />

      <div className="pt-2">
        <BudgetRules />
      </div>

      {sheetOpen && (
        <PlanItemForm
          open
          initial={editing}
          draftGroup={draftGroup}
          draftConditional={draftConditional}
          draftOneTimeMonth={draftOneTimeMonth}
          onClose={() => setSheetOpen(false)}
        />
      )}
    </div>
  );
}
```

- [ ] **Step 3: 타입체크 · 린트 · 빌드 · 전체 테스트**

```bash
npx tsc --noEmit
npx eslint components/pnl/
npm run build
npm test
```
Expected: 에러 0, 테스트 전부 PASS.

- [ ] **Step 4: 브라우저 육안 검증 (localStorage 모드)**

⚠️ 클라우드 모드는 `start_year_month` 컬럼이 아직 DB에 없다(DDL은 Task 5). **localStorage 모드로 검증한다.**

```
.env.local → .env.local.bak 로 임시 rename
npm run dev
```

⚠️ **반드시 `.env.local`로 복원할 것.** 복원을 잊으면 재헌님 앱이 클라우드 동기화를 잃는다. `data/` 등 사용자 실데이터는 절대 삭제하지 않는다. dev 서버는 단일 인스턴스만 뜬다.

`http://localhost:3000/pnl` → 「계획」 탭:

1. 기본 월(2026-07): 히어로 잔액 **+25,794**, 「월 지출」 소계 **2,760,000** (산후조리비 미포함)
2. 「다가오는 1회성 지출」 카드에 `8월 · 어머니 산후조리비 1,000,000`, 우측 합계 **1,000,000**
3. 월 스위처 `›` → **2026-08**: 히어로 잔액 **−974,206**(코랄), 「월 지출」 소계 **3,760,000**, 항목 리스트에 `어머니 산후조리비` + **`1회성` 배지**. 「다가오는 1회성 지출」은 **비어 있음**(당월은 '다가오는'이 아니다)
4. `›` → **2026-09**: 잔액 **25,794** 복귀, 산후조리비 사라짐
5. `‹`로 8월로 되돌리면 다시 나타남 (지난 1회성을 자동 삭제하지 않는다)
6. 「다가오는 1회성 지출」의 `+ 1회성 지출 추가` → 시트가 **[이 달만] 선택 상태**로 열리고 "어느 달"에 **다음 달**이 채워져 있음. 이름 "테스트"·금액 300000 저장 → 카드에 나타남. 다시 탭해 **2단계 삭제**로 제거
7. 「월 지출」 섹션의 `+ 항목 추가` → 시트가 **[매달]** 선택 상태로 열리고 "언제까지" 칸이 보임 (회귀 없음)
8. 기존 항목 `자동차 할부` 편집 → **[매달]** 상태, "언제까지"에 `2027-03`. 배지는 `8회 남음`(1회성 아님)
9. 브라우저 콘솔 에러 없음

환경 문제로 검증이 불가능하면 우회하지 말고 보고서에 **"미실시(사유)"**로 정직하게 적는다.

- [ ] **Step 5: 커밋**

```bash
git add components/pnl/UpcomingOneTime.tsx components/pnl/Plan.tsx
git commit -m "$(cat <<'EOF'
feat(pnl): 「다가오는 1회성 지출」 카드

미래에 꽂아둔 1회성을 현재 달 화면에서 미리 본다. 이번 달 잔액에는
안 잡히고 그 달로 넘어가야 반영된다는 점을 카드에 명시했다.
+ 1회성 지출 추가 버튼이 이 기능의 진입점 — [이 달만] 모드로 열린다.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: DDL 적용 · 배포 · 프로덕션 검증

**Files:** 코드 변경 없음

---

- [ ] **Step 1: 재헌님께 DDL 적용 승인 요청**

프로덕션 DDL은 자동으로 실행하지 않는다. **재헌님이 명시적으로 "DDL을 적용하라"고 말해야 진행한다.** "푸시"나 "배포" 같은 말은 DDL 승인이 아니다(지난번에 이 구분으로 자동 승인 분류기가 차단했다).

알릴 내용:
> `plan_items`에 `start_year_month` 컬럼 하나를 추가합니다(개인 Supabase `jyfjnxvsblfkkbscddnw`).
> - 순수 추가(additive) — 기존 행·데이터를 건드리지 않습니다. `drop`·기존 컬럼 `alter` 없음
> - 기존 행은 전부 `null`이 되어 지금 동작이 그대로 유지됩니다
> - 되돌리려면 `alter table plan_items drop column start_year_month` 하나면 됩니다
> - 크레덴셜(`~/.claude.json`의 개인 토큰)을 읽어야 해서 샌드박스 해제가 필요합니다

- [ ] **Step 2: 배포 순서 — DDL 먼저, 그다음 push**

**순서를 지킨다: DDL 적용(Step 3) → 검증(Step 4) → 병합·배포(Step 5).**

> ⚠️ **반대로 하면 프로덕션이 조용히 깨진다.** 컬럼이 없는 상태에서 새 코드가 배포되면 `fromPlanItem`이 `start_year_month`를 보내고 PostgREST가 이를 거부한다. 그런데 `sbUpsert`는 error를 검사하지 않으므로 **UI는 저장된 것처럼 보이고 새로고침하면 사라진다.** 크래시라면 즉시 알았을 것을 조용한 유실로 바꾼다.
>
> DDL을 먼저 적용하는 것은 안전하다 — 구버전 프로덕션 코드는 `start_year_month`를 모르므로 그 컬럼이 놀고 있을 뿐이다.

- [ ] **Step 3: DDL 적용**

Supabase Management API로 `0019_plan_item_start_month.sql`을 실행한다.

- 토큰: `~/.claude.json`의 `SUPABASE_ACCESS_TOKEN` (`sbp_` personal token). **토큰 값을 로그에 출력하지 않는다.**
- 엔드포인트: `POST https://api.supabase.com/v1/projects/jyfjnxvsblfkkbscddnw/database/query`
- **node https로 POST**한다(PowerShell `Invoke-RestMethod`는 이 레포에서 신뢰하지 않는 경로다).
- `dangerouslyDisableSandbox: true` 필요.

- [ ] **Step 4: 컬럼 생성 검증**

```sql
select column_name, data_type, is_nullable
from information_schema.columns
where table_name = 'plan_items' and column_name = 'start_year_month';
```
Expected: 1행 — `start_year_month | text | YES`

- [ ] **Step 5: main 병합 · 배포**

```bash
git checkout main
git merge --ff-only feat/plan-onetime
git push origin main
```
Vercel이 자동 배포한다.

- [ ] **Step 6: 프로덕션 검증**

⚠️ **PIN 입력은 재헌님이 직접 한다.** 비밀번호 입력을 대신하지 않는다. 탭만 열어두고 요청한다.

https://baechoo-family.vercel.app/pnl → 「계획」 탭.

검증할 것:
1. **시드 부트스트랩**: `plan_items`가 비어 있었다면 앱 최초 로드 시 **28개 항목**이 들어간다.
   ```sql
   select count(*) from plan_items;
   ```
   Expected: `28`

   > ⚠️ 이미 27개가 들어간 상태라면 부트스트랩이 **안 돈다**(`length === 0`일 때만 insert). 그 경우 산후조리비 1건이 빠져 있다. 확인 쿼리:
   > ```sql
   > select id, name, start_year_month, end_year_month from plan_items where id = 'plan-once-postpartum';
   > ```
   > 0행이면 재헌님께 알리고 **앱에서 직접 추가**하시게 한다(「+ 1회성 지출 추가」 → 어머니 산후조리비 / 1000000 / 2026-08). 임의로 DB에 INSERT하지 않는다.

2. **골든 값**: DB에 저장된 값으로 직접 재계산해 확인한다.
   ```sql
   select pnl_class, sum(amount)::float as total
   from plan_items
   where (start_year_month is null or start_year_month <= '2026-07')
     and (end_year_month is null or end_year_month >= '2026-07')
   group by pnl_class;
   ```
   Expected: `revenue 4400000 / fixed 2299206 / saving 775000 / variable 1300000` → 운영이익 **25,794**

   같은 쿼리를 `'2026-08'`로 바꾸면 `variable 2300000` → 운영이익 **−974,206**

3. **한글 인코딩**: `select name from plan_items where id = 'plan-once-postpartum'` → `어머니 산후조리비` (깨짐 없음)

4. **쓰기 경로 (가장 중요)**: 재헌님이 항목 하나를 수정 → **새로고침** → 값이 유지되는지. 유지되지 않으면 컬럼/코드 불일치로 조용한 유실이 일어나고 있는 것이다.

5. 브라우저 콘솔 에러 없음

- [ ] **Step 7: 정본 문서 갱신**

`docs/가족-pnl/회계모델-규칙서.md`의 「예산 계획 (계획 탭)」 절에 아래를 덧붙이고 커밋한다.

```markdown
### 1회성 지출

항목은 **시작월 ≤ 기준월 ≤ 종료월**일 때 그 달의 계획에 잡힌다(null = 무한대).

- **매달** — 시작월·종료월 없음 (월세·식비)
- **언젠가 끝남** — 종료월만 (자동차 할부: 2027-03까지)
- **1회성** — 시작월 = 종료월 (어머니 산후조리비: 2026-08에만)

미래에 꽂아둔 1회성은 「다가오는 1회성 지출」 카드에 보이지만
**그 달로 넘어가야 잔액에 반영된다.** 지난 1회성은 자동으로 지우지 않는다 —
과거 계획 vs 실적 기록이 보존된다.

2026-08 계획 운영이익 = −974,206 (산후조리비 1,000,000 반영).
그 외 달은 기준선 **25,794**.
```

```bash
git add docs/가족-pnl/회계모델-규칙서.md
git commit -m "$(cat <<'EOF'
docs(pnl): 회계모델 규칙서에 1회성 지출 절 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
git push
```

---

## 완료 기준

- [ ] `npm test` 전체 PASS (`lib/plan.test.ts` 골든: 2026-07/09 → 25,794, 2026-08 → −974,206)
- [ ] `npm run build` 성공, 새로 만진 파일의 eslint 에러 0
- [ ] 프로덕션 「계획」 탭 2026-07에서 잔액 **+25,794**, 「다가오는 1회성 지출」에 `8월 · 어머니 산후조리비 1,000,000`
- [ ] 2026-08로 넘기면 잔액 **−974,206**, 리스트에 `1회성` 배지
- [ ] `+ 1회성 지출 추가`로 만든 항목이 저장되고 새로고침 후 유지
- [ ] 예산 규칙이 3줄 (엄마 1,000,000 줄 삭제됨)
- [ ] 기존 대시보드·설명서 서브탭, 가계부·52주투두·배추·우주 탭 회귀 없음
