# 예산 목록 순서 지정 + 계정과목 2단 선택 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 예산 과목의 표시 순서를 사람이 정하고 그 순서가 대시보드까지 그대로 반영되게 하며, 33개짜리 계정과목 드롭다운을 카테고리 → 과목 2단으로 쪼갠다.

**Architecture:** 예산 행에 `sort_order` 하나를 두고 과목 순서를 사람이 정한다. 카테고리 순서는 소속 과목의 최소 순서로 파생하므로 별도 저장소가 없다. `BurnRow`에 `order`를 실어 보내 `groupBurnRows` 시그니처를 유지한 채 정렬을 바꾼다. 순서 이동은 순수함수(`lib/budgetOrder.ts`)가 새 순서를 계산하고 컴포넌트는 저장만 한다.

**Tech Stack:** Next.js 16 (App Router) · TypeScript · Tailwind v4 · Supabase (PostgREST) · vitest

**정본 스펙:** `docs/superpowers/specs/2026-08-02-예산-순서·2단선택-design.md`
**선행 작업:** `2026-08-02-예산-버전-design.md`(예산 버전, main `8f201f7`에 머지됨)

## Global Constraints

- **UI 문구·주석은 한국어.** 이모지는 절제한다. **화면에 정의·산식을 설명하는 안내 문구를 넣지 않는다** — 맥락은 코드 주석으로. (직전 작업에서 이것 때문에 수정이 한 번 있었다.)
- **신규 npm 의존성 0.**
- 이 저장소의 Next.js는 학습 데이터와 다르다. React 컴포넌트를 쓰기 전에 `node_modules/next/dist/docs/`의 해당 가이드를 읽는다 (`AGENTS.md`).
- **`budgets.id`는 uuid 타입**이다. 새 예산 행 id는 빈 문자열로 넘겨 `repo.newId()`가 채우게 한다(기존 관례).
- **금융 테이블(`transactions`·`categories`·`budgets`·`recurring_expenses`·`budget_versions`)에는 `deleted_at`이 없다.** `.is("deleted_at", null)` 필터를 걸면 PostgREST가 `42703`으로 400을 낸다.
- **테스트 픽스처에 실금액을 쓰지 않는다.** 300,000·700,000처럼 둥근 수가 하필 실제 가족 예산과 일치해 세 번 반복된 실수다. 12345 / 23456 / 34567 계열을 쓰고 `reports/review-*.json`과 대조한다.
- **`saveBudget`은 실패 시 예외를 던진다**(`sbUpsertOrThrow`). 저장 결과를 낙관적으로 먼저 그리지 않는다.
- **DDL을 배포보다 먼저 적용한다.** 0024 때 push가 앞서 조용한 실패 창이 열린 전례가 있다.
- 커밋 메시지는 한국어. 끝에 `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.
- 앱 루트는 `C:\Users\MGRV\.claude\baechoo-family`, 브랜치는 `feat/budget-ordering`. 검증은 `npm run test`(vitest) · `npx tsc --noEmit -p tsconfig.json` · `npm run build` · `npm run lint`. **`npm run build`와 vitest 둘 다 테스트 파일의 타입 에러를 못 잡으므로 `tsc --noEmit`이 필수다.** 린트에는 이 작업과 무관한 선재 문제 6건(`react-hooks/set-state-in-effect` 5건 + 미사용 변수 1건)이 있다 — 신규만 없으면 된다.

---

### Task 1: 마이그레이션 0026 + `Budget.sortOrder` 데이터 레이어

**Files:**
- Create: `supabase/migrations/0026_budget_sort_order.sql`
- Modify: `lib/types.ts` (`Budget` 인터페이스)
- Modify: `lib/repo.ts` (`toBudget`·`fromBudget`)

**Interfaces:**
- Consumes: 없음 (이 태스크가 처음)
- Produces:
  - `Budget.sortOrder: number | null` — 그 버전 안에서 사람이 정한 표시 순서. 없으면 `null`
  - DB 컬럼 `budgets.sort_order integer`

- [ ] **Step 1: 마이그레이션 파일 작성**

`supabase/migrations/0026_budget_sort_order.sql`:

```sql
-- 0026: budgets.sort_order — 예산 과목의 표시 순서를 사람이 정한다.
-- 버전별이다(예산 행에 붙으므로). 카테고리 순서는 소속 과목의 최소 sort_order로
-- 파생하므로 별도 컬럼을 두지 않는다.

alter table budgets add column if not exists sort_order integer;

-- 기존 행 백필 — 지금 화면에 보이는 순서(카테고리명 → 과목명) 그대로 0부터.
-- group_name이 null인 과목("미분류")은 뒤로 보낸다(화면과 같은 규칙).
with ordered as (
  select
    b.id,
    row_number() over (
      partition by b.version_id
      order by (c.group_name is null), c.group_name, c.name
    ) - 1 as rn
  from budgets b
  join categories c on c.id = b.category_id
  where b.category_id is not null
)
update budgets
set sort_order = ordered.rn
from ordered
where budgets.id = ordered.id
  and budgets.sort_order is null;
```

`categoryId`가 null인 구버전 "전체 월예산" 행은 백필 대상이 아니다 — 목록에 과목으로 뜨지 않는다.

- [ ] **Step 2: 작업 전 백업**

`db-backups/backup-before-0026.mjs`를 만든다(`db-backups/backup-before-0025.mjs`와 같은 방식):

```js
// 0026 적용 전 스냅샷. db-backups/는 gitignore 대상이다(가족 금융 데이터 — 원격 금지).
import fs from 'fs';

const env = Object.fromEntries(
  fs.readFileSync('C:/Users/MGRV/.claude/baechoo-family/.env.local', 'utf8')
    .split(/\r?\n/).filter((l) => l.includes('='))
    .map((l) => { const i = l.indexOf('='); return [l.slice(0, i).trim(), l.slice(i + 1).trim()]; })
);
const url = env.NEXT_PUBLIC_SUPABASE_URL, key = env.NEXT_PUBLIC_SUPABASE_ANON_KEY;
const H = { apikey: key, Authorization: `Bearer ${key}` };

const out = {};
for (const t of ['budgets', 'budget_versions', 'categories']) {
  const res = await fetch(`${url}/rest/v1/${t}?select=*`, { headers: H });
  if (!res.ok) throw new Error(`${t}: ${res.status} ${await res.text()}`);
  out[t] = await res.json();
  console.log(t, out[t].length);
}
fs.writeFileSync(
  'C:/Users/MGRV/.claude/baechoo-family/db-backups/baechoo-backup-before-0026.json',
  JSON.stringify(out, null, 2)
);
```

Run: `node db-backups/backup-before-0026.mjs`
Expected: `budgets 22` / `budget_versions 1` / `categories 34`. 다르면 멈추고 보고한다.

- [ ] **Step 3: 타입 추가**

`lib/types.ts`의 `Budget` 인터페이스에 한 줄 추가:

```ts
export interface Budget {
  id: string;
  yearMonth: string | null; // (구) 이번 달 조정. 버전 도입으로 미사용 — 컬럼·값만 보존
  categoryId: string | null; // null = 전체 예산(구버전)
  amount: number;
  versionId: string | null; // 어느 예산 버전에 속하는가
  sortOrder: number | null; // 그 버전 안에서 사람이 정한 표시 순서. 없으면 자동 정렬 뒤로
}
```

- [ ] **Step 4: repo 매퍼 갱신**

`lib/repo.ts`의 `toBudget`·`fromBudget`:

```ts
const toBudget = (r: Record<string, unknown>): Budget => ({
  id: r.id as string,
  yearMonth: (r.year_month as string) ?? null,
  categoryId: (r.category_id as string) ?? null,
  amount: Number(r.amount),
  versionId: (r.version_id as string) ?? null,
  sortOrder: r.sort_order === null || r.sort_order === undefined ? null : Number(r.sort_order),
});
const fromBudget = (x: Budget) => ({
  id: x.id,
  year_month: x.yearMonth,
  category_id: x.categoryId,
  amount: x.amount,
  version_id: x.versionId,
  sort_order: x.sortOrder,
});
```

⚠️ `Number(r.sort_order)`를 그냥 쓰면 `null`이 `0`이 된다 — 순서 0번과 "순서 없음"이 뒤섞이므로 위처럼 명시적으로 갈라야 한다.

`lib/repo.ts`의 `normalizeBudgetVersions`(localStorage 승격)는 `versionId`만 채우므로 손대지 않는다. 구버전 localStorage 예산은 `sortOrder`가 `undefined`가 되는데, 이후 코드가 `?? null`로 읽으므로 문제되지 않는다.

- [ ] **Step 5: 타입 체크**

Run:
```bash
npx tsc --noEmit -p tsconfig.json
```
Expected: `Budget` 리터럴을 만드는 곳에서 `sortOrder` 누락 에러가 뜬다. 뜨는 곳을 전부 `sortOrder: null`로 채운다(테스트 픽스처 포함). 다시 돌려 에러 0을 확인한다.

- [ ] **Step 6: 나머지 검증**

Run:
```bash
npm run test
npm run build
npm run lint
```
Expected: 전부 PASS. 린트는 선재 6건만.

- [ ] **Step 7: 커밋**

```bash
git add supabase/migrations/0026_budget_sort_order.sql lib/types.ts lib/repo.ts
git commit -m "$(cat <<'EOF'
feat(budget): 0026 budgets.sort_order — 과목 표시 순서 컬럼·매퍼

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

`db-backups/`는 gitignore 대상이라 백업 스크립트·JSON은 커밋에 들어가지 않는다. `git show --stat`으로 확인한다.

---

### Task 2: 순서 반영 — `BurnRow.order` + 정렬 규칙 (TDD)

**Files:**
- Modify: `lib/compute.ts` (`BurnRow`·`budgetBurndown`·`groupBurnRows` + `groupBudgetsByCategory` 신설)
- Test: `lib/compute.test.ts`

**Interfaces:**
- Consumes: `Budget.sortOrder`(Task 1), 기존 `budgetsOfMonth(budgets, versions, ym)`
- Produces:
  - `BurnRow.order: number | null`
  - `export interface BudgetGroup { name: string; rows: Budget[] }`
  - `export function groupBudgetsByCategory(rows: Budget[], categoryById: (id: string) => Category | undefined): BudgetGroup[]`
  - `budgetBurndown`·`groupBurnRows` 시그니처는 **변경 없음**

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`lib/compute.test.ts` 맨 아래에 추가. import 줄에 `groupBudgetsByCategory`를 넣는다.

```ts
describe("순서 지정 반영", () => {
  const V: BudgetVersion[] = [
    { id: "bv-o", name: "v", startMonth: "2026-01", memo: null, createdAt: "" },
  ];
  const cat = (id: string, name: string, groupName: string | null): Category => ({
    id,
    name,
    type: "expense",
    groupName,
    costType: "variable",
    color: "",
  });
  const cats = [
    cat("c-a", "가", "알파"),
    cat("c-b", "나", "알파"),
    cat("c-c", "다", "베타"),
    cat("c-d", "라", null), // 미분류
    cat("c-e", "마", "감마"), // 예산 행 없음
  ];
  const bud = (id: string, categoryId: string, amount: number, sortOrder: number | null): Budget => ({
    id,
    yearMonth: null,
    categoryId,
    amount,
    versionId: "bv-o",
    sortOrder,
  });
  // 베타(다)가 알파(가·나)보다 앞. 미분류(라)는 순서가 있어도 그룹은 맨 뒤.
  const budgets = [
    bud("b1", "c-c", 12345, 0),
    bud("b2", "c-a", 23456, 1),
    bud("b3", "c-b", 34567, 2),
    bud("b4", "c-d", 45678, 3),
  ];
  const txns: Transaction[] = [];

  it("budgetBurndown이 예산 행의 sortOrder를 order로 싣는다", () => {
    const r = budgetBurndown(budgets, V, cats, txns, "2026-02");
    const byId = new Map(r.rows.map((x) => [x.category.id, x.order]));
    expect(byId.get("c-c")).toBe(0);
    expect(byId.get("c-a")).toBe(1);
  });

  it("예산 행이 없는 과목의 order는 null이다", () => {
    const r = budgetBurndown(budgets, V, cats, txns, "2026-02");
    expect(r.rows.find((x) => x.category.id === "c-e")!.order).toBe(null);
  });

  it("행 정렬 — 순서가 있는 것이 앞, 없는 것은 뒤", () => {
    const r = budgetBurndown(budgets, V, cats, txns, "2026-02");
    expect(r.rows.map((x) => x.category.id)).toEqual([
      "c-c",
      "c-a",
      "c-b",
      "c-d",
      "c-e",
    ]);
  });

  it("그룹 순서 — 소속 과목의 최소 order 순, 미분류는 맨 뒤", () => {
    const g = groupBurnRows(budgetBurndown(budgets, V, cats, txns, "2026-02").rows);
    expect(g.map((x) => x.name)).toEqual(["베타", "알파", "감마", "미분류"]);
  });

  it("그룹 안 과목도 order 순", () => {
    const g = groupBurnRows(budgetBurndown(budgets, V, cats, txns, "2026-02").rows);
    const alpha = g.find((x) => x.name === "알파")!;
    expect(alpha.rows.map((r) => r.category.id)).toEqual(["c-a", "c-b"]);
  });
});

describe("groupBudgetsByCategory — 예산 탭 목록 묶음", () => {
  const cats = new Map<string, Category>([
    ["c-a", { id: "c-a", name: "가", type: "expense", groupName: "알파", costType: "variable", color: "" }],
    ["c-b", { id: "c-b", name: "나", type: "expense", groupName: "알파", costType: "variable", color: "" }],
    ["c-c", { id: "c-c", name: "다", type: "expense", groupName: "베타", costType: "variable", color: "" }],
    ["c-d", { id: "c-d", name: "라", type: "expense", groupName: null, costType: "variable", color: "" }],
  ]);
  const byId = (id: string) => cats.get(id);
  const bud = (id: string, categoryId: string, sortOrder: number | null): Budget => ({
    id,
    yearMonth: null,
    categoryId,
    amount: 12345,
    versionId: "bv-o",
    sortOrder,
  });

  it("그룹은 최소 sortOrder 순, 미분류는 맨 뒤", () => {
    const g = groupBudgetsByCategory(
      [bud("b1", "c-a", 2), bud("b2", "c-c", 0), bud("b3", "c-b", 3), bud("b4", "c-d", 1)],
      byId
    );
    expect(g.map((x) => x.name)).toEqual(["베타", "알파", "미분류"]);
    expect(g[1].rows.map((r) => r.id)).toEqual(["b1", "b3"]);
  });

  it("sortOrder가 없는 행은 뒤로, 과목명 순", () => {
    const g = groupBudgetsByCategory(
      [bud("b1", "c-b", null), bud("b2", "c-a", null), bud("b3", "c-a", 0)],
      byId
    );
    expect(g[0].name).toBe("알파");
    expect(g[0].rows.map((r) => r.id)).toEqual(["b3", "b2"]);
  });

  it("빈 목록은 빈 배열", () => {
    expect(groupBudgetsByCategory([], byId)).toEqual([]);
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npm run test -- lib/compute.test.ts`
Expected: FAIL — `groupBudgetsByCategory is not a function`, `order` 속성 없음.

- [ ] **Step 3: `BurnRow`에 order 추가하고 `budgetBurndown`을 고친다**

`lib/compute.ts`의 `BurnRow`(현 119~125행 근처):

```ts
export interface BurnRow {
  category: Category;
  budget: number; // 그 달에 적용되는 예산. 안 잡았으면 0
  spend: number;
  pct: number; // 소진률 %. 예산 0인데 쓴 게 있으면 Infinity(= 막대 가득·초과)
  order: number | null; // 그 버전에서 사람이 정한 순서. 예산 행이 없으면 null
}
```

`budgetBurndown` 본문 — 과목마다 `budgetForCategory`를 부르는 대신 그 달 행을 한 번만 뽑아 맵으로 만든다. 금액과 순서를 같은 행에서 읽어야 하기 때문이다:

```ts
export function budgetBurndown(
  budgets: Budget[],
  versions: BudgetVersion[],
  categories: Category[],
  txns: Transaction[],
  ym: string
): BurnSummary {
  const spendMap = spendByCategory(monthTransactions(txns, ym));
  // 금액과 순서를 같은 행에서 읽어야 하므로 그 달 적용 버전의 행을 한 번만 뽑는다.
  // 같은 과목 행이 둘이면 앞의 것을 쓴다(budgetForCategory의 .find와 같은 규칙).
  const rowByCat = new Map<string, Budget>();
  for (const b of budgetsOfMonth(budgets, versions, ym)) {
    if (b.categoryId !== null && !rowByCat.has(b.categoryId)) rowByCat.set(b.categoryId, b);
  }
  const rows: BurnRow[] = [];

  for (const category of categories) {
    if (category.type !== "expense") continue;
    const row = rowByCat.get(category.id);
    const budget = row?.amount ?? 0;
    const spend = spendMap.get(category.id) ?? 0;
    rows.push({
      category,
      budget,
      spend,
      pct: budget > 0 ? (spend / budget) * 100 : spend > 0 ? Infinity : 0,
      order: row?.sortOrder ?? null,
    });
  }

  rows.sort(byOrderThenBurn);

  const budget = rows.reduce((s, r) => s + r.budget, 0);
  const spend = rows.reduce((s, r) => s + r.spend, 0);
  return {
    rows,
    budget,
    spend,
    pct: budget > 0 ? (spend / budget) * 100 : 0,
  };
}
```

- [ ] **Step 4: 정렬 비교자를 추가한다**

`lib/compute.ts`의 `byBurn`(현 135행 근처) 바로 아래:

```ts
// 사람이 정한 순서가 있으면 그것이 먼저. 순서가 없는 행은 뒤에서 기존 소진률 순.
function byOrderThenBurn(a: BurnRow, b: BurnRow): number {
  if (a.order !== null && b.order !== null) return a.order - b.order;
  if (a.order !== null) return -1;
  if (b.order !== null) return 1;
  return byBurn(a, b);
}

// 그룹의 순서 = 소속 행의 최소 order. 하나도 없으면 null(자동 정렬 대상).
function groupOrder(rows: BurnRow[]): number | null {
  let min: number | null = null;
  for (const r of rows) {
    if (r.order !== null && (min === null || r.order < min)) min = r.order;
  }
  return min;
}
```

- [ ] **Step 5: `groupBurnRows` 정렬을 바꾼다**

`lib/compute.ts`의 `groupBurnRows` — 시그니처는 그대로 두고 정렬만 바꾼다:

```ts
// 계정과목 행들을 상위 카테고리로 묶는다.
// 그룹 순서 = 소속 과목의 최소 order 순. 순서가 없는 그룹은 뒤에서 예산 합 큰 순.
// "미분류"는 항상 맨 뒤. 그룹 안도 order 순(없으면 소진률 순).
export function groupBurnRows(rows: BurnRow[]): BurnGroup[] {
  const map = new Map<string, BurnRow[]>();
  for (const r of rows) {
    const key = r.category.groupName || UNGROUPED;
    map.set(key, [...(map.get(key) ?? []), r]);
  }
  const groups: BurnGroup[] = [...map.entries()].map(([name, gRows]) => ({
    name,
    rows: [...gRows].sort(byOrderThenBurn),
    budget: gRows.reduce((s, r) => s + r.budget, 0),
    spend: gRows.reduce((s, r) => s + r.spend, 0),
  }));
  return groups.sort((a, b) => {
    if (a.name === UNGROUPED) return 1;
    if (b.name === UNGROUPED) return -1;
    const ao = groupOrder(a.rows);
    const bo = groupOrder(b.rows);
    if (ao !== null && bo !== null) return ao - bo;
    if (ao !== null) return -1;
    if (bo !== null) return 1;
    return b.budget - a.budget || b.spend - a.spend || a.name.localeCompare(b.name);
  });
}
```

- [ ] **Step 6: `groupBudgetsByCategory`를 추가한다**

`lib/compute.ts`의 `groupBurnRows` 아래에 추가한다. 예산 탭이 **선택한 버전**(적용중 버전이 아닐 수 있다)의 행을 묶어 보여주는 데 쓴다:

```ts
/* ───────────── 예산 탭 목록 묶음 ───────────── */

export interface BudgetGroup {
  name: string; // 카테고리명 (없으면 "미분류")
  rows: Budget[]; // 그 카테고리의 예산 행 — sortOrder 순
}

// 한 버전의 예산 행들을 카테고리로 묶는다. 예산 탭이 대시보드와 같은 모양을 갖게 하려는 것이다.
// 정렬 규칙은 groupBurnRows와 같다 — sortOrder 순, 없으면 뒤에 과목명 순, "미분류"는 맨 뒤.
export function groupBudgetsByCategory(
  rows: Budget[],
  categoryById: (id: string) => Category | undefined
): BudgetGroup[] {
  const nameOf = (b: Budget) => categoryById(b.categoryId ?? "")?.name ?? "";
  const bySort = (a: Budget, b: Budget) => {
    if (a.sortOrder !== null && b.sortOrder !== null) return a.sortOrder - b.sortOrder;
    if (a.sortOrder !== null) return -1;
    if (b.sortOrder !== null) return 1;
    return nameOf(a).localeCompare(nameOf(b));
  };
  const minSort = (gRows: Budget[]): number | null => {
    let min: number | null = null;
    for (const r of gRows) {
      if (r.sortOrder !== null && (min === null || r.sortOrder < min)) min = r.sortOrder;
    }
    return min;
  };

  const map = new Map<string, Budget[]>();
  for (const b of rows) {
    const key = categoryById(b.categoryId ?? "")?.groupName || UNGROUPED;
    map.set(key, [...(map.get(key) ?? []), b]);
  }
  const groups: BudgetGroup[] = [...map.entries()].map(([name, gRows]) => ({
    name,
    rows: [...gRows].sort(bySort),
  }));
  return groups.sort((a, b) => {
    if (a.name === UNGROUPED) return 1;
    if (b.name === UNGROUPED) return -1;
    const ao = minSort(a.rows);
    const bo = minSort(b.rows);
    if (ao !== null && bo !== null) return ao - bo;
    if (ao !== null) return -1;
    if (bo !== null) return 1;
    return a.name.localeCompare(b.name);
  });
}
```

- [ ] **Step 7: 테스트 통과 확인**

Run:
```bash
npm run test
npx tsc --noEmit -p tsconfig.json
npm run build
npm run lint
```
Expected: 전부 PASS. 기존 `budgetBurndown`·`groupBurnRows` 테스트가 깨지면 **픽스처에 `sortOrder: null`을 채워** 통과시킨다(동작은 그대로여야 한다 — 순서가 전부 null이면 기존 정렬 그대로).

- [ ] **Step 8: 커밋**

```bash
git add lib/compute.ts lib/compute.test.ts
git commit -m "$(cat <<'EOF'
feat(budget): 소진률 정렬에 사람이 정한 순서 반영·예산 목록 묶음 함수

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: 순서 이동 순수함수 (TDD)

**Files:**
- Create: `lib/budgetOrder.ts`
- Test: `lib/budgetOrder.test.ts`

**Interfaces:**
- Consumes: `BudgetGroup`·`groupBudgetsByCategory`(Task 2), `Budget.sortOrder`(Task 1)
- Produces:
  - `export function moveSubject(groups: BudgetGroup[], groupName: string, index: number, dir: -1 | 1): Budget[]`
  - `export function moveGroup(groups: BudgetGroup[], groupIndex: number, dir: -1 | 1): Budget[]`
  - 둘 다 **표시 순서대로 재배열된 전체 행**을 돌려주고, 각 행의 `sortOrder`는 `0..n-1`로 다시 매겨져 있다. 이동이 불가능하면(경계) **입력과 같은 순서**를 그대로 돌려준다.

**설계 메모:** 인접 두 행의 값만 맞바꾸지 않고 **전체를 0부터 다시 매기는** 이유는, 백필·수동 편집으로 값이 비거나 중복돼도 한 번의 이동으로 정상화되기 때문이다. 22행 규모라 비용이 문제되지 않는다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`lib/budgetOrder.test.ts` (신규):

```ts
import { describe, it, expect } from "vitest";
import { moveSubject, moveGroup } from "./budgetOrder";
import type { Budget } from "./types";
import type { BudgetGroup } from "./compute";

const bud = (id: string, sortOrder: number | null): Budget => ({
  id,
  yearMonth: null,
  categoryId: "c-" + id,
  amount: 12345,
  versionId: "bv-o",
  sortOrder,
});

// 알파[a1, a2] · 베타[b1] · 감마[g1, g2]
const groups = (): BudgetGroup[] => [
  { name: "알파", rows: [bud("a1", 0), bud("a2", 1)] },
  { name: "베타", rows: [bud("b1", 2)] },
  { name: "감마", rows: [bud("g1", 3), bud("g2", 4)] },
];

const ids = (rows: Budget[]) => rows.map((r) => r.id);
const orders = (rows: Budget[]) => rows.map((r) => r.sortOrder);

describe("moveSubject — 카테고리 안에서 과목 이동", () => {
  it("아래로 한 칸", () => {
    const out = moveSubject(groups(), "알파", 0, 1);
    expect(ids(out)).toEqual(["a2", "a1", "b1", "g1", "g2"]);
    expect(orders(out)).toEqual([0, 1, 2, 3, 4]);
  });

  it("위로 한 칸", () => {
    const out = moveSubject(groups(), "감마", 1, -1);
    expect(ids(out)).toEqual(["a1", "a2", "b1", "g2", "g1"]);
  });

  it("카테고리 경계를 넘지 않는다 — 첫 행의 위로는 무동작", () => {
    const out = moveSubject(groups(), "감마", 0, -1);
    expect(ids(out)).toEqual(["a1", "a2", "b1", "g1", "g2"]);
  });

  it("마지막 행의 아래로도 무동작", () => {
    const out = moveSubject(groups(), "알파", 1, 1);
    expect(ids(out)).toEqual(["a1", "a2", "b1", "g1", "g2"]);
  });

  it("한 행짜리 카테고리는 움직이지 않는다", () => {
    const out = moveSubject(groups(), "베타", 0, 1);
    expect(ids(out)).toEqual(["a1", "a2", "b1", "g1", "g2"]);
  });
});

describe("moveGroup — 카테고리 블록 통째 이동", () => {
  it("아래로 한 칸 — 소속 과목이 함께 움직인다", () => {
    const out = moveGroup(groups(), 0, 1);
    expect(ids(out)).toEqual(["b1", "a1", "a2", "g1", "g2"]);
    expect(orders(out)).toEqual([0, 1, 2, 3, 4]);
  });

  it("위로 한 칸", () => {
    const out = moveGroup(groups(), 2, -1);
    expect(ids(out)).toEqual(["a1", "a2", "g1", "g2", "b1"]);
  });

  it("맨 위의 위로는 무동작", () => {
    const out = moveGroup(groups(), 0, -1);
    expect(ids(out)).toEqual(["a1", "a2", "b1", "g1", "g2"]);
  });

  it("맨 아래의 아래로도 무동작", () => {
    const out = moveGroup(groups(), 2, 1);
    expect(ids(out)).toEqual(["a1", "a2", "b1", "g1", "g2"]);
  });
});

describe("sortOrder 정상화", () => {
  it("값이 비어 있거나 중복돼도 한 번의 이동으로 0..n-1이 된다", () => {
    const messy: BudgetGroup[] = [
      { name: "알파", rows: [bud("a1", null), bud("a2", 7)] },
      { name: "베타", rows: [bud("b1", 7)] },
    ];
    const out = moveGroup(messy, 0, 1);
    expect(orders(out)).toEqual([0, 1, 2]);
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npm run test -- lib/budgetOrder.test.ts`
Expected: FAIL — `Cannot find module './budgetOrder'`.

- [ ] **Step 3: 구현**

`lib/budgetOrder.ts` (신규):

```ts
import type { BudgetGroup } from "./compute";
import type { Budget } from "./types";

// 표시 순서대로 평탄화한 뒤 sortOrder를 0..n-1로 다시 매긴다.
// 백필이나 수동 편집으로 값이 비거나 중복돼도 이동 한 번이면 정상화된다.
function reindex(groups: BudgetGroup[]): Budget[] {
  return groups
    .flatMap((g) => g.rows)
    .map((row, i) => ({ ...row, sortOrder: i }));
}

// 같은 카테고리 안에서 과목을 한 칸 옮긴다. 카테고리 경계는 넘지 않는다.
// 경계를 벗어나는 요청은 무동작 — 현재 순서를 그대로 돌려준다.
export function moveSubject(
  groups: BudgetGroup[],
  groupName: string,
  index: number,
  dir: -1 | 1
): Budget[] {
  const gi = groups.findIndex((g) => g.name === groupName);
  if (gi < 0) return reindex(groups);
  const rows = groups[gi].rows;
  const to = index + dir;
  if (index < 0 || index >= rows.length || to < 0 || to >= rows.length) {
    return reindex(groups);
  }
  const next = rows.slice();
  [next[index], next[to]] = [next[to], next[index]];
  const nextGroups = groups.slice();
  nextGroups[gi] = { ...groups[gi], rows: next };
  return reindex(nextGroups);
}

// 카테고리 블록을 통째로 한 칸 옮긴다. 소속 과목이 함께 움직인다.
export function moveGroup(
  groups: BudgetGroup[],
  groupIndex: number,
  dir: -1 | 1
): Budget[] {
  const to = groupIndex + dir;
  if (groupIndex < 0 || groupIndex >= groups.length || to < 0 || to >= groups.length) {
    return reindex(groups);
  }
  const next = groups.slice();
  [next[groupIndex], next[to]] = [next[to], next[groupIndex]];
  return reindex(next);
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run:
```bash
npm run test -- lib/budgetOrder.test.ts
npx tsc --noEmit -p tsconfig.json
```
Expected: 14 케이스 PASS, 타입 에러 0.

- [ ] **Step 5: 커밋**

```bash
git add lib/budgetOrder.ts lib/budgetOrder.test.ts
git commit -m "$(cat <<'EOF'
feat(budget): 순서 이동 순수함수 moveSubject·moveGroup

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: 예산 탭 — 카테고리 묶음 + 「순서 편집」

**Files:**
- Modify: `components/budget/BudgetVersions.tsx` (목록 렌더 전면 교체)

**Interfaces:**
- Consumes: `groupBudgetsByCategory`(Task 2), `moveSubject`·`moveGroup`(Task 3), data-context의 `saveBudget`
- Produces: 화면만 — 다른 태스크가 의존하는 export 없음

- [ ] **Step 1: 목록 정렬을 묶음으로 바꾼다**

`components/budget/BudgetVersions.tsx` 상단. 현재 `rows`는 카테고리명→과목명으로 정렬한 평면 배열인데(현 48~59행), 이것을 묶음으로 교체한다:

```tsx
const groups = selected
  ? groupBudgetsByCategory(
      budgets.filter((b) => b.versionId === selected.id && b.categoryId !== null),
      categoryById
    )
  : [];
const total = groups.reduce(
  (s, g) => s + g.rows.reduce((t, b) => t + b.amount, 0),
  0
);
```

import에 `groupBudgetsByCategory`(`@/lib/compute`)와 `moveSubject`·`moveGroup`(`@/lib/budgetOrder`)을 추가한다. 기존 `rows` 변수와 그 정렬 코드는 지운다.

- [ ] **Step 2: 순서 편집 상태와 저장 함수를 추가한다**

같은 컴포넌트 안:

```tsx
const [ordering, setOrdering] = useState(false);
const [saving, setSaving] = useState(false);

// 이동 결과를 저장한다. saveBudget은 실패 시 예외를 던지므로(sbUpsertOrThrow)
// 낙관적으로 먼저 그리지 않고, 바뀐 행만 저장이 끝난 뒤 컨텍스트가 반영하게 둔다.
async function applyOrder(next: Budget[]) {
  if (saving) return;
  const current = new Map(
    groups.flatMap((g) => g.rows).map((b) => [b.id, b.sortOrder])
  );
  const changed = next.filter((b) => current.get(b.id) !== b.sortOrder);
  if (changed.length === 0) return;
  setSaving(true);
  try {
    for (const b of changed) await saveBudget(b);
  } catch {
    window.alert("순서를 저장하지 못했어요. 다시 시도해 주세요.");
  } finally {
    setSaving(false);
  }
}
```

`useData()` 구조 분해에 `saveBudget`을 추가한다.

- [ ] **Step 3: 섹션 헤더에 「순서 편집」 토글을 붙인다**

현재 `SectionTitle`의 `right`는 `+ 설정` 버튼 하나다(현 130행 근처). 두 개로 바꾼다:

```tsx
<SectionTitle
  right={
    <div className="flex items-center gap-3">
      <button
        onClick={() => setOrdering((v) => !v)}
        className="text-xs font-semibold text-stone"
      >
        {ordering ? "편집 완료" : "순서 편집"}
      </button>
      {!ordering && (
        <button
          onClick={() => setBudgetForm({})}
          className="text-xs font-semibold text-leaf"
        >
          + 설정
        </button>
      )}
    </div>
  }
>
  {selected.name} · 과목별 예산
</SectionTitle>
```

- [ ] **Step 4: 목록 본문을 묶음 렌더로 교체한다**

현재 `rows.map(...)` 블록(현 165~217행 근처)을 통째로 아래로 바꾼다. 카테고리 머리글은 대시보드와 같은 `bg-stone` 띠를 쓴다:

```tsx
{groups.length === 0 ? (
  <Empty>이 버전에는 아직 예산이 없어요.</Empty>
) : (
  groups.map((g, gi) => (
    <div key={g.name}>
      <div className="mb-1.5 flex items-center gap-2">
        <p className="flex-1 rounded-md bg-stone px-2 py-1 text-[11px] font-bold text-white">
          {g.name}
        </p>
        {ordering && (
          <div className="flex shrink-0 items-center gap-1">
            <button
              disabled={gi === 0 || saving}
              onClick={() => applyOrder(moveGroup(groups, gi, -1))}
              aria-label={`${g.name} 위로`}
              className="px-2 py-1 text-sm text-leaf disabled:text-stone/40"
            >
              ▲
            </button>
            <button
              disabled={gi === groups.length - 1 || saving}
              onClick={() => applyOrder(moveGroup(groups, gi, 1))}
              aria-label={`${g.name} 아래로`}
              className="px-2 py-1 text-sm text-leaf disabled:text-stone/40"
            >
              ▼
            </button>
          </div>
        )}
      </div>
      <div className="space-y-3 border-l-2 border-line pl-2">
        {g.rows.map((bud, ri) => {
          const cat = categoryById(bud.categoryId!);
          const used = spend.get(bud.categoryId!) ?? 0;
          return (
            <div key={bud.id}>
              <div className="mb-1 flex items-center justify-between">
                <span className="flex min-w-0 items-center gap-1 text-sm font-semibold text-ink">
                  <span className="truncate">{cat?.name}</span>
                  {cat?.costType && (
                    <Pill tone={cat.costType === "fixed" ? "sky" : "stone"}>
                      {COST_TYPE_LABEL[cat.costType]}
                    </Pill>
                  )}
                </span>
                <div className="flex shrink-0 items-center gap-2">
                  <span className="text-xs text-stone">
                    {isApplied ? `${won(used)} / ` : ""}
                    {won(bud.amount)}
                  </span>
                  {ordering ? (
                    <>
                      <button
                        disabled={ri === 0 || saving}
                        onClick={() => applyOrder(moveSubject(groups, g.name, ri, -1))}
                        aria-label={`${cat?.name ?? "과목"} 위로`}
                        className="px-1 text-sm text-leaf disabled:text-stone/40"
                      >
                        ▲
                      </button>
                      <button
                        disabled={ri === g.rows.length - 1 || saving}
                        onClick={() => applyOrder(moveSubject(groups, g.name, ri, 1))}
                        aria-label={`${cat?.name ?? "과목"} 아래로`}
                        className="px-1 text-sm text-leaf disabled:text-stone/40"
                      >
                        ▼
                      </button>
                    </>
                  ) : (
                    <>
                      <button
                        onClick={() => setBudgetForm({ initial: bud })}
                        className="text-xs text-leaf"
                      >
                        수정
                      </button>
                      <button
                        onClick={() => {
                          if (
                            window.confirm(
                              `${cat?.name ?? "이"} 예산을 ${selected.name}에서 삭제할까요?`
                            )
                          )
                            removeBudget(bud.id);
                        }}
                        className="text-xs text-coral"
                      >
                        삭제
                      </button>
                    </>
                  )}
                </div>
              </div>
              {/* 사용액은 이번 달 기준이라 적용중 버전에서만 의미가 있다 */}
              {isApplied && !ordering && <ProgressBar value={used} max={bud.amount} />}
            </div>
          );
        })}
      </div>
    </div>
  ))
)}
```

카테고리명이 머리글로 올라갔으므로 **과목 행에서는 `groupName` 접두사를 뺀다**(위 코드는 `cat?.name`만 쓴다). `Card`의 `className`을 `space-y-3`에서 `space-y-4`로 바꿔 그룹 간격을 준다.

- [ ] **Step 5: 검증**

Run:
```bash
npx tsc --noEmit -p tsconfig.json
npm run build
npm run lint
npm run test
```
Expected: 전부 PASS. 미사용이 된 import(`ProgressBar`가 여전히 쓰이는지 등)를 린트가 잡으면 정리한다.

- [ ] **Step 6: 커밋**

```bash
git add components/budget/BudgetVersions.tsx
git commit -m "$(cat <<'EOF'
feat(budget): 예산 탭 카테고리 묶음 렌더·「순서 편집」 두 층 이동

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: 계정과목 2단 선택 (`CategoryPicker`)

**Files:**
- Create: `components/budget/CategoryPicker.tsx`
- Modify: `components/budget/forms.tsx` (`TransactionForm`·`BudgetForm`의 계정 과목 Field)

**Interfaces:**
- Consumes: `groupBudgetsByCategory`·`budgetsOfMonth`·`resolveVersion`(Task 2 및 기존), data-context의 `budgets`·`budgetVersions`·`categories`
- Produces:
  ```tsx
  export default function CategoryPicker({
    type, value, onChange, extraOption,
  }: {
    type: TxType;                                  // 지출/수입 — 이 유형의 과목만 고른다
    value: string;                                 // 선택된 계정 과목 id ("" = 미선택)
    onChange: (categoryId: string) => void;
    extraOption?: { value: string; label: string }; // 과목 select 맨 위에 끼울 특수 항목
  })
  ```

- [ ] **Step 1: `CategoryPicker`를 만든다**

`components/budget/CategoryPicker.tsx` (신규):

```tsx
"use client";

import { useMemo, useState } from "react";
import { useData } from "@/lib/data-context";
import { budgetsOfMonth, groupBudgetsByCategory } from "@/lib/compute";
import { currentYearMonth } from "@/lib/format";
import { inputCls } from "./ui";
import { UNGROUPED, type TxType } from "@/lib/types";

// 계정 과목이 33개라 단일 드롭다운으로는 찾기 어렵다. 카테고리를 먼저 고르고
// 그 안의 과목만 보여준다. 카테고리 나열 순서는 이번 달 적용 버전의 예산 순서를 따른다.
export default function CategoryPicker({
  type,
  value,
  onChange,
  extraOption,
}: {
  type: TxType;
  value: string;
  onChange: (categoryId: string) => void;
  extraOption?: { value: string; label: string };
}) {
  const { categories, categoryById, budgets, budgetVersions } = useData();

  const cats = useMemo(
    () => categories.filter((c) => c.type === type),
    [categories, type]
  );

  // 예산 순서가 있는 카테고리 먼저, 나머지는 이름순, "미분류"는 맨 뒤.
  const groupNames = useMemo(() => {
    const ym = currentYearMonth();
    const ordered = groupBudgetsByCategory(
      budgetsOfMonth(budgets, budgetVersions, ym).filter((b) => b.categoryId !== null),
      categoryById
    ).map((g) => g.name);
    const present = new Set(cats.map((c) => c.groupName || UNGROUPED));
    const out = ordered.filter((n) => present.has(n));
    const rest = [...present]
      .filter((n) => !out.includes(n) && n !== UNGROUPED)
      .sort((a, b) => a.localeCompare(b));
    const tail = present.has(UNGROUPED) ? [UNGROUPED] : [];
    return [...out.filter((n) => n !== UNGROUPED), ...rest, ...tail];
  }, [budgets, budgetVersions, categoryById, cats]);

  const groupOf = (id: string) => categoryById(id)?.groupName || UNGROUPED;
  const [group, setGroup] = useState<string>(value ? groupOf(value) : "");

  const subjects = cats.filter((c) => (c.groupName || UNGROUPED) === group);

  return (
    <div className="grid grid-cols-2 gap-2">
      <select
        className={inputCls}
        value={group}
        onChange={(e) => {
          setGroup(e.target.value);
          onChange(""); // 카테고리를 바꾸면 과목 선택은 비운다
        }}
        aria-label="카테고리"
      >
        <option value="">카테고리</option>
        {groupNames.map((n) => (
          <option key={n} value={n}>
            {n}
          </option>
        ))}
      </select>
      <select
        className={inputCls}
        value={value}
        onChange={(e) => onChange(e.target.value)}
        aria-label="계정 과목"
      >
        <option value="">계정 과목</option>
        {extraOption && (
          <option value={extraOption.value}>{extraOption.label}</option>
        )}
        {subjects.map((c) => (
          <option key={c.id} value={c.id}>
            {c.name}
          </option>
        ))}
      </select>
    </div>
  );
}
```

⚠️ `group` 초기값은 `useState`라 첫 렌더에서만 읽힌다. 호출부가 시트를 열 때마다 새로 마운트하므로(`{open && <Form .../>}` 패턴) 편집 진입 시 카테고리가 올바르게 잡힌다. **`type`이 바뀔 때는 호출부가 `key`로 리마운트한다**(Step 2 참조) — effect로 setState하면 이 저장소의 `set-state-in-effect` 룰에 걸린다.

- [ ] **Step 2: `TransactionForm`에 적용한다**

`components/budget/forms.tsx`의 계정 과목 Field(현 108~122행)를 교체한다:

```tsx
      <Field label="계정 과목">
        <CategoryPicker
          key={type}
          type={type}
          value={categoryId}
          onChange={setCategoryId}
        />
      </Field>
```

`key={type}`이 필요한 이유는 지출↔수입 토글 시 카테고리 선택까지 초기화해야 하기 때문이다(현재 코드도 토글에서 `setCategoryId("")`를 하고 있으니 그대로 둔다).

파일 상단에 `import CategoryPicker from "./CategoryPicker";`를 추가한다. `cats` 지역 변수가 이 폼에서 더는 쓰이지 않으면 지운다(린트가 잡는다).

- [ ] **Step 3: `BudgetForm`에 적용한다**

`components/budget/forms.tsx`의 `BudgetForm` 계정 과목 Field(현 434~450행)를 교체한다. 구버전 "전체 월예산" 행을 편집할 때만 나오는 특수 선택지는 `extraOption`으로 넘긴다:

```tsx
      <Field label="계정 과목">
        <CategoryPicker
          type="expense"
          value={scope}
          onChange={setScope}
          extraOption={
            legacyOverall ? { value: "__all__", label: "전체 월예산 (구버전)" } : undefined
          }
        />
      </Field>
```

⚠️ `scope`가 `"__all__"`일 때 `groupOf("__all__")`는 `categoryById`가 못 찾아 `UNGROUPED`를 돌려준다. 구버전 행 편집이라는 예외 경로이고 과목 select에 `extraOption`이 항상 보이므로 동작에 문제가 없다.

- [ ] **Step 4: 새 예산 행의 `sortOrder`를 맨 뒤로 준다**

`BudgetForm.submit`이 `saveBudget`에 넘기는 payload에 `sortOrder`가 빠지면 타입 에러가 난다. 기존 행을 고칠 때는 그 값을 유지하고, 새로 만들 때는 그 버전의 최대값 + 1을 준다:

```tsx
  async function submit() {
    if (!valid) return;
    const categoryId = scope === "__all__" ? null : scope;
    // 같은 버전 안에서 같은 과목의 행은 하나뿐이다.
    const existing = budgets.find(
      (b) => b.versionId === versionId && b.categoryId === categoryId
    );
    // 새 행은 목록 맨 뒤. 같은 카테고리 옆에 끼워 넣지 않는다 — 「순서 편집」으로 옮긴다.
    const maxOrder = budgets
      .filter((b) => b.versionId === versionId && b.sortOrder !== null)
      .reduce((m, b) => Math.max(m, b.sortOrder!), -1);
    await saveBudget({
      id: existing?.id ?? "",
      yearMonth: null,
      categoryId,
      amount: amt,
      versionId,
      sortOrder: existing ? existing.sortOrder : maxOrder + 1,
    });
    setAmount("");
    onClose();
  }
```

- [ ] **Step 5: 카테고리가 하나뿐이면 자동으로 고른다**

수입 계정 과목은 `groupName`이 없어 「미분류」 하나로만 묶인다. 그 상태에서 카테고리를 굳이 한 번 더 고르게 하면 2단이 손해다. `CategoryPicker`의 `group` 초기값을 이렇게 바꾼다:

```tsx
  const [group, setGroup] = useState<string>(() => {
    if (value) return groupOf(value);
    return groupNames.length === 1 ? groupNames[0] : "";
  });
```

⚠️ `groupOf`와 `groupNames`가 `useState` 초기화보다 위에 선언돼 있어야 한다. Step 1의 코드 배치(`groupNames` → `groupOf` → `useState`)를 그대로 지킨다.

- [ ] **Step 6: 검증**

Run:
```bash
npx tsc --noEmit -p tsconfig.json
npm run build
npm run lint
npm run test
```
Expected: 전부 PASS. 신규 린트 0.

- [ ] **Step 7: 커밋**

```bash
git add components/budget/CategoryPicker.tsx components/budget/forms.tsx
git commit -m "$(cat <<'EOF'
feat(budget): 계정 과목 선택을 카테고리 2단 드롭다운으로

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: 0026 적용 · 배포 · 라이브 검증

**Files:** 없음 (운영·검증만)

**Interfaces:**
- Consumes: Task 1~5 전부

- [ ] **Step 1: 최종 점검**

Run:
```bash
npm run test && npx tsc --noEmit -p tsconfig.json && npm run lint && npm run build
```
Expected: 전부 PASS.

- [ ] **Step 2: 0026을 프로덕션에 적용한다**

🔴 **DDL을 배포보다 먼저.** main 머지가 곧 Vercel 배포이므로, DDL이 뒤따르면 `sort_order`를 읽는 코드가 컬럼 없는 DB를 만난다.

🔴 **내가 직접 못 한다.** 연결된 Supabase MCP는 회사 계정이고 배추가족은 개인 계정 프로젝트(`jyfjnxvsblfkkbscddnw`)다. `~/.claude.json` 토큰 트리 탐색은 Credential Exploration으로 차단된다. **0025 때와 같이 재헌이 `! node <스크립트>`로 직접 실행**하는 것이 표준 경로다.

`scratchpad/apply-0026.mjs`를 0025 때와 같은 형태로 만들어 재헌에게 실행을 요청한다. 검증 쿼리를 스크립트에 함께 넣는다:

```sql
select count(*) from budgets where sort_order is null and category_id is not null;  -- 0
select category_id, sort_order from budgets where version_id = 'bv-1' order by sort_order limit 5;
```

Expected: null 0건, `sort_order`가 0,1,2,3,4로 이어짐.

- [ ] **Step 3: main 머지 · push**

```bash
git checkout main && git merge --no-ff feat/budget-ordering && git push
```

⚠️ `git push`는 이 저장소의 로컬 credential 설정(`credential.https://github.com.helper=manager`)으로 동작한다. 전역 gh 헬퍼가 개인 계정 자격증명을 가리므로 **전역 설정을 건드리지 않는다.**

- [ ] **Step 4: 라이브 검증**

`https://baechoo-family.vercel.app` — PIN 1106.

🔴 **Playwright 확인은 1회만.** 한 세션에서 아래를 훑는다:
1. 가계부 → 예산 탭: 카테고리 머리글로 묶여 보이고, 「순서 편집」 버튼이 있다
2. 「순서 편집」을 켜면 `[수정][삭제]`가 `[▲][▼]`로 바뀌고 머리글에도 ▲▼가 붙는다
3. 카테고리 하나를 ▼로 한 칸 내린 뒤 대시보드로 가면 **같은 순서**로 보인다
4. 거래 입력(+ 버튼)의 계정 과목이 2단이고, 왼쪽에서 카테고리를 고르면 오른쪽이 그 카테고리 과목만 보여준다
5. 왼쪽 카테고리 나열 순서가 3번에서 바꾼 순서와 같다
6. 합계가 여전히 ₩3,884,781이다

- [ ] **Step 5: 결과 보고**

바꾼 순서가 대시보드·2단 드롭다운에 그대로 반영됐는지, 다르면 어디가 다른지 그대로 보고한다. 「대체로 정상」 같은 완충어를 쓰지 않는다. 순서를 바꿨다면 **원래대로 되돌려 놓는다**(검증용 변경을 남기지 않는다).

---

## 범위 밖 (이 플랜에서 하지 않는다)

- 고정지출 등록 폼(`RecurringForm`)과 거래내역 필터(`Transactions.tsx`)의 2단 전환
- 카테고리를 엔티티로 승격 (`event_categories`처럼) — 지금은 문자열 그룹명으로 충분하다
- 드래그 앤 드롭 — 이 앱은 캘린더 드래그에서 터치 함정(passive 이벤트·스크롤 충돌)을 이미 겪었다
- 예산 0원 과목을 예산 탭 목록에 노출
- 이월 minor(`totalBudget` 중복 제거 · `computeYearPnl` 두 버전 회귀 테스트 · `BudgetForm` 저장 실패 UI · 복제 중간 실패 잔존 · `(version_id, category_id)` 유니크 제약 · `forms.tsx` 1,097줄)
