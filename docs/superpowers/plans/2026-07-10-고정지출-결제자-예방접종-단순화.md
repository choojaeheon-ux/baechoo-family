# 고정지출 결제자 표시 · 예방접종 단순화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 고정지출 체크리스트에서 결제자를 보고 바꿀 수 있게 하고, 예방접종을 차수 없이 연 1회 체크로 단순화한다.

**Architecture:** 두 변경은 서로 독립이다. (1) `CalendarView`의 행을 `<div>` + 두 형제 버튼으로 쪼개 결제자 칩을 넣고, 칩은 이미 저장된 거래의 `member`만 갈아끼워 upsert한다. (2) `BaechooVaccine`에서 `doses`·`intervalMonths`·`nextDue`를 지우고 `lastDone` 하나만 남긴 뒤, 체크 상태와 다음 예정일을 순수 함수로 파생한다.

**Tech Stack:** Next.js 16.2.9, React 19, TypeScript, Tailwind v4, Supabase, vitest 4

## Global Constraints

- 언어: 모든 UI 문자열은 한국어
- 이 Next.js는 학습 데이터와 다르다. 코드 작성 전 `node_modules/next/dist/docs/`의 해당 가이드를 읽는다 (`AGENTS.md`)
- 컴포넌트 테스트 인프라 없음 — 순수 함수만 vitest로 테스트하고, UI는 dev 서버 + Playwright로 구동 검증한다
- `<button>` 안에 `<button>`을 넣지 않는다
- 기존 테스트(`lib/compute.test.ts`, `lib/pnl.test.ts`) 회귀 0
- `data/`·라이브 Supabase 데이터를 삭제하지 않는다. `drop column`은 Task 4에서만, 사전 조회 로그를 남기고 실행한다
- 브랜치: `main`. 각 Task 끝에 커밋한다

**Spec:** `docs/superpowers/specs/2026-07-10-고정지출-결제자-예방접종-단순화-design.md`

---

## File Structure

| 파일 | 책임 | Task |
|---|---|---|
| `components/budget/CalendarView.tsx` | 고정지출 체크리스트 행 구조 + 결제자 칩·세그먼트 | 1 |
| `lib/vaccine.ts` | 접종 상태 파생 순수 함수 (저장·UI 무지) | 2 |
| `lib/vaccine.test.ts` | 위 함수의 경계 규칙 | 2 |
| `lib/types.ts` | `BaechooVaccine` 모델 | 3 |
| `lib/repo.ts` | Supabase 매핑 + 구버전 localStorage 승격 | 3 |
| `components/baechoo/VaccineList.tsx` | 토글 체크박스·정렬·알약 | 3 |
| `components/baechoo/forms.tsx` | `VaccineDetailSheet` 축소 | 3 |
| `supabase/migrations/0017_vaccine_simplify.sql` | 컬럼 이관·드롭 | 4 |

Task 3은 타입·저장·화면을 한꺼번에 바꾼다. 쪼개면 중간 커밋에서 `tsc`가 깨진다 — `doses`를 지우는 순간 두 컴포넌트가 동시에 컴파일 불가가 되기 때문이다. 이 셋은 한 원자 단위다.

---

### Task 1: 고정지출 체크리스트 결제자 표시·변경

**Files:**
- Modify: `components/budget/CalendarView.tsx:84-118` (`togglePayer` 추가)
- Modify: `components/budget/CalendarView.tsx:423-497` (세그먼트 + 행 구조)

**Interfaces:**
- Consumes: `saveTransaction(t: Transaction) => Promise<void>` (data-context), `memberName(id: Member) => string`, `DueItem { recurring, dueDate, paidTxn }` (`lib/recurring.ts`)
- Produces: 없음 (UI 종단)

- [ ] **Step 1: `togglePayer` 추가**

`unpay` 함수 바로 아래(현재 118행 뒤)에 붙인다.

```tsx
  // 이미 체크된 항목의 결제자만 교체 (체크 상태는 유지)
  async function togglePayer(d: DueItem) {
    if (!d.paidTxn) return;
    const next: Member = d.paidTxn.member === "chuchu" ? "baejji" : "chuchu";
    await saveTransaction({ ...d.paidTxn, member: next });
  }
```

`saveTransaction`은 `id` 기준 upsert다(`lib/repo.ts:642-647`). 새 거래가 생기지 않는다.

- [ ] **Step 2: 세그먼트에 라벨을 붙이고 탭 타깃을 44px로 키운다**

`{/* 고정지출 투두 */}` 아래 `SectionTitle`의 `right` prop을 통째로 교체한다.

```tsx
      <SectionTitle
        right={
          <div className="flex items-center gap-1 text-[11px]">
            <span className="mr-1 font-semibold text-stone">결제자</span>
            {(["chuchu", "baejji"] as Member[]).map((mm) => (
              <button
                key={mm}
                onClick={() => setMember(mm)}
                className={`flex min-h-11 items-center rounded-full px-3 font-semibold ${
                  member === mm
                    ? "bg-leaf text-white"
                    : "bg-card border border-line text-stone"
                }`}
              >
                {memberName(mm)}
              </button>
            ))}
          </div>
        }
      >
        고정지출 체크리스트
      </SectionTitle>
```

`min-h-11` = 2.75rem = 44px. 하드코딩된 `"추추"`/`"배찌"` 문자열은 `memberName(mm)`으로 바꾼다.

- [ ] **Step 3: 행을 `<div>` + 두 형제 버튼으로 쪼갠다**

`due.map((d) => { ... })` 안의 `return (...)` 전체를 교체한다.

```tsx
            return (
              <div
                key={d.recurring.id}
                className="flex w-full items-center gap-3 px-1 py-2"
              >
                <button
                  onClick={() => (paid ? unpay(d) : pay(d))}
                  className="flex min-w-0 flex-1 items-center gap-3 rounded-xl text-left active:bg-cream"
                >
                  <span
                    className={`flex h-6 w-6 shrink-0 items-center justify-center rounded-full border-2 text-xs ${
                      paid
                        ? "border-leaf bg-leaf text-white"
                        : "border-line text-transparent"
                    }`}
                  >
                    ✓
                  </span>
                  <span className="text-lg">{cat?.icon ?? "💸"}</span>
                  <div className="min-w-0 flex-1">
                    <p className={`truncate text-sm font-semibold ${paid ? "text-stone line-through" : "text-ink"}`}>
                      {d.recurring.name}
                      {inst && (
                        <span className="ml-1 text-[11px] font-normal text-stone">
                          ({inst.paid}/{inst.total})
                        </span>
                      )}
                    </p>
                    <p className="text-xs text-stone">
                      {Number(d.dueDate.slice(8))}일 출금
                    </p>
                  </div>
                </button>
                <div className="shrink-0 text-right">
                  <p className="text-sm font-bold tabular text-ink">{won(d.recurring.amount)}</p>
                  {paid && d.paidTxn ? (
                    <button
                      onClick={() => togglePayer(d)}
                      aria-label={`결제자 ${memberName(d.paidTxn.member)}, 눌러서 변경`}
                      className="mt-0.5 rounded-full bg-leaf-light px-2 py-0.5 text-[11px] font-semibold text-leaf-dark active:scale-95"
                    >
                      {memberName(d.paidTxn.member)}
                    </button>
                  ) : (
                    <Pill tone={left < 0 ? "coral" : left <= 3 ? "gold" : "stone"}>
                      {left < 0 ? "지남" : ddayLabel(d.dueDate)}
                    </Pill>
                  )}
                </div>
              </div>
            );
```

바깥 `<Card className="space-y-1">`와 `due.length === 0` 분기는 그대로 둔다. `pay`·`unpay`·할부 카운터는 건드리지 않는다.

- [ ] **Step 4: 타입 체크**

Run: `npx tsc --noEmit`
Expected: 에러 0. (`Member`와 `memberName`은 이미 20-22행에서 import되어 있다.)

- [ ] **Step 5: 기존 테스트 회귀 확인**

Run: `npm test`
Expected: `lib/compute.test.ts`, `lib/pnl.test.ts` 전부 PASS

- [ ] **Step 6: dev 서버 + Playwright로 구동 검증**

Run: `npm run dev` (백그라운드), 브라우저 390×664로 `http://localhost:3000/budget` → 캘린더 탭.

확인할 것:
1. 세그먼트 버튼 실측 높이 ≥ 44px — `getBoundingClientRect().height`
2. `배찌` 선택 → 미체크 항목 체크 → 그 행 우측에 `배찌` 칩이 뜬다
3. 칩 탭 → `추추`로 바뀌고 체크 동그라미는 그대로 채워져 있다
4. 칩 탭이 `unpay`를 부르지 않는다 — 거래 건수가 늘거나 줄지 않는다
5. 행 본문 탭 → 체크 해제, 칩이 D-day 알약으로 돌아온다

라이브 Supabase에 붙으므로 **검증 후 만든 거래는 반드시 되돌린다**(행 탭으로 체크 해제).

- [ ] **Step 7: 커밋**

```bash
git add components/budget/CalendarView.tsx
git commit -m "fix(budget): 고정지출 체크리스트에 결제자 표시·변경 추가

추추/배찌 세그먼트가 화면에 아무 결과도 남기지 않아 죽은 버튼처럼 보였다.
체크된 행에 결제자 칩을 띄우고, 칩 탭으로 결제자만 교체한다."
```

---

### Task 2: 접종 상태 파생 순수 함수 (TDD)

**Files:**
- Rewrite: `lib/vaccine.ts`
- Create: `lib/vaccine.test.ts`

**Interfaces:**
- Consumes: `addMonths(iso: string, n: number) => string` (`lib/format.ts:81`, 월말 초과 시 그 달 말일로 클램프)
- Produces:
  - `VACCINE_INTERVAL_MONTHS: 12`
  - `vaccineNextDue(lastDone: string | null) => string | null`
  - `vaccineDone(lastDone: string | null, today: string) => boolean`

  Task 3의 `VaccineList.tsx`가 이 세 개를 쓴다. `latestDose`·`nextDoseNumber`는 이 Task에서 사라진다.

`today`를 인자로 받는 이유는 테스트 가능성이다. 호출부가 `todayISO()`를 넘긴다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

Create `lib/vaccine.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { vaccineNextDue, vaccineDone, VACCINE_INTERVAL_MONTHS } from "./vaccine";

describe("vaccineNextDue", () => {
  it("미접종이면 예정일이 없다", () => {
    expect(vaccineNextDue(null)).toBeNull();
  });

  it("최근 접종일 + 12개월", () => {
    expect(vaccineNextDue("2026-07-10")).toBe("2027-07-10");
  });

  it("윤년 2/29 접종은 다음 해 2/28로 클램프된다", () => {
    expect(vaccineNextDue("2024-02-29")).toBe("2025-02-28");
  });

  it("주기는 12개월이다", () => {
    expect(VACCINE_INTERVAL_MONTHS).toBe(12);
  });
});

describe("vaccineDone", () => {
  it("미접종이면 미완료다", () => {
    expect(vaccineDone(null, "2026-07-10")).toBe(false);
  });

  it("오늘 접종했으면 완료다", () => {
    expect(vaccineDone("2026-07-10", "2026-07-10")).toBe(true);
  });

  it("예정일이 아직 남았으면 완료다", () => {
    expect(vaccineDone("2026-01-01", "2026-07-10")).toBe(true);
  });

  it("예정일 당일이면 미완료다 — 오늘 맞아야 한다", () => {
    expect(vaccineDone("2025-07-10", "2026-07-10")).toBe(false);
  });

  it("예정일이 지났으면 미완료다", () => {
    expect(vaccineDone("2025-06-01", "2026-07-10")).toBe(false);
  });
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `npx vitest run lib/vaccine.test.ts`
Expected: FAIL — `vaccineNextDue`·`vaccineDone`·`VACCINE_INTERVAL_MONTHS` export 없음

- [ ] **Step 3: 최소 구현**

`lib/vaccine.ts` 전체를 교체한다.

```ts
import { addMonths } from "./format";

// 배추 정기 백신 5종은 모두 연 1회 부스터다.
export const VACCINE_INTERVAL_MONTHS = 12;

// 다음 접종 예정일 = 최근 접종일 + 12개월. 미접종이면 null.
export function vaccineNextDue(lastDone: string | null): string | null {
  return lastDone ? addMonths(lastDone, VACCINE_INTERVAL_MONTHS) : null;
}

// 예정일이 아직 오지 않았으면 완료. 예정일 당일은 "오늘 맞아야 함"이므로 미완료다.
export function vaccineDone(lastDone: string | null, today: string): boolean {
  const due = vaccineNextDue(lastDone);
  return due !== null && due > today;
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `npx vitest run lib/vaccine.test.ts`
Expected: 9 tests PASS

이 시점에 `VaccineList.tsx`·`forms.tsx`는 `latestDose`·`nextDoseNumber`를 찾지 못해 `tsc`가 깨진다. Task 3에서 함께 고친다. **이 Task는 커밋하지 않고 Task 3의 커밋에 합류한다.**

- [ ] **Step 5: 깨진 참조를 확인해 Task 3의 작업 목록으로 삼는다**

Run: `npx tsc --noEmit`
Expected: `components/baechoo/VaccineList.tsx`, `components/baechoo/forms.tsx`에서 에러. 다른 파일은 없어야 한다.

---

### Task 3: 예방접종 모델 교체 — 타입·저장·화면

**Files:**
- Modify: `lib/types.ts:319-336` (`VaccineDose` 삭제, `BaechooVaccine` 축소)
- Modify: `lib/repo.ts:31-32` (import), `lib/repo.ts:92` (LS 읽기), `lib/repo.ts:489-507` (매퍼), 그리고 `normalizeExam`(50행) 옆에 `normalizeVaccine` 추가
- Modify: `components/baechoo/VaccineList.tsx`
- Modify: `components/baechoo/forms.tsx:27` (import), `640-788` (`VaccineDetailSheet`)

**Interfaces:**
- Consumes: `vaccineNextDue`, `vaccineDone`, `VACCINE_INTERVAL_MONTHS` (Task 2), `todayISO()`, `dday(iso) => number`, `ddayLabel(iso) => string` (`lib/format.ts`)
- Produces: `BaechooVaccine { id, name, lastDone: string | null, memo: string | null, createdAt }`

- [ ] **Step 1: 타입을 줄인다**

`lib/types.ts`의 `/* 배추 예방접종 */` 블록에서 `VaccineDose` 인터페이스를 통째로 지우고 `BaechooVaccine`을 교체한다.

```ts
/* ───────────── 배추 예방접종 ───────────── */

// 예방접종 — 백신별 최근 접종일 하나. 다음 예정일·체크 여부는 lib/vaccine.ts에서 파생.
export interface BaechooVaccine {
  id: string;
  name: string;
  lastDone: string | null;  // 최근 접종일 (YYYY-MM-DD), null = 미접종
  memo: string | null;
  createdAt: string;        // YYYY-MM-DD
}
```

`STANDARD_VACCINES`는 그대로 둔다.

- [ ] **Step 2: 저장 계층을 바꾼다**

`lib/repo.ts:31-32`의 import에서 `VaccineDose`를 뺀다 (`BaechooVaccine`은 남긴다).

매퍼(489-507행)를 교체한다.

```ts
// 배추 — 예방접종 (최근 접종일 하나. 다음 예정일은 파생)
const toVaccine = (r: Record<string, unknown>): BaechooVaccine => ({
  id: r.id as string,
  name: (r.name as string) ?? "",
  lastDone: (r.last_done as string) ?? null,
  memo: (r.memo as string) ?? null,
  createdAt: (r.created_at as string) ?? "",
});
const fromVaccine = (x: BaechooVaccine) => ({
  id: x.id,
  name: x.name,
  last_done: x.lastDone,
  memo: x.memo,
  created_at: x.createdAt || null,
});
```

`normalizeExam`(50행) 바로 아래에 구버전 localStorage 승격 함수를 추가한다.

```ts
// 구버전 localStorage 접종 기록(doses 차수 배열)을 최근 접종일 하나로 승격
function normalizeVaccine(
  x: BaechooVaccine & { doses?: { n: number; date: string }[] }
): BaechooVaccine {
  const lastDone =
    x.lastDone ??
    (Array.isArray(x.doses) && x.doses.length > 0
      ? x.doses.reduce((a, b) => (b.date > a.date ? b : a)).date
      : null);
  return {
    id: x.id,
    name: x.name,
    lastDone,
    memo: x.memo ?? null,
    createdAt: x.createdAt,
  };
}
```

92행을 바꾼다.

```ts
      baechooVaccines: (parsed.baechooVaccines ?? []).map(normalizeVaccine),
```

- [ ] **Step 3: 상세 시트를 줄인다**

`components/baechoo/forms.tsx:27`의 `import { nextDoseNumber } from "@/lib/vaccine";`를 지운다. 그 위 import 블록에서 `type VaccineDose,`도 지운다.

`VaccineDetailSheet`(640-788행) 전체를 교체한다.

```tsx
export function VaccineDetailSheet({
  open,
  onClose,
  initial,
}: {
  open: boolean;
  onClose: () => void;
  initial?: BaechooVaccine;
}) {
  const { saveBaechooVaccine, removeBaechooVaccine } = useData();
  const [name, setName] = useState(initial?.name ?? "");
  const [lastDone, setLastDone] = useState(initial?.lastDone ?? "");
  const [memo, setMemo] = useState(initial?.memo ?? "");

  const valid = name.trim().length > 0;

  async function submit() {
    if (!valid) return;
    await saveBaechooVaccine({
      id: initial?.id ?? "",
      name: name.trim(),
      lastDone: lastDone || null,
      memo: memo.trim() || null,
      createdAt: initial?.createdAt ?? todayISO(),
    });
    onClose();
  }

  return (
    <Sheet open={open} onClose={onClose} title={initial ? "백신 관리" : "백신 추가"}>
      <Field label="백신 이름">
        <input
          className={inputCls}
          value={name}
          onChange={(e) => setName(e.target.value)}
          placeholder="예: 종합백신, 광견병백신"
        />
      </Field>

      <Field label="최근 접종일 (비우면 미접종)">
        <input
          type="date"
          className={inputCls}
          value={lastDone}
          onChange={(e) => setLastDone(e.target.value)}
        />
      </Field>

      <Field label="메모 (선택)">
        <input
          className={inputCls}
          value={memo}
          onChange={(e) => setMemo(e.target.value)}
        />
      </Field>

      <div className="mt-2">
        <PrimaryButton onClick={submit} disabled={!valid}>
          저장
        </PrimaryButton>
      </div>
      {initial && (
        <DeleteButton
          onDelete={async () => {
            await removeBaechooVaccine(initial.id);
            onClose();
          }}
        />
      )}
    </Sheet>
  );
}
```

`updateDose`·`addDose`·`removeDose`·`interval`·`nextDue` 상태는 전부 사라진다.

- [ ] **Step 4: 목록 화면을 바꾼다**

`components/baechoo/VaccineList.tsx` 전체를 교체한다.

```tsx
"use client";

import { useMemo, useState } from "react";
import { useData } from "@/lib/data-context";
import { todayISO, dday, ddayLabel } from "@/lib/format";
import { STANDARD_VACCINES, type BaechooVaccine } from "@/lib/types";
import { Card, Pill, SectionTitle } from "@/components/budget/ui";
import { VaccineDetailSheet } from "./forms";
import { vaccineDone, vaccineNextDue } from "@/lib/vaccine";

// 체크 동그라미 (HealthTodos 패턴)
function Check({ checked, onClick }: { checked: boolean; onClick: () => void }) {
  return (
    <button
      onClick={onClick}
      aria-label={checked ? "접종 취소" : "오늘 접종 체크"}
      className={`flex h-6 w-6 shrink-0 items-center justify-center rounded-full border-2 text-xs transition ${
        checked ? "border-leaf bg-leaf text-white" : "border-line text-transparent"
      }`}
    >
      ✓
    </button>
  );
}

export default function VaccineList() {
  const { baechooVaccines, saveBaechooVaccine } = useData();
  const [form, setForm] = useState<{ open: boolean; initial?: BaechooVaccine }>({
    open: false,
  });
  const [seeding, setSeeding] = useState(false);
  const today = todayISO();

  // 리마인드가 목적이므로 미완료를 위로. 기한 지남 → 미접종 → 접종 완료.
  const sorted = useMemo(() => {
    const rank = (v: BaechooVaccine) =>
      vaccineDone(v.lastDone, today) ? 2 : v.lastDone ? 0 : 1;
    return [...baechooVaccines].sort((a, b) => {
      const ra = rank(a);
      const rb = rank(b);
      if (ra !== rb) return ra - rb;
      if (ra === 1) return a.name < b.name ? -1 : a.name > b.name ? 1 : 0;
      const da = vaccineNextDue(a.lastDone)!;
      const db = vaccineNextDue(b.lastDone)!;
      return da < db ? -1 : da > db ? 1 : 0;
    });
  }, [baechooVaccines, today]);

  // 체크 = 오늘 접종. 해제 = 미접종으로 되돌림.
  async function toggle(v: BaechooVaccine) {
    await saveBaechooVaccine({
      ...v,
      lastDone: vaccineDone(v.lastDone, today) ? null : today,
    });
  }

  // 표준 백신 일괄 추가 (이미 있는 이름은 건너뜀)
  async function addStandard() {
    setSeeding(true);
    try {
      const existing = new Set(baechooVaccines.map((v) => v.name));
      for (const name of STANDARD_VACCINES) {
        if (existing.has(name)) continue;
        await saveBaechooVaccine({
          id: "",
          name,
          lastDone: null,
          memo: null,
          createdAt: today,
        });
      }
    } finally {
      setSeeding(false);
    }
  }

  return (
    <div>
      <SectionTitle
        right={
          <button
            onClick={() => setForm({ open: true })}
            className="text-xs font-bold text-leaf-dark"
          >
            + 백신
          </button>
        }
      >
        예방접종
      </SectionTitle>

      {sorted.length === 0 ? (
        <Card>
          <p className="py-1 text-center text-sm text-stone">
            예방접종을 체크리스트로 관리해 보세요.
          </p>
          <button
            onClick={addStandard}
            disabled={seeding}
            className="mt-2 w-full rounded-xl border border-dashed border-leaf bg-leaf-light/40 py-2.5 text-sm font-bold text-leaf-dark active:scale-[0.99] disabled:opacity-50"
          >
            표준 백신 추가 (배추 정기 5종)
          </button>
        </Card>
      ) : (
        <Card>
          <div className="space-y-2">
            {sorted.map((v) => {
              const done = vaccineDone(v.lastDone, today);
              const due = vaccineNextDue(v.lastDone);
              return (
                <div key={v.id} className="flex items-center gap-3">
                  <Check checked={done} onClick={() => toggle(v)} />
                  <button
                    onClick={() => setForm({ open: true, initial: v })}
                    className="min-w-0 flex-1 text-left"
                  >
                    <span className="block truncate text-sm font-semibold text-ink">
                      {v.name}
                    </span>
                    <span className="block text-[11px] text-stone">
                      {!v.lastDone
                        ? "접종 기록 없음"
                        : done
                          ? `${v.lastDone} 접종 · 다음 ${due}`
                          : `${v.lastDone} 접종`}
                    </span>
                  </button>
                  {done && due ? (
                    <Pill tone={dday(due) <= 30 ? "gold" : "stone"}>
                      {ddayLabel(due)}
                    </Pill>
                  ) : (
                    <Pill tone="coral">접종 필요</Pill>
                  )}
                </div>
              );
            })}
          </div>
        </Card>
      )}

      {form.open && (
        <VaccineDetailSheet
          key={form.initial?.id ?? "new"}
          open={form.open}
          onClose={() => setForm({ open: false })}
          initial={form.initial}
        />
      )}
    </div>
  );
}
```

- [ ] **Step 5: 타입 체크 — `doses` 잔존 참조 0**

Run: `npx tsc --noEmit`
Expected: 에러 0

Run: `grep -rn "doses\|nextDoseNumber\|latestDose\|intervalMonths\|VaccineDose" --include=*.ts --include=*.tsx app components lib`
Expected: 출력 없음

- [ ] **Step 6: 전체 테스트**

Run: `npm test`
Expected: `vaccine.test.ts` 9개 포함 전부 PASS, 기존 테스트 회귀 0

- [ ] **Step 7: 커밋 (Task 2 결과물 포함)**

```bash
git add lib/vaccine.ts lib/vaccine.test.ts lib/types.ts lib/repo.ts \
        components/baechoo/VaccineList.tsx components/baechoo/forms.tsx
git commit -m "feat(baechoo): 예방접종 차수 제거 — 연 1회 체크로 단순화

doses/intervalMonths/nextDue를 lastDone 하나로 줄이고, 체크 여부와
다음 예정일은 vaccineDone/vaccineNextDue로 파생한다. 1년이 지나면
체크가 저절로 풀리며 다시 리마인드가 뜬다."
```

---

### Task 4: 마이그레이션 — 컬럼 이관·드롭

**Files:**
- Create: `supabase/migrations/0017_vaccine_simplify.sql`

**Interfaces:**
- Consumes: Task 3의 `fromVaccine`이 `last_done` 컬럼을 기대한다
- Produces: `baechoo_vaccines(id, name, last_done, memo, created_at, deleted_at)`

`0013`이 만들었던 `last_done`을 `0014`가 지웠던 자리라, 되살리는 셈이다.

- [ ] **Step 1: 적용 전 현재 내용을 조회해 로그로 남긴다**

`drop column`은 되돌릴 수 없다. Supabase MCP `execute_sql`로:

```sql
select id, name, next_due, interval_months, doses from baechoo_vaccines;
```

출력을 대화에 남긴다. `doses`의 최근 날짜가 무엇인지 눈으로 확인해 둔다 — Step 3에서 `last_done`이 그 값과 일치해야 한다.

- [ ] **Step 2: 마이그레이션 파일을 만든다**

Create `supabase/migrations/0017_vaccine_simplify.sql`:

```sql
-- 배추가족 — 예방접종 단순화: 차수(doses) 제거, 최근 접종일 하나만 유지.
-- 다음 예정일(next_due)과 주기(interval_months)는 lib/vaccine.ts에서 파생한다.
-- 0013이 만든 last_done을 0014가 지웠던 자리라, 되살리는 셈이다.

alter table baechoo_vaccines add column if not exists last_done date;

-- 기존 차수 기록 중 가장 최근 접종일을 끌어올려 리마인드가 끊기지 않게 한다.
update baechoo_vaccines
set last_done = (
  select max((d->>'date')::date)
  from jsonb_array_elements(doses) d
)
where doses is not null
  and jsonb_array_length(doses) > 0
  and last_done is null;

alter table baechoo_vaccines drop column if exists doses;
alter table baechoo_vaccines drop column if exists next_due;
alter table baechoo_vaccines drop column if exists interval_months;

drop index if exists idx_baechoo_vaccines_next_due;
create index if not exists idx_baechoo_vaccines_last_done
  on baechoo_vaccines(last_done);
```

- [ ] **Step 3: 적용하고 결과를 검증한다**

Supabase MCP `apply_migration`으로 위 SQL을 `0017_vaccine_simplify` 이름으로 적용한다.

그다음 `execute_sql`로:

```sql
select id, name, last_done, memo from baechoo_vaccines order by name;
```

Expected: Step 1에서 `doses`가 있던 행의 `last_done`이 그 배열의 최댓값 날짜와 같다. `doses`가 비었던 행은 `last_done`이 null이다.

컬럼이 실제로 사라졌는지도 확인한다.

```sql
select column_name from information_schema.columns
where table_name = 'baechoo_vaccines' order by column_name;
```

Expected: `created_at, deleted_at, id, last_done, memo, name` — 그 외 없음

- [ ] **Step 4: dev 서버 + Playwright로 구동 검증**

`http://localhost:3000/baechoo` → 예방접종 섹션.

확인할 것:
1. 백신 목록이 뜨고, 접종 기록이 있던 백신은 `YYYY-MM-DD 접종 · 다음 …` 부제를 보여준다
2. 미접종 백신은 `접종 필요`(coral) 알약 + 빈 체크박스
3. 체크 → 부제가 `오늘 날짜 접종 · 다음 (1년 뒤)`로 바뀌고 알약이 `D-365`로 바뀐다
4. 다시 체크 해제 → `접종 기록 없음` + `접종 필요`로 돌아온다
5. 백신을 탭해 상세 시트를 열면 차수 UI가 없고 이름·접종일·메모만 있다

**라이브 데이터다.** 검증으로 바꾼 값은 상세 시트에서 원래 접종일로 되돌린다. Step 1의 조회 로그가 원본이다.

- [ ] **Step 5: 커밋**

```bash
git add supabase/migrations/0017_vaccine_simplify.sql
git commit -m "chore(db): 0017 예방접종 차수 컬럼 제거, last_done 이관"
```

---

### Task 5: 마무리

- [ ] **Step 1: 전체 검증**

Run: `npm test && npx tsc --noEmit && npx next build`
Expected: 전부 통과

- [ ] **Step 2: 푸시**

```bash
git push origin main
```

Vercel이 자동 배포한다. 배포 후 프로덕션 URL(`baechoo-family.vercel.app`)에서 두 화면을 한 번씩 열어본다.
