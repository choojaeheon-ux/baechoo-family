# 배추 예방접종 체크리스트 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 건강 탭에 매년 부스터를 자동 관리하는 예방접종 체크리스트 섹션을 추가한다.

**Architecture:** 우주 체크리스트(0012)와 동일한 표준 엔티티 경로 — `BaechooVaccine` 타입 → `repo.ts` 매퍼/소프트삭제/휴지통 → `data-context` 낙관적 mutator → 신규 `VaccineList` 섹션을 `HealthList`에 삽입. 신규 Supabase 테이블 `baechoo_vaccines`(마이그레이션 0013). 체크 시 접종일 기록 + 다음 예정일 +N개월 자동 갱신.

**Tech Stack:** Next.js 16(App Router) · React 19 · TypeScript · Tailwind v4 · Supabase(저장소 이중화: 클라우드/localStorage)

## Global Constraints

- 이 Next.js는 학습 데이터와 다를 수 있음 — 코드 작성 전 `node_modules/next/dist/docs/`의 관련 가이드 확인(레포 `AGENTS.md`).
- **테스트 프레임워크 없음.** 검증 게이트 = `npx tsc --noEmit`(타입) + `npx eslint <변경파일>`(린트) + `npm run build`(통합) + localStorage 모드 수동 스모크. 단위 테스트 하네스를 새로 만들지 않는다.
- **Next16/React19 ESLint 함정**: `react-hooks/set-state-in-effect` — effect 안에서 `setState` 직접호출 금지. 조건부 리셋은 파생값으로 계산.
- 저장소 이중화: 모든 엔티티는 `hasSupabase ? sb : ls` 양쪽 경로를 갖는다. 삭제는 소프트삭제(`sbSoftDelete`).
- 날짜는 절대값 `YYYY-MM-DD`. 외부로 나가는 결과물 아님(개인 앱)이라 카피 규칙 제약 없음.
- ID는 `repo.newId()`(폼에서는 빈 문자열 `""`로 저장 → `save*`가 `id || newId()`로 채움).
- 커밋 푸시는 사용자 지시 시에만. 각 태스크는 변경 파일만 스테이징해 커밋.

---

## File Structure

| 파일 | 역할 | 변경 |
| --- | --- | --- |
| `lib/types.ts` | `BaechooVaccine` 인터페이스 + `DataSnapshot.baechooVaccines` + `STANDARD_VACCINES` 상수 | 수정 |
| `lib/format.ts` | `addMonths(iso, n)` 날짜 헬퍼(월말 클램프) | 수정 |
| `lib/repo.ts` | `toVaccine`/`fromVaccine` 매퍼, ls/cloud save·delete, loadAll, 휴지통(`TrashKind`·`loadBaechooTrash`·`purgeOldBaechooTrash`) | 수정 |
| `lib/data-context.tsx` | state·refresh·`saveBaechooVaccine`/`removeBaechooVaccine` mutator + 타입 | 수정 |
| `supabase/migrations/0013_baechoo_vaccines.sql` | 신규 테이블 DDL | 생성 |
| `components/baechoo/VaccineList.tsx` | 예방접종 섹션(카드·체크·표준백신 추가) | 생성 |
| `components/baechoo/forms.tsx` | `VaccineForm`(편집/삭제) | 수정 |
| `components/baechoo/HealthList.tsx` | `<VaccineList />` 삽입 | 수정 |

---

## Task 1: 데이터 레이어 (타입 · 날짜 헬퍼 · repo · context)

UI 없이 `BaechooVaccine` 엔티티를 끝까지 배선한다. 컴파일·린트로 검증.

**Files:**
- Modify: `lib/types.ts`
- Modify: `lib/format.ts`
- Modify: `lib/repo.ts`
- Modify: `lib/data-context.tsx`

**Interfaces:**
- Produces:
  - `interface BaechooVaccine { id: string; name: string; lastDone: string | null; nextDue: string | null; intervalMonths: number; history: string[]; memo: string | null; createdAt: string }`
  - `const STANDARD_VACCINES: string[]` — 프리셋 백신 이름 5종
  - `addMonths(iso: string, n: number): string` — `lib/format.ts`
  - `saveBaechooVaccine(v: BaechooVaccine): Promise<void>` / `removeBaechooVaccine(id: string): Promise<void>` — `useData()`
  - `DataSnapshot.baechooVaccines: BaechooVaccine[]`

- [ ] **Step 1: 타입 추가 (`lib/types.ts`)**

`UjuChecklist` 인터페이스 블록 바로 위(`/* ───────────── 우주 육아기록부 ───────────── */` 위)에 배추 예방접종 블록 추가:

```ts
/* ───────────── 배추 예방접종 ───────────── */

// 예방접종 — 매년 부스터(체크 시 접종일 기록 + 다음 예정일 +interval개월)
export interface BaechooVaccine {
  id: string;
  name: string;
  lastDone: string | null;    // 마지막 접종일 (YYYY-MM-DD, 미접종이면 null)
  nextDue: string | null;     // 다음 예정일 (YYYY-MM-DD, 미접종이면 null)
  intervalMonths: number;     // 추가접종 주기(개월, 기본 12)
  history: string[];          // 접종일 누적 (YYYY-MM-DD 오름차순)
  memo: string | null;
  createdAt: string;          // YYYY-MM-DD
}

// 표준 성견 연례 백신 프리셋 ("표준 백신 추가" 버튼)
export const STANDARD_VACCINES: string[] = [
  "종합백신 (DHPPL)",
  "코로나 장염",
  "켄넬코프 (기관지염)",
  "인플루엔자",
  "광견병",
];
```

- [ ] **Step 2: DataSnapshot에 필드 추가 (`lib/types.ts`)**

`DataSnapshot` 인터페이스 마지막 필드 `ujuChecklists: UjuChecklist[];` 다음 줄에 추가:

```ts
  baechooVaccines: BaechooVaccine[];
```

- [ ] **Step 3: 날짜 헬퍼 추가 (`lib/format.ts`)**

`ddayLabel` 함수 정의 끝(`}` 다음, `const WEEKDAYS` 위)에 추가:

```ts
// iso 날짜에 n개월 더하기 (월말 초과 시 그 달 말일로 클램프)
export function addMonths(iso: string, n: number): string {
  const [y, m, d] = iso.split("-").map(Number);
  const targetMonthIndex = m - 1 + n;       // 0-based
  const lastDay = new Date(y, targetMonthIndex + 1, 0).getDate();
  const day = Math.min(d, lastDay);
  return toISODate(new Date(y, targetMonthIndex, day));
}
```

- [ ] **Step 4: repo 매퍼 추가 (`lib/repo.ts`)**

(a) import 타입에 `BaechooVaccine` 추가 — `UjuChecklist,` 다음 줄:

```ts
  BaechooVaccine,
```

(b) `toUjuChecklist`/`fromUjuChecklist` 정의 블록 다음(`/* ───────────── 공개 API ───────────── */` 위)에 매퍼 추가:

```ts
// 배추 — 예방접종 (history는 jsonb 배열)
const toVaccine = (r: Record<string, unknown>): BaechooVaccine => ({
  id: r.id as string,
  name: (r.name as string) ?? "",
  lastDone: (r.last_done as string) ?? null,
  nextDue: (r.next_due as string) ?? null,
  intervalMonths: Number(r.interval_months ?? 12),
  history: Array.isArray(r.history) ? (r.history as string[]) : [],
  memo: (r.memo as string) ?? null,
  createdAt: (r.created_at as string) ?? "",
});
const fromVaccine = (x: BaechooVaccine) => ({
  id: x.id,
  name: x.name,
  last_done: x.lastDone,
  next_due: x.nextDue,
  interval_months: x.intervalMonths,
  history: x.history,
  memo: x.memo,
  created_at: x.createdAt || null,
});
```

- [ ] **Step 5: localStorage 스냅샷에 배선 (`lib/repo.ts`)**

(a) `lsRead`의 반환 객체에서 `ujuChecklists: parsed.ujuChecklists ?? [],` 다음 줄에 추가:

```ts
      baechooVaccines: parsed.baechooVaccines ?? [],
```

(b) `emptySnapshot`의 반환 객체에서 `ujuChecklists: [],` 다음 줄에 추가:

```ts
    baechooVaccines: [],
```

- [ ] **Step 6: loadAll에 배선 (`lib/repo.ts`)**

(a) 구조분해 배열에 `ujuChecks,` 다음 줄:

```ts
    vaccines,
```

(b) `Promise.all` 배열의 `sb.from("uju_checklists").select("*").is("deleted_at", null),` 다음 줄:

```ts
    sb.from("baechoo_vaccines").select("*").is("deleted_at", null),
```

(c) 반환 객체의 `ujuChecklists: (ujuChecks.data ?? []).map(toUjuChecklist),` 다음 줄:

```ts
    baechooVaccines: (vaccines.data ?? []).map(toVaccine),
```

- [ ] **Step 7: save/delete 함수 추가 (`lib/repo.ts`)**

`saveUjuChecklist`/`deleteUjuChecklist` 정의 블록 다음(`/* ───────────── 휴지통 ... ───────────── */` 위)에 추가:

```ts
export async function saveBaechooVaccine(
  x: BaechooVaccine
): Promise<BaechooVaccine> {
  const row = { ...x, id: x.id || newId() };
  if (hasSupabase) await sbUpsert("baechoo_vaccines", fromVaccine(row));
  else lsUpsert("baechooVaccines", row);
  return row;
}
export async function deleteBaechooVaccine(id: string) {
  if (hasSupabase) await sbSoftDelete("baechoo_vaccines", id);
  else lsDelete("baechooVaccines", id);
}
```

- [ ] **Step 8: 휴지통에 배선 (`lib/repo.ts`)**

(a) `TrashKind` 유니온에 `"vaccine"` 추가:

```ts
export type TrashKind =
  | "meal"
  | "health"
  | "exam"
  | "healthTodo"
  | "walk"
  | "ujuChecklist"
  | "vaccine";
```

(b) `loadBaechooTrash`의 `del` 호출 구조분해에 추가 — `ujuChecks]` → `ujuChecks, vaccines]`, 그리고 `Promise.all` 배열의 `del("uju_checklists"),` 다음 줄에 `del("baechoo_vaccines"),`:

```ts
  const [meals, healths, exams, htodos, walks, ujuChecks, vaccines] =
    await Promise.all([
      del("baechoo_meals"),
      del("baechoo_health"),
      del("baechoo_exams"),
      del("baechoo_health_todos"),
      del("baechoo_walks"),
      del("uju_checklists"),
      del("baechoo_vaccines"),
    ]);
```

(c) uju 루프(`for (const r of ujuChecks.data ?? [])`) 다음에 vaccine 루프 추가(`items.sort(...)` 위):

```ts
  for (const r of vaccines.data ?? []) {
    const v = toVaccine(r);
    items.push({
      kind: "vaccine",
      table: "baechoo_vaccines",
      id: v.id,
      deletedAt: r.deleted_at,
      label: `예방접종 · ${v.name || "-"}`,
    });
  }
```

(d) `purgeOldBaechooTrash`의 테이블 배열에 `"baechoo_vaccines"` 추가:

```ts
    [
      "baechoo_meals",
      "baechoo_health",
      "baechoo_exams",
      "baechoo_health_todos",
      "baechoo_walks",
      "uju_checklists",
      "baechoo_vaccines",
    ].map((t) => sb.from(t).delete().lt("deleted_at", cutoff))
```

- [ ] **Step 9: data-context 배선 (`lib/data-context.tsx`)**

(a) import 타입에 추가 — `UjuChecklist,` 다음 줄:

```ts
  BaechooVaccine,
```

(b) `DataContextValue` 인터페이스: `ujuChecklists: UjuChecklist[];` 다음 줄에:

```ts
  baechooVaccines: BaechooVaccine[];
```

그리고 mutator 선언 끝(`removeUjuChecklist: (id: string) => Promise<void>;`) 다음 줄에:

```ts
  saveBaechooVaccine: (v: BaechooVaccine) => Promise<void>;
  removeBaechooVaccine: (id: string) => Promise<void>;
```

(c) state 선언 — `const [ujuChecklists, setUjuChecklists] = useState<UjuChecklist[]>([]);` 다음 줄에:

```ts
  const [baechooVaccines, setBaechooVaccines] = useState<BaechooVaccine[]>([]);
```

(d) `refresh` 내부 — `setUjuChecklists(snap.ujuChecklists);` 다음 줄에:

```ts
    setBaechooVaccines(snap.baechooVaccines);
```

(e) `value` 객체 — `ujuChecklists,` 다음 줄에 `baechooVaccines,` 추가, 그리고 mutator `removeUjuChecklist` 블록 다음에:

```ts
      saveBaechooVaccine: async (v) => {
        const saved = await repo.saveBaechooVaccine(v);
        upsertLocal(setBaechooVaccines, saved);
      },
      removeBaechooVaccine: async (id) => {
        await repo.deleteBaechooVaccine(id);
        setBaechooVaccines((p) => p.filter((x) => x.id !== id));
      },
```

(f) `useMemo` 의존성 배열 — `ujuChecklists,` 다음 줄에:

```ts
      baechooVaccines,
```

- [ ] **Step 10: 타입체크 + 린트**

Run: `cd "C:/Users/MGRV/.claude/baechoo-family" && npx tsc --noEmit`
Expected: 에러 없음 (exit 0)

Run: `cd "C:/Users/MGRV/.claude/baechoo-family" && npx eslint lib/types.ts lib/format.ts lib/repo.ts lib/data-context.tsx`
Expected: 에러 없음

- [ ] **Step 11: 커밋**

```bash
cd "C:/Users/MGRV/.claude/baechoo-family"
git add lib/types.ts lib/format.ts lib/repo.ts lib/data-context.tsx
git commit -m "feat: 배추 예방접종 데이터 레이어(BaechooVaccine·repo·context·휴지통)"
```

---

## Task 2: Supabase 마이그레이션 0013

신규 테이블 `baechoo_vaccines`를 생성하고 개인 프로젝트에 적용·검증한다.

**Files:**
- Create: `supabase/migrations/0013_baechoo_vaccines.sql`

**Interfaces:**
- Consumes: Task 1의 컬럼명(`name`·`last_done`·`next_due`·`interval_months`·`history`·`memo`·`created_at`·`deleted_at`)
- Produces: 클라우드 테이블 `baechoo_vaccines`(RLS family_all, next_due 인덱스)

- [ ] **Step 1: 마이그레이션 파일 작성**

Create `supabase/migrations/0013_baechoo_vaccines.sql`:

```sql
-- 배추가족 — 배추 예방접종 체크리스트 (매년 부스터)
-- 0001~0012 실행한 개인 Supabase 프로젝트의 SQL Editor에 붙여넣어 실행하세요.

create table if not exists baechoo_vaccines (
  id text primary key,
  name text not null,
  last_done date,                    -- 마지막 접종일 (미접종이면 null)
  next_due date,                     -- 다음 예정일 (미접종이면 null)
  interval_months int not null default 12,
  history jsonb not null default '[]'::jsonb,  -- 접종일 배열
  memo text,
  created_at timestamptz not null default now(),
  deleted_at timestamptz             -- 소프트 삭제(휴지통)
);

alter table baechoo_vaccines enable row level security;
drop policy if exists "family_all" on baechoo_vaccines;
create policy "family_all" on baechoo_vaccines
  for all to anon, authenticated using (true) with check (true);

create index if not exists idx_baechoo_vaccines_next_due on baechoo_vaccines(next_due);
```

- [ ] **Step 2: 개인 프로젝트에 적용**

`supabase-personal` MCP가 로드돼 있으면:
Run: `mcp__supabase-personal__apply_migration` (name=`0013_baechoo_vaccines`, query=위 SQL)

MCP 미로드 시: `~/.claude.json`의 `SUPABASE_ACCESS_TOKEN`으로 Management API 직접 적용 —
`POST https://api.supabase.com/v1/projects/jyfjnxvsblfkkbscddnw/database/query` (Bearer 토큰, body `{"query": "<위 SQL>"}`). 한글 주석 포함이므로 **node https로 `Buffer.from(json,'utf8')`** 전송(PowerShell Invoke-RestMethod는 mojibake). 토큰은 사용자에게 그때 확인.

- [ ] **Step 3: 적용 검증**

테이블 존재·컬럼 확인 쿼리 실행:

```sql
select column_name, data_type
from information_schema.columns
where table_name = 'baechoo_vaccines'
order by ordinal_position;
```

Expected: id·name·last_done·next_due·interval_months·history·memo·created_at·deleted_at 9개 컬럼.

- [ ] **Step 4: 커밋**

```bash
cd "C:/Users/MGRV/.claude/baechoo-family"
git add supabase/migrations/0013_baechoo_vaccines.sql
git commit -m "feat: 0013 baechoo_vaccines 마이그레이션(예방접종 테이블)"
```

---

## Task 3: 예방접종 UI (VaccineList · VaccineForm · 건강 탭 삽입)

건강 탭에 예방접종 섹션을 렌더하고 표준 백신 추가·체크·편집을 구현한다.

**Files:**
- Create: `components/baechoo/VaccineList.tsx`
- Modify: `components/baechoo/forms.tsx`
- Modify: `components/baechoo/HealthList.tsx`

**Interfaces:**
- Consumes: `useData().baechooVaccines`·`saveBaechooVaccine`·`removeBaechooVaccine`(Task 1), `STANDARD_VACCINES`·`BaechooVaccine`(Task 1), `addMonths`·`dday`·`ddayLabel`·`todayISO`(format)
- Produces: `<VaccineList />`(default export), `VaccineForm`(named export, `forms.tsx`)

- [ ] **Step 1: VaccineForm 추가 (`components/baechoo/forms.tsx`)**

(a) 타입 import 블록에 `BaechooVaccine` 추가 — `type BaechooHealthTodo,` 다음 줄:

```ts
  type BaechooVaccine,
```

(b) 파일 맨 끝(`HealthTodoForm` 닫는 `}` 다음)에 추가:

```tsx
/* ───────────── 예방접종 폼 ───────────── */
export function VaccineForm({
  open,
  onClose,
  initial,
}: {
  open: boolean;
  onClose: () => void;
  initial?: BaechooVaccine;
}) {
  const { saveBaechooVaccine, removeBaechooVaccine } = useData();
  const [name, setName] = useState(initial?.name ?? "");
  const [lastDone, setLastDone] = useState(initial?.lastDone ?? "");
  const [nextDue, setNextDue] = useState(initial?.nextDue ?? "");
  const [interval, setInterval] = useState(String(initial?.intervalMonths ?? 12));
  const [memo, setMemo] = useState(initial?.memo ?? "");

  const valid = name.trim().length > 0;

  async function submit() {
    if (!valid) return;
    const months = Number(interval) || 12;
    await saveBaechooVaccine({
      id: initial?.id ?? "",
      name: name.trim(),
      lastDone: lastDone || null,
      nextDue: nextDue || null,
      intervalMonths: months,
      history: initial?.history ?? [],
      memo: memo.trim() || null,
      createdAt: initial?.createdAt ?? todayISO(),
    });
    onClose();
  }

  return (
    <Sheet open={open} onClose={onClose} title={initial ? "백신 수정" : "백신 추가"}>
      <Field label="백신 이름">
        <input
          className={inputCls}
          value={name}
          onChange={(e) => setName(e.target.value)}
          placeholder="예: 종합백신 (DHPPL), 광견병"
        />
      </Field>

      <div className="flex gap-2">
        <div className="min-w-0 flex-1">
          <Field label="마지막 접종일 (선택)">
            <input
              type="date"
              className={inputCls}
              value={lastDone}
              onChange={(e) => setLastDone(e.target.value)}
            />
          </Field>
        </div>
        <div className="min-w-0 flex-1">
          <Field label="다음 예정일 (선택)">
            <input
              type="date"
              className={inputCls}
              value={nextDue}
              onChange={(e) => setNextDue(e.target.value)}
            />
          </Field>
        </div>
      </div>

      <Field label="추가접종 주기 (개월)">
        <input
          className={inputCls}
          inputMode="numeric"
          value={interval}
          onChange={(e) => setInterval(e.target.value.replace(/[^0-9]/g, ""))}
          placeholder="12"
        />
      </Field>

      <Field label="메모 (선택)">
        <input
          className={inputCls}
          value={memo}
          onChange={(e) => setMemo(e.target.value)}
        />
      </Field>

      <div className="mt-2">
        <PrimaryButton onClick={submit} disabled={!valid}>
          저장
        </PrimaryButton>
      </div>
      {initial && (
        <DeleteButton
          onDelete={async () => {
            await removeBaechooVaccine(initial.id);
            onClose();
          }}
        />
      )}
    </Sheet>
  );
}
```

> 참고: `replace(/[^0-9]/g, "")` 필터는 iOS 숫자입력 호환을 위해 `type="text"+inputMode`를 쓰는 기존 컨벤션(메모리: 배추 폼 숫자입력 iOS 글씨 안 보임 수정)을 따른 것.

- [ ] **Step 2: VaccineList 컴포넌트 생성 (`components/baechoo/VaccineList.tsx`)**

```tsx
"use client";

import { useMemo, useState } from "react";
import { useData } from "@/lib/data-context";
import { todayISO, addMonths, dday, ddayLabel } from "@/lib/format";
import { STANDARD_VACCINES, type BaechooVaccine } from "@/lib/types";
import { Card, Pill, SectionTitle } from "@/components/budget/ui";
import { VaccineForm } from "./forms";

// 체크 동그라미 (HealthTodos 패턴)
function Check({ onClick }: { onClick: () => void }) {
  return (
    <button
      onClick={onClick}
      className="flex h-6 w-6 shrink-0 items-center justify-center rounded-full border-2 border-line text-xs text-transparent transition"
    >
      ✓
    </button>
  );
}

function dateLabelKo(iso: string): string {
  return `${iso.slice(0, 4)}-${iso.slice(5, 7)}-${iso.slice(8, 10)}`;
}

export default function VaccineList() {
  const { baechooVaccines, saveBaechooVaccine } = useData();
  const [form, setForm] = useState<{ open: boolean; initial?: BaechooVaccine }>({
    open: false,
  });
  const [seeding, setSeeding] = useState(false);

  // 다음 예정일 가까운 순. 미접종(nextDue=null)은 뒤로.
  const sorted = useMemo(
    () =>
      [...baechooVaccines].sort((a, b) => {
        if (!a.nextDue && !b.nextDue) return a.name < b.name ? -1 : 1;
        if (!a.nextDue) return 1;
        if (!b.nextDue) return -1;
        return a.nextDue < b.nextDue ? -1 : a.nextDue > b.nextDue ? 1 : 0;
      }),
    [baechooVaccines]
  );

  // 체크 = 오늘 접종 기록 + 다음 예정일 +interval개월
  async function complete(v: BaechooVaccine) {
    const today = todayISO();
    await saveBaechooVaccine({
      ...v,
      lastDone: today,
      nextDue: addMonths(today, v.intervalMonths),
      history: [...v.history, today],
    });
  }

  // 표준 백신 일괄 추가 (이미 있는 이름은 건너뜀)
  async function addStandard() {
    setSeeding(true);
    try {
      const existing = new Set(baechooVaccines.map((v) => v.name));
      for (const name of STANDARD_VACCINES) {
        if (existing.has(name)) continue;
        await saveBaechooVaccine({
          id: "",
          name,
          lastDone: null,
          nextDue: null,
          intervalMonths: 12,
          history: [],
          memo: null,
          createdAt: todayISO(),
        });
      }
    } finally {
      setSeeding(false);
    }
  }

  return (
    <div>
      <SectionTitle
        right={
          <button
            onClick={() => setForm({ open: true })}
            className="text-xs font-bold text-leaf-dark"
          >
            + 백신
          </button>
        }
      >
        예방접종
      </SectionTitle>

      {sorted.length === 0 ? (
        <Card>
          <p className="py-1 text-center text-sm text-stone">
            예방접종을 체크리스트로 관리해 보세요.
          </p>
          <button
            onClick={addStandard}
            disabled={seeding}
            className="mt-2 w-full rounded-xl border border-dashed border-leaf bg-leaf-light/40 py-2.5 text-sm font-bold text-leaf-dark active:scale-[0.99] disabled:opacity-50"
          >
            표준 백신 추가 (종합·코로나·켄넬코프·인플루엔자·광견병)
          </button>
        </Card>
      ) : (
        <Card>
          <div className="space-y-2">
            {sorted.map((v) => {
              const left = v.nextDue ? dday(v.nextDue) : null;
              return (
                <div key={v.id} className="flex items-center gap-3">
                  <Check onClick={() => complete(v)} />
                  <button
                    onClick={() => setForm({ open: true, initial: v })}
                    className="min-w-0 flex-1 text-left"
                  >
                    <span className="block truncate text-sm font-semibold text-ink">
                      {v.name}
                    </span>
                    <span className="block text-[11px] text-stone">
                      {v.lastDone
                        ? `마지막 접종 ${dateLabelKo(v.lastDone)}`
                        : "접종 기록 없음"}
                    </span>
                  </button>
                  {left === null ? (
                    <Pill tone="stone">미접종</Pill>
                  ) : (
                    <Pill tone={left < 0 ? "coral" : left <= 30 ? "gold" : "stone"}>
                      {ddayLabel(v.nextDue!)}
                    </Pill>
                  )}
                </div>
              );
            })}
          </div>
        </Card>
      )}

      {form.open && (
        <VaccineForm
          key={form.initial?.id ?? "new"}
          open={form.open}
          onClose={() => setForm({ open: false })}
          initial={form.initial}
        />
      )}
    </div>
  );
}
```

- [ ] **Step 3: 건강 탭에 삽입 (`components/baechoo/HealthList.tsx`)**

(a) import 추가 — `import HealthTodos from "./HealthTodos";` 다음 줄:

```ts
import VaccineList from "./VaccineList";
```

(b) `<HealthTodos />` 렌더 다음(`+ 건강 기록` 버튼 위)에 삽입:

```tsx
      {/* 할 일 (약·접종 D-day + 매일 양치) */}
      <HealthTodos />

      {/* 예방접종 체크리스트 */}
      <VaccineList />

      <button
        onClick={() => setForm({ open: true })}
```

- [ ] **Step 4: 타입체크 + 린트 + 빌드**

Run: `cd "C:/Users/MGRV/.claude/baechoo-family" && npx eslint components/baechoo/VaccineList.tsx components/baechoo/forms.tsx components/baechoo/HealthList.tsx`
Expected: 에러 없음

Run: `cd "C:/Users/MGRV/.claude/baechoo-family" && npm run build`
Expected: 빌드 성공(타입·린트 통과)

- [ ] **Step 5: localStorage 모드 수동 스모크**

`.env.local`을 `.env.local.bak`으로 임시 rename(메모리 노하우) 후 `npm run dev`. 배추 → 건강 탭에서:
1. "표준 백신 추가" 클릭 → 5종이 "미접종" Pill로 뜨는지
2. 한 백신 체크(✓) → "마지막 접종 오늘날짜" + 다음 예정 `D-365`(또는 윤년 ±1) Pill로 바뀌고 리스트 하단으로 정렬되는지
3. 이름 탭 → VaccineForm 열려 수정/삭제(2단계) 동작
4. "+ 백신"으로 직접 추가

확인 후 `.env.local.bak` → `.env.local` 복원.

(Playwright 사용 시: Next dev 오버레이가 좌하단 클릭 가로채므로 해당 영역 버튼은 navigate 우회.)

- [ ] **Step 6: 커밋**

```bash
cd "C:/Users/MGRV/.claude/baechoo-family"
git add components/baechoo/VaccineList.tsx components/baechoo/forms.tsx components/baechoo/HealthList.tsx
git commit -m "feat: 건강 탭 예방접종 체크리스트 UI(VaccineList·VaccineForm·표준백신 추가)"
```

---

## Self-Review

**1. Spec coverage**

| 스펙 요구 | 태스크 |
| --- | --- |
| 건강 탭 내 신설 섹션(서브탭 아님), 할 일 아래/건강 기록 위 | Task 3 Step 3 |
| 표준 백신 5종 프리셋, 버튼 일괄 추가, 중복 건너뜀 | Task 1(STANDARD_VACCINES) + Task 3 Step 2(addStandard) |
| 매년 부스터 자동(체크=접종일 기록 + nextDue +interval, history 누적) | Task 3 Step 2(complete) + Task 1(addMonths) |
| 카드: 이름·마지막 접종일·D-day Pill(coral/gold≤30/stone, 미접종 stone) | Task 3 Step 2 |
| 데이터 모델 BaechooVaccine + DataSnapshot | Task 1 |
| baechoo_vaccines 테이블(0013, family_all RLS, next_due idx, deleted_at) | Task 2 |
| 표준 엔티티 경로(types/repo/context) + 휴지통 | Task 1 |
| 편집 폼 VaccineForm(이름·접종일·다음예정·주기·메모, 2단계 삭제) | Task 3 Step 1 |
| 건강 기록 연동 안 함 | 설계상 미구현(자동생성 코드 없음) — 준수 |
| addMonths 월말 클램프 | Task 1 Step 3 |

누락 없음.

**2. Placeholder scan**: "TBD"·"적절히 처리"·코드 없는 단계 없음. 모든 코드 스텝에 실제 코드 포함.

**3. Type consistency**: `BaechooVaccine` 필드명(lastDone·nextDue·intervalMonths·history·createdAt)이 types/repo 매퍼(camel↔snake)/context/UI 전반 일치. `saveBaechooVaccine`/`removeBaechooVaccine`·`deleteBaechooVaccine`(repo) 명명 일치. `addMonths`·`STANDARD_VACCINES` 정의처(Task1)와 사용처(Task3) 일치. 휴지통 `"vaccine"` kind·`baechoo_vaccines` 테이블명 일치.
