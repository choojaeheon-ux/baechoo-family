# 지역화폐 거래내역 연동 + 대시보드 이동 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 지역화폐 충전 시 거래내역에 지출을 자동 생성하고(삭제 시 잔액 보정), 지역화폐 섹션을 고정지출 탭에서 대시보드 탭으로 옮긴다.

**Architecture:** `LocalCurrency`에 충전 거래용 기본값(카테고리·결제수단)을, `Transaction`에 역링크(`localCurrencyId`)를 추가한다. 충전(`+충전`) 시 잔액 증가와 함께 지출 거래를 생성하고, 그 거래를 거래내역에서 삭제하면 잔액을 되돌린다. 지역화폐 UI 전체(추가/수정/충전/사용)를 `FixedExpenses.tsx`에서 `Dashboard.tsx`로 이동한다.

**Tech Stack:** Next.js 16(App Router) · React 19 · TypeScript · Tailwind v4 · Supabase(클라우드) / localStorage(폴백)

## Global Constraints

- 언어: 모든 UI 문구·커밋 메시지 한국어.
- 테스트 프레임워크 없음. 각 태스크의 검증 = `npx tsc --noEmit` + (해당 시) `npx next build`. UI 동작은 명시된 수동 확인으로 검증.
- DDL(스키마 변경)은 사용자가 Supabase SQL Editor에 **직접 실행**한다. 코드는 컬럼이 이미 있다고 가정하되, localStorage 모드는 마이그레이션 없이 동작해야 한다.
- 커밋 메시지 말미: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- 영문 표기는 `baechoo`. 이모지 절제(기존 코드 톤 유지).

---

### Task 1: 데이터 모델 · 영속화 매핑 · 마이그레이션

새 필드를 타입에 추가하고, 영속화 매핑과 기존 생성자를 모두 갱신해 빌드가 깨지지 않게 한다(필드는 패스스루로만 채운다 — 실제 입력 UI는 Task 2·5).

**Files:**
- Modify: `lib/types.ts` (`LocalCurrency`, `Transaction` 인터페이스)
- Modify: `lib/repo.ts:150-205` (`toTxn`/`fromTxn`/`toLc`/`fromLc`)
- Modify: `components/budget/forms.tsx` (`TransactionForm` submit, `LocalCurrencyForm` submit — 패스스루)
- Create: `supabase/migrations/0005_local_currency_link.sql`

**Interfaces:**
- Produces: `LocalCurrency.defaultCategoryId: string | null`, `LocalCurrency.defaultPaymentMethodId: string | null`, `Transaction.localCurrencyId: string | null`

- [ ] **Step 1: `lib/types.ts` — `LocalCurrency`에 필드 2개 추가**

`LocalCurrency` 인터페이스를 다음으로 교체:

```ts
// 지역화폐 (온누리·경기지역화폐 등) — 선불 지갑, 매월 충전·이월 잔액
export interface LocalCurrency {
  id: string;
  name: string;
  balance: number; // 이월 포함 현재 잔액
  monthlyCharge: number; // 매월 충전금(기본값)
  defaultCategoryId: string | null; // 충전 거래에 쓸 기본 카테고리
  defaultPaymentMethodId: string | null; // 충전 거래에 쓸 기본 결제수단
}
```

- [ ] **Step 2: `lib/types.ts` — `Transaction`에 `localCurrencyId` 추가**

`Transaction` 인터페이스의 `recurringId: string | null;` 줄 바로 아래에 추가:

```ts
  localCurrencyId: string | null; // 지역화폐 충전으로 생성된 거래면 해당 지역화폐 id
```

- [ ] **Step 3: `lib/repo.ts` — `toLc`/`fromLc` 매핑 추가**

`toLc`(194-199)와 `fromLc`(200-205)를 다음으로 교체:

```ts
const toLc = (r: Record<string, unknown>): LocalCurrency => ({
  id: r.id as string,
  name: r.name as string,
  balance: Number(r.balance ?? 0),
  monthlyCharge: Number(r.monthly_charge ?? 0),
  defaultCategoryId: (r.default_category_id as string) ?? null,
  defaultPaymentMethodId: (r.default_payment_method_id as string) ?? null,
});
const fromLc = (x: LocalCurrency) => ({
  id: x.id,
  name: x.name,
  balance: x.balance,
  monthly_charge: x.monthlyCharge,
  default_category_id: x.defaultCategoryId,
  default_payment_method_id: x.defaultPaymentMethodId,
});
```

- [ ] **Step 4: `lib/repo.ts` — `toTxn`/`fromTxn`에 `local_currency_id` 매핑 추가**

`toTxn`의 `recurringId: (r.recurring_id as string) ?? null,` 줄 아래에 추가:

```ts
  localCurrencyId: (r.local_currency_id as string) ?? null,
```

`fromTxn`의 `recurring_id: x.recurringId,` 줄 아래에 추가:

```ts
  local_currency_id: x.localCurrencyId,
```

- [ ] **Step 5: `components/budget/forms.tsx` — `TransactionForm` submit에 패스스루 추가**

`TransactionForm`의 `submit()` 안 `saveTransaction({...})` 객체에서 `recurringId: initial?.recurringId ?? null,` 줄 아래에 추가:

```ts
      localCurrencyId: initial?.localCurrencyId ?? null,
```

- [ ] **Step 6: `components/budget/forms.tsx` — `LocalCurrencyForm` submit에 패스스루 추가**

`LocalCurrencyForm`의 `submit()` 안 `saveLocalCurrency({...})` 객체(현재 `id`/`name`/`balance`/`monthlyCharge`)에 두 줄 추가:

```ts
      defaultCategoryId: initial?.defaultCategoryId ?? null,
      defaultPaymentMethodId: initial?.defaultPaymentMethodId ?? null,
```

- [ ] **Step 7: 마이그레이션 파일 생성 — `supabase/migrations/0005_local_currency_link.sql`**

```sql
-- 배추가족 가계부 — 지역화폐 충전 거래 연동
-- 0001~0004 실행한 개인 Supabase 프로젝트의 SQL Editor에 붙여넣어 실행하세요.

-- 지역화폐: 충전 거래에 쓸 기본 카테고리·결제수단
alter table local_currencies
  add column if not exists default_category_id text,
  add column if not exists default_payment_method_id text;

-- 거래: 어떤 지역화폐 충전으로 생성됐는지 역링크 (삭제 시 잔액 보정용)
alter table transactions
  add column if not exists local_currency_id text;
```

- [ ] **Step 8: 타입체크**

Run: `npx tsc --noEmit`
Expected: 종료 코드 0 (에러 없음)

- [ ] **Step 9: 커밋**

```bash
git add lib/types.ts lib/repo.ts components/budget/forms.tsx supabase/migrations/0005_local_currency_link.sql
git commit -m "feat(지역화폐): 충전 거래 연동용 데이터 모델·매핑·마이그레이션

LocalCurrency에 기본 카테고리·결제수단, Transaction에 localCurrencyId 추가.
repo 매핑·기존 생성자 패스스루·마이그레이션 0005 포함.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: 지역화폐 폼에 기본 카테고리·결제수단 입력 추가

`LocalCurrencyForm`에서 충전 거래에 쓸 기본 카테고리·결제수단을 실제로 지정할 수 있게 한다.

**Files:**
- Modify: `components/budget/forms.tsx` (`LocalCurrencyForm` 전체)

**Interfaces:**
- Consumes: `LocalCurrency.defaultCategoryId`, `LocalCurrency.defaultPaymentMethodId` (Task 1)

- [ ] **Step 1: `LocalCurrencyForm` 본문 교체**

`LocalCurrencyForm`(현재 694-771) 전체를 다음으로 교체. 카테고리·결제수단 state와 select UI를 추가하고, submit에서 함께 저장한다(Task 1의 패스스루를 실제 값으로 대체):

```tsx
export function LocalCurrencyForm({
  open,
  onClose,
  initial,
}: {
  open: boolean;
  onClose: () => void;
  initial?: LocalCurrency;
}) {
  const { categories, paymentMethods, saveLocalCurrency, removeLocalCurrency } =
    useData();
  const cats = categories.filter((c) => c.type === "expense");
  const [name, setName] = useState(initial?.name ?? "");
  const [balance, setBalance] = useState(initial ? String(initial.balance) : "0");
  const [monthly, setMonthly] = useState(
    initial ? String(initial.monthlyCharge) : "0"
  );
  const [categoryId, setCategoryId] = useState(
    initial?.defaultCategoryId ?? cats[0]?.id ?? ""
  );
  const [paymentMethodId, setPaymentMethodId] = useState(
    initial?.defaultPaymentMethodId ?? ""
  );

  const bal = Number(balance.replace(/[^0-9]/g, ""));
  const mon = Number(monthly.replace(/[^0-9]/g, ""));
  const valid = name.trim().length > 0;

  async function submit() {
    if (!valid) return;
    await saveLocalCurrency({
      id: initial?.id ?? "",
      name: name.trim(),
      balance: bal,
      monthlyCharge: mon,
      defaultCategoryId: categoryId || null,
      defaultPaymentMethodId: paymentMethodId || null,
    });
    onClose();
  }

  return (
    <Sheet open={open} onClose={onClose} title={initial ? "지역화폐 수정" : "지역화폐 추가"}>
      <Field label="이름">
        <input
          className={inputCls}
          value={name}
          onChange={(e) => setName(e.target.value)}
          placeholder="예: 온누리상품권, 경기지역화폐"
        />
      </Field>
      <Field label="매월 충전금">
        <input
          className={inputCls + " text-right tabular"}
          inputMode="numeric"
          value={monthly ? Number(mon).toLocaleString("ko-KR") : ""}
          onChange={(e) => setMonthly(e.target.value)}
          placeholder="0"
        />
      </Field>
      <Field label="현재 잔액 (이월 포함)">
        <input
          className={inputCls + " text-right tabular"}
          inputMode="numeric"
          value={balance ? Number(bal).toLocaleString("ko-KR") : ""}
          onChange={(e) => setBalance(e.target.value)}
          placeholder="0"
        />
      </Field>
      <Field label="충전 시 기록할 카테고리">
        <select
          className={inputCls}
          value={categoryId}
          onChange={(e) => setCategoryId(e.target.value)}
        >
          {cats.map((c) => (
            <option key={c.id} value={c.id}>
              {c.icon} {c.name}
            </option>
          ))}
        </select>
      </Field>
      <Field label="충전 시 결제수단">
        <select
          className={inputCls}
          value={paymentMethodId}
          onChange={(e) => setPaymentMethodId(e.target.value)}
        >
          <option value="">선택 안함</option>
          {paymentMethods.map((p) => (
            <option key={p.id} value={p.id}>
              {p.name} ({PAYMENT_KIND_LABEL[p.kind]})
            </option>
          ))}
        </select>
      </Field>
      <div className="mt-2">
        <PrimaryButton onClick={submit} disabled={!valid}>
          저장
        </PrimaryButton>
      </div>
      {initial && (
        <button
          onClick={async () => {
            await removeLocalCurrency(initial.id);
            onClose();
          }}
          className="mt-3 w-full py-2 text-sm text-coral"
        >
          삭제
        </button>
      )}
    </Sheet>
  );
}
```

- [ ] **Step 2: 타입체크**

Run: `npx tsc --noEmit`
Expected: 종료 코드 0

- [ ] **Step 3: 커밋**

```bash
git add components/budget/forms.tsx
git commit -m "feat(지역화폐): 폼에 충전 기본 카테고리·결제수단 입력 추가

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: 충전 거래 삭제 시 잔액 자동 보정

거래내역에서 충전 거래(`localCurrencyId` 보유)를 삭제하면, 그 지역화폐 잔액을 충전액만큼 되돌린다.

**Files:**
- Modify: `lib/data-context.tsx:143-146` (`removeTransaction`)

**Interfaces:**
- Consumes: `Transaction.localCurrencyId` (Task 1), `localCurrencies` state, `repo.saveLocalCurrency`

- [ ] **Step 1: `removeTransaction` 교체**

`removeTransaction`(143-146)를 다음으로 교체. 삭제 전 state에서 거래를 찾아 보정한다(`value`는 `transactions`·`localCurrencies`를 deps로 memo되어 최신 state를 본다):

```ts
      removeTransaction: async (id) => {
        const tx = transactions.find((t) => t.id === id);
        await repo.deleteTransaction(id);
        setTransactions((p) => p.filter((x) => x.id !== id));
        if (tx?.localCurrencyId) {
          const lc = localCurrencies.find((l) => l.id === tx.localCurrencyId);
          if (lc) {
            const saved = await repo.saveLocalCurrency({
              ...lc,
              balance: Math.max(lc.balance - tx.amount, 0),
            });
            upsertLocal(setLocalCurrencies, saved);
          }
        }
      },
```

- [ ] **Step 2: 타입체크**

Run: `npx tsc --noEmit`
Expected: 종료 코드 0 (`upsertLocal`·`repo`·`localCurrencies`·`transactions` 모두 이미 스코프에 존재)

- [ ] **Step 3: 커밋**

```bash
git add lib/data-context.tsx
git commit -m "feat(지역화폐): 충전 거래 삭제 시 잔액 자동 보정

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: 지역화폐 섹션을 대시보드로 이동 (동작 동일)

지역화폐 UI 전체를 `FixedExpenses.tsx`에서 `Dashboard.tsx`로 옮긴다. 이 태스크에서는 충전 동작이 아직 잔액만 조정한다(거래 생성은 Task 5).

**Files:**
- Modify: `components/budget/Dashboard.tsx` (지역화폐 섹션·state·시트 추가)
- Modify: `components/budget/FixedExpenses.tsx` (지역화폐 관련 전부 제거)

**Interfaces:**
- Consumes: `useData()`의 `localCurrencies`, `saveLocalCurrency`; `LocalCurrencyForm`, `AmountSheet` (forms.tsx)

- [ ] **Step 1: `FixedExpenses.tsx` — 지역화폐 state 제거**

다음 두 줄(현재 38-39)을 삭제:

```ts
  const [chargeLc, setChargeLc] = useState<LocalCurrency | null>(null);
  const [useLc, setUseLc] = useState<LocalCurrency | null>(null);
```

그리고 lcOpen·editLc 줄(현재 36-37)도 삭제:

```ts
  const [lcOpen, setLcOpen] = useState(false);
  const [editLc, setEditLc] = useState<LocalCurrency | null>(null);
```

- [ ] **Step 2: `FixedExpenses.tsx` — `useData` 구조분해에서 미사용 항목 제거**

`useData()` 구조분해에서 `localCurrencies,` 와 `saveLocalCurrency,` 를 제거한다(남은 사용처 없음). 결과:

```ts
  const {
    recurring,
    paymentMethods,
    categoryById,
    paymentMethodById,
  } = useData();
```

- [ ] **Step 3: `FixedExpenses.tsx` — 지역화폐 섹션 JSX 제거**

`{/* 지역화폐 */}` 주석부터 그 `</Card>`까지(현재 146-202) 블록 전체를 삭제한다. (바로 위 `할부·대출` `</Card>`와 아래 `{/* 결제수단 관리 */}` 사이의 지역화폐 SectionTitle + Card 전부.)

- [ ] **Step 4: `FixedExpenses.tsx` — `LocalCurrencyForm`·`AmountSheet` 렌더 제거**

JSX 하단에서 `LocalCurrencyForm`(현재 248-252, Task 직전 작업으로 `{lcOpen && (...)}`로 감싸진 형태) 블록과 두 개의 `AmountSheet`(현재 253-275) 블록을 모두 삭제한다. `RecurringForm`·`PaymentMethodForm` 블록은 남긴다.

- [ ] **Step 5: `FixedExpenses.tsx` — import 정리**

forms import에서 `LocalCurrencyForm,` 과 `AmountSheet,` 제거:

```ts
import {
  RecurringForm,
  PaymentMethodForm,
} from "./forms";
```

types import에서 `LocalCurrency,` 제거(`PaymentMethod`·`RecurringExpense`·`RecurringKind`는 유지):

```ts
import {
  PAYMENT_KIND_LABEL,
  type PaymentMethod,
  type RecurringExpense,
  type RecurringKind,
} from "@/lib/types";
```

- [ ] **Step 6: `FixedExpenses.tsx` 타입체크**

Run: `npx tsc --noEmit`
Expected: 종료 코드 0 (미사용 import/state 없음)

- [ ] **Step 7: `Dashboard.tsx` — import·hook 추가**

상단 import에 useState 추가:

```ts
import { useState } from "react";
```

ui import에 별도 변경 없음(Card·SectionTitle·Empty 이미 사용). forms import 추가:

```ts
import { LocalCurrencyForm, AmountSheet } from "./forms";
```

types import 추가:

```ts
import type { LocalCurrency } from "@/lib/types";
```

`useData()` 구조분해에 `localCurrencies`, `saveLocalCurrency` 추가:

```ts
  const {
    transactions,
    budgets,
    recurring,
    goals,
    categoryById,
    localCurrencies,
    saveLocalCurrency,
  } = useData();
```

- [ ] **Step 8: `Dashboard.tsx` — 지역화폐 state 추가**

컴포넌트 본문 상단(파생값 계산 전, `const monthTxns = ...` 위)에 추가:

```ts
  const [lcOpen, setLcOpen] = useState(false);
  const [editLc, setEditLc] = useState<LocalCurrency | null>(null);
  const [chargeLc, setChargeLc] = useState<LocalCurrency | null>(null);
  const [useLc, setUseLc] = useState<LocalCurrency | null>(null);
```

- [ ] **Step 9: `Dashboard.tsx` — 지역화폐 섹션 JSX 추가**

"이번 달 예산" 요약 Card 닫는 `</Card>` 바로 다음(저축 목표 SectionTitle 앞)에 다음 블록 삽입:

```tsx
      {/* 지역화폐 */}
      <SectionTitle
        right={
          <button
            onClick={() => {
              setEditLc(null);
              setLcOpen(true);
            }}
            className="text-xs font-semibold text-leaf"
          >
            + 추가
          </button>
        }
      >
        지역화폐
      </SectionTitle>
      <Card className="space-y-2">
        {localCurrencies.length === 0 ? (
          <Empty>온누리·경기지역화폐 등을 등록하고 매월 충전·잔액을 관리하세요.</Empty>
        ) : (
          localCurrencies.map((lc) => (
            <div key={lc.id} className="rounded-xl bg-cream p-3">
              <div className="flex items-center justify-between">
                <button
                  onClick={() => {
                    setEditLc(lc);
                    setLcOpen(true);
                  }}
                  className="text-left"
                >
                  <p className="text-sm font-bold text-ink">🎟️ {lc.name}</p>
                  <p className="text-[11px] text-stone">
                    매월 충전 {won(lc.monthlyCharge)}
                  </p>
                </button>
                <div className="text-right">
                  <p className="text-[11px] text-stone">잔액(이월 포함)</p>
                  <p className="text-lg font-extrabold tabular text-leaf-dark">
                    {won(lc.balance)}
                  </p>
                </div>
              </div>
              <div className="mt-2 flex gap-2">
                <button
                  onClick={() => setChargeLc(lc)}
                  className="flex-1 rounded-lg bg-leaf py-1.5 text-xs font-semibold text-white active:scale-[0.98]"
                >
                  + 충전
                </button>
                <button
                  onClick={() => setUseLc(lc)}
                  className="flex-1 rounded-lg border border-line bg-card py-1.5 text-xs font-semibold text-stone active:scale-[0.98]"
                >
                  − 사용
                </button>
              </div>
            </div>
          ))
        )}
      </Card>
```

- [ ] **Step 10: `Dashboard.tsx` — 폼·시트 렌더 추가**

컴포넌트 최상위 닫는 `</div>`(맨 끝 `오늘도 배추가족 화이팅` 아래) 직전에 추가:

```tsx
      {lcOpen && (
        <LocalCurrencyForm
          open={lcOpen}
          onClose={() => setLcOpen(false)}
          initial={editLc ?? undefined}
        />
      )}
      <AmountSheet
        key={`charge-${chargeLc?.id ?? "none"}`}
        open={!!chargeLc}
        onClose={() => setChargeLc(null)}
        title={`${chargeLc?.name ?? ""} 충전`}
        label="충전 금액"
        defaultAmount={chargeLc?.monthlyCharge ?? 0}
        onConfirm={(amt) => {
          if (chargeLc)
            saveLocalCurrency({ ...chargeLc, balance: chargeLc.balance + amt });
        }}
      />
      <AmountSheet
        key={`use-${useLc?.id ?? "none"}`}
        open={!!useLc}
        onClose={() => setUseLc(null)}
        title={`${useLc?.name ?? ""} 사용`}
        label="사용 금액"
        onConfirm={(amt) => {
          if (useLc)
            saveLocalCurrency({ ...useLc, balance: Math.max(useLc.balance - amt, 0) });
        }}
      />
```

- [ ] **Step 11: 빌드 검증**

Run: `npx next build`
Expected: `✓ Compiled successfully` + TypeScript 통과, 에러 없음

- [ ] **Step 12: 수동 확인 (dev 서버)**

Run: `npm run dev` → 대시보드 탭 진입
Expected: "이번 달 예산" 아래에 지역화폐 섹션 표시(추가/충전/사용/수정 동작), 고정지출 탭에는 지역화폐 섹션 없음

- [ ] **Step 13: 커밋**

```bash
git add components/budget/Dashboard.tsx components/budget/FixedExpenses.tsx
git commit -m "feat(지역화폐): 섹션을 고정지출 탭에서 대시보드로 이동

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: 충전 시 지출 거래 자동 생성

대시보드의 `+충전` 확정 시 잔액 증가에 더해 지출 거래를 자동 생성한다(카테고리·결제수단 = 지역화폐 기본값, fallback = 첫 지출 카테고리).

**Files:**
- Modify: `components/budget/Dashboard.tsx` (`useData` 구조분해 + import + 충전 `AmountSheet` onConfirm)

**Interfaces:**
- Consumes: `useData()`의 `saveTransaction`, `categories`; `todayISO` (lib/format); `LocalCurrency.defaultCategoryId`·`defaultPaymentMethodId` (Task 1)

- [ ] **Step 1: `Dashboard.tsx` — `saveTransaction`·`categories` 구조분해 추가**

`useData()` 구조분해에 `saveTransaction`, `categories` 추가:

```ts
  const {
    transactions,
    budgets,
    recurring,
    goals,
    categoryById,
    localCurrencies,
    saveLocalCurrency,
    saveTransaction,
    categories,
  } = useData();
```

- [ ] **Step 2: `Dashboard.tsx` — `todayISO` import 추가**

format import에 `todayISO` 추가(기존: `won, weekdayKo, ddayLabel, dday`):

```ts
import { won, weekdayKo, ddayLabel, dday, todayISO } from "@/lib/format";
```

- [ ] **Step 3: `Dashboard.tsx` — 충전 `AmountSheet`의 onConfirm 교체**

충전용 `AmountSheet`(`key={`charge-...`}`)의 `onConfirm`을 다음으로 교체:

```tsx
        onConfirm={(amt) => {
          if (!chargeLc) return;
          saveLocalCurrency({ ...chargeLc, balance: chargeLc.balance + amt });
          const fallbackCat = categories.find((c) => c.type === "expense");
          const categoryId = chargeLc.defaultCategoryId ?? fallbackCat?.id ?? "";
          saveTransaction({
            id: "",
            date: todayISO(),
            amount: amt,
            type: "expense",
            categoryId,
            memo: `${chargeLc.name} 충전`,
            member: "chuchu",
            paymentMethodId: chargeLc.defaultPaymentMethodId,
            isSpecial: false,
            habitTag: null,
            source: "auto",
            recurringId: null,
            localCurrencyId: chargeLc.id,
            isPaid: true,
          });
        }}
```

- [ ] **Step 4: 빌드 검증**

Run: `npx next build`
Expected: `✓ Compiled successfully` + TypeScript 통과

- [ ] **Step 5: 수동 확인 (dev 서버)**

Run: `npm run dev`
Expected:
1. 지역화폐 추가 시 카테고리·결제수단 지정 → `+충전` 5만원 → 거래내역 탭에 `OO 충전` 지출 5만원 생성 + 대시보드 예산 사용액 +5만원
2. 그 충전 거래를 거래내역에서 삭제 → 지역화폐 잔액 5만원 감소
3. `−사용` 3만원 → 잔액만 -3만원, 거래내역에 신규 거래 없음

- [ ] **Step 6: 커밋**

```bash
git add components/budget/Dashboard.tsx
git commit -m "feat(지역화폐): 충전 시 지출 거래 자동 생성

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## 배포 전 필수 (사용자 작업)

라이브(클라우드) 반영 전에 사용자가 Supabase SQL Editor에서 `supabase/migrations/0005_local_currency_link.sql`을 실행해야 한다. 미실행 시 클라우드 모드에서 충전 거래 저장(`local_currency_id` 컬럼)·지역화폐 기본값 저장이 실패한다. localStorage 모드(로컬 테스트)는 마이그레이션 없이 동작한다.

## Self-Review

- **Spec 커버리지:** 충전=지출 자동 생성(Task 5), 지역화폐별 기본 카테고리·결제수단(Task 1·2·5), 삭제 시 잔액 보정 + `localCurrencyId`(Task 1·3), 대시보드 통째 이동(Task 4), 마이그레이션(Task 1) — 모든 스펙 항목에 대응 태스크 존재.
- **타입 일관성:** `defaultCategoryId`/`defaultPaymentMethodId`/`localCurrencyId` 명칭이 types·repo·forms·data-context·Dashboard 전반에서 동일. `saveTransaction` 객체는 `Transaction` 전체 필드를 채움(Task 1에서 `localCurrencyId` 추가됨).
- **플레이스홀더:** 없음. 각 코드 스텝에 실제 코드 포함.
- **빌드 안전성:** 타입에 필드 추가하는 Task 1이 모든 기존 생성자(TransactionForm·LocalCurrencyForm submit)도 함께 패치해 태스크 경계마다 빌드 green.
